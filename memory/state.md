# HireView — Current State

*Last updated: 2026-07-01 (seeded from HireView-Dev-Log.docx, Architecture-Redesign doc, and git log — update this in place as state changes, don't just append)*

## Status: live, in active use at Brillio

Deployed workflow tool for a recruiter working two roles at once at Brillio. Multi-machine by design (work + personal computer).

## What's shipped

- **JD Analyzer** — paste a JD, get a full LinkedIn Recruiter sourcing brief: wide (discovery, 5k–15k results) and narrow (execution, 500–1k) filter sets, boolean strings, seniority, industries, company size, spotlights.
- **Resume Screener** — bulk PDF/Word upload, Claude scores each against the JD (0–100, must-have 65% / nice-to-have 35% weighting), ranked results, history with status pipeline (New Applicant → Recruiter Screen → Contacted → Screening → Archived). Sub-45 scores excluded from history (raised from 30 on 2026-07-01).
- **Credibility Checker (Phase 4)** — optional second resume + LinkedIn PDF cross-referenced against the screened resume; surfaces discrepancies before a TA call. LinkedIn PDF stored to Supabase Storage during check. Replaced the old "compare" feature.
- **Projects layer** — a project = one open role. Supabase `projects` table; `screenings.project_id` FK. This became the app's core organizing unit.
- **Architecture Redesign (shipped 2026-06-29)** — nav collapsed from 5 items to 2: **Projects** and **All Candidates**. Old routes (`/jd-analyzer`, `/history`, `/tracker`, `/screener`) redirect. Every role/project is a self-contained workspace with tabs: **Filters** (LinkedIn filters, copy buttons), **Screen** (uploader scoped to the role), **Pipeline** (candidate cards — status, flags, notes, credibility), **Tracker** (swimlane TA→L1→L2→In-Person→Offer/Reject, drag-to-reorder, slide-over drawer), **Settings**.
- **Tracker tab (added 2026-06-30)** — was originally a separate page, now folded in as a 5th project tab. Excel export. Scheduled/unscheduled indicator dots.
- **Interview View (added 2026-07-06)** — two popup windows per candidate on pipeline cards: left-half resume/LinkedIn viewer (`/interview/[id]/document`) and top-right notes window (`/interview/[id]`) with AI-generated interview questions (cached, lazy) and debounced notes. Windows tile exactly using complement sizing math. "View resume" button in expanded card also opens the document window.
- **Card redesign (2026-07-06)** — filename removed, credibility signal emoji removed from collapsed state, career story shows role hierarchy (bold headers, subordinate bullets, softer summary), roles displayed oldest-first (chronological), Credibility Check section collapsible (default closed).

## What's shipped (added 2026-07-07)

- **Multi-user auth (Task 1)** — Supabase Auth email/password, `@supabase/ssr` middleware, `/login` page, `user_id` on all three tables, recruiter isolation, admin sees all, `/admin/users` management page. User creation via `createUser` (no invite email) with pre-filled temp password `HireView2025!`. Sign-in is immediate.
- **Analytics dashboard (Task 2)** — `/analytics` admin-only. `screening_batches` aggregate table tracks all runs including below-threshold rejections (`scores int[]`, `passed_count`, `total_count`). 4 stat cards: Resumes Screened / Passed to Pipeline / Filtered Out / Time Saved. Score distribution, daily activity, by-role and by-recruiter tables. Date range + recruiter filter.
- **Calibration feedback UI (Task 3)** — Thumbs up/down in expanded pipeline cards. Thumbs down → correction modal (score slider + reason). `POST /api/history/[id]/calibrate` re-downloads resume + saves as calibration_examples. Core scoring logic untouched.
- **Configurable score threshold (Task 4)** — `score_threshold` column on projects (default 45). Slider in Settings tab. `screen-resumes` reads per-project threshold.
- **SiteHeader account dropdown** — person icon → dropdown with email (green dot), Change password link, Sign out. Admin nav links (Analytics, Team) with visual divider.
- **`/auth/set-password` page** — lets users update their temp password. Accessible via header dropdown.
- **JD file upload** — "Upload file" button in New Role modal. Supports PDF, DOCX, TXT via `POST /api/extract-jd-text` (reuses `extractResumeText`).

## What's shipped (added 2026-07-07, session 3)

- **Access request feature** — "Don't have access? Request it →" link on login page expands a collapsible form (email + name + message). Submissions saved to `access_requests` table + email to vladvashchuk2005@gmail.com via Resend (requires `RESEND_API_KEY` env var). Team page shows amber "Pending requests" section with Approve (creates account with `HireView2026!`) / Dismiss. SiteHeader shows pulsing amber dot badge on Team link when pending requests exist.
- **Keyword highlighting + match counts** — must-have (amber) and nice-to-have (violet) keywords highlighted inline in career trajectory text. Score chips show `X/Y kw` badge for fraud detection visibility.
- **Invite flow overhaul** — switched from `inviteUserByEmail` to `createUser` (no email rate limits, immediate activation). Default temp password `HireView2026!` pre-filled in form.
- **Photo in drawer** — candidate profile photo moved from chip to drawer as circular profile photo; still displays on tracker chip.
- **TypeScript fix** — `cookiesToSet` type annotation in `lib/supabase-server.ts` and `middleware.ts`.

## What's shipped (added 2026-07-08, Phase 1.1)

- **Duplicate Resume Detection (Phase 1.1 — merged to main)** — content fingerprinting (skills hash, responsibility vectors, metric claims, career arc signature) built from a second Claude extraction call per saved candidate, deliberately never matching on name/contact/company. New `resume_fingerprints` table, `duplicate_flag`/`duplicate_match_id` on `screenings`. Red "Duplicate detected" badge on collapsed Pipeline and All Candidates cards, click-to-jump to the matching candidate. Matching scoped to same project for v1 — cross-project matching is Feature 1.4, once Teams (1.3) exists. Built on `phase-1-fraud-prevention` (merged from `generalize-credibility-crossref` + `main`), verified with swapped-identity test resumes, PR #2 merged into `main` 2026-07-08.

## What's shipped (added 2026-07-08/09, Phase 1.2 — merged to main)

- **Recruiter Attribution** — full append-only action history (`screening_actions` table) logging status changes, tracker stage moves, flags, notes, and credibility checks with who and when. Threaded through `updateScreening`'s wrapper functions and `upsertTrackerEntry` via an optional `actorUserId` param, sourced from `getAuthUser()` in the two API routes that handle recruiter-driven changes. "Activity" section on the expanded Pipeline card shows the timeline with a colored initial avatar + bold email + plain-language description ("V moved Shiraz Amin to Recruiter Screen on Jul 9, 2026"). Migration run, live-tested (status change + flag), PR merged to main.

## What's shipped (added 2026-07-09, Phase 1.3 — code complete + migrated + live-tested, not yet committed)

- **Teams Architecture** — `teams` + `team_members` tables. `team_id` added to `projects` (source of truth) and denormalized onto `screenings` (from the project, at save time) so list queries filter with `.in("team_id", ...)` instead of a join. `lib/teams.ts` (create team, list with members, add/remove member, `getUserTeamIds`/`getPrimaryTeamId`). New `teamIdsFilter()` in `lib/auth.ts` — added alongside `userIdFilter` (not replacing it — `userIdFilter` stays exactly as-is because `screen-resumes/route.ts`, a do-not-touch file, depends on its sync signature). `/api/projects` GET and `/api/history` GET now scope by team instead of by individual `user_id`. New projects auto-assign to the creator's own team (no selector UI yet — deferred until someone actually belongs to 2+ teams, see decisions-log). Team management UI added to `/admin/users` (new "Teams" section: create team, add/remove members) — new `/api/admin/teams` + `/api/admin/teams/[id]/members` routes.
- **Real behavior change, not just additive:** this replaces per-user screening isolation with per-team isolation. Recruiters on the same team now see each other's projects and candidates, not just their own. Migration ran 2026-07-09; Vlad confirmed keeping the default "General" team as-is (both him and Teti share it for now — deliberate, not an oversight).
- Migrated and live-tested by Vlad: two teams created, add/remove member on the Teams UI confirmed working. One polish fix during testing — add/remove now updates the UI optimistically instead of waiting on a refetch (see decisions-log).
- Committed via Claude Code from Vlad's own machine (this session's sandbox git stayed broken all along — see open-questions.md): commit `b33f2b1` "feat: add Teams architecture (Phase 1.3)" on branch `phase-1-3-teams-architecture`, pushed to origin. PR not opened via `gh` (not on PATH in that environment either) — compare-URL fallback given instead: `https://github.com/vashVlad/HireView/compare/main...phase-1-3-teams-architecture?expand=1`. Not yet merged — awaiting Vlad's review.

## What's built, pending migration + live test (2026-07-09, Phase 1.4)

- **Candidate History Alert** — extends Phase 1.1's fingerprint matching across projects within the same team (the boundary 1.3 defined). Two new banners on the collapsed card, distinct from 1.1's existing same-project "Duplicate detected" (untouched): yellow "Previously seen" (this content pattern applied to another project before, no prior fraud signal) and red "Known fraud pattern" (the matched record, or this one, already carries a same-project `duplicate_flag` or was already flagged — escalation propagates through the match chain). New `history_alert_type`/`history_alert_match_id` columns on `screenings`, `team_id` denormalized onto `resume_fingerprints`. New `findCrossProjectMatch`/`markHistoryAlertPair`/`getScreeningFraudSignals` in `lib/resumeFingerprints.ts`, wired into `saveScreening`'s existing best-effort fingerprinting block. Both banners link to the matched candidate's project (resolved server-side via `enrichHistoryAlerts` — the match is usually not in the current page's already-loaded list, unlike 1.1's same-project case). Migration: `supabase-migration-history-alert.sql`, not yet run.
- **Live-tested and confirmed working 2026-07-09**, after debugging an app-wide outage (see decisions-log — `SCREENING_COLUMNS` referenced the new columns before the migration ran) and a real `getProject()` bug (missing `team_id` in its select, fixed). Confirmed: same resume into two projects in the same team correctly shows "Previously seen"; when the matched record already had a same-project `duplicate_flag`, it correctly escalated both sides to "Known fraud pattern" instead. Same-project resubmission correctly stays on 1.1's "Duplicate detected" and does not also trigger 1.4's banner (they're deliberately separate signals).
- **Follow-up UI addition, same session:** Projects page (`app/projects/page.tsx`) now groups project cards by team when more than one team is represented (admin viewing multiple teams, or a recruiter in 2+ teams) — single-team views are unaffected, no header shown. `ProjectSummary.teamName` added, resolved server-side in `getProjectSummaries`. Team member chips (colored initials, email on hover) added to each group header via new `GET /api/teams`.
- **Fixed same session:** admin-run screenings weren't showing in the Activity timeline (`saveScreening` was using `userIdFilter()`'s admin-is-undefined result for attribution) — now re-resolves the true session user via `getAuthUser()` for that log call, without touching `screen-resumes/route.ts`.
- **Merged to main, confirmed 2026-07-09.** Committed via Claude Code as `b33f2b1` (1.3), `ab916ce` (1.4 + fixes + team chips), `f9d62f8` (1.5) — each merged into main as its own PR (#4, #5, #6) by Vlad via the compare-URL flow (`gh` was never on PATH in that environment). GitHub PR list confirmed: 0 open, 6 closed/merged total (including #1–#3 for earlier features). Phase 1 (Fraud Prevention, 1.1–1.5) is fully in main.

## What's built, committed, pending live test + merge (2026-07-09, FunnelView)

- **FunnelView — Manager Visibility.** Admin-only, isolated module: `lib/funnelview/data.ts` (`getFunnelData()`, own direct Supabase queries, no reuse of `SCREENING_COLUMNS`), `lib/funnelview/types.ts`, `app/api/funnelview/route.ts` (admin-gated, same pattern as `/api/analytics`), `app/funnelview/page.tsx`. Nav link added to `SiteHeader.tsx` admin section.
- **Funnel:** Total Screened (from `screening_batches.total_count`, org-wide) → Passed Threshold (count of `screenings` rows) → Reached Out (status ≠ `new_applicant`) → TA → L1 → L2 → In-Person → Offer, each showing count + conversion % from the immediately preceding stage. Archived/rejected candidates reported as a separate count (not folded into the cumulative bars), attributed to whichever stage they last reached via `previous_stage`.
- **Source split:** inbound (`linkedin_mode = false`, applied via job board) vs outbound (`linkedin_mode = true`, sourced via LinkedIn) — confirmed this mapping by reading the actual UI copy in `app/projects/[id]/page.tsx` ("Screen LinkedIn profiles" toggle) rather than assuming.
- **Candidate table:** name, role, current stage (+ previous if different), source badge, recruiter (colored initial avatar + email, reusing `lib/avatarColor.ts`). Filterable by role, archived/rejected hidden by default with a toggle.
- No new schema, no new tables, no new role, org-wide not team-scoped (see decisions-log). Zero entanglement with `lib/screenings.ts` or the do-not-touch files.
- **Merged to main, 2026-07-09.** `88b1d02` "feat: add FunnelView (Manager Visibility)" merged via merge commit `34182e0`. (`b22b2ab`, the docs/ + supabase/migrations/ reorg, had already reached main earlier via a separate PR #7 — Claude Code caught this before merging, so the actual delta merged here was FunnelView alone, not both.) `npx tsc --noEmit` clean, `npm run build` succeeded — `/funnelview` and `/api/funnelview` both compiled and appear in the route manifest. `phase-1-3-teams-architecture` branch deleted both locally and on origin after the merge — retired per the new one-branch-per-feature rule (see decisions-log).
- **Live-tested and validated, 2026-07-09.** Confirmed against 78 real candidate screenings: 78 screened → 66 passed threshold (85%) → 24 reached out → 6 TA → 3 L1 → 3 L2 → 1 In-Person → 0 Offer, plus 10 archived. Numbers confirmed accurate, visualization confirmed clean and readable.
- **Excel export added, 2026-07-09.** "Export Excel Report" button, admin-gated (same as the page). Two-sheet workbook: Funnel Summary (stage/count/% of previous) and All Candidates (name, source, score, current/previous stage, recruiter, screened date, fraud flags, archived). Built client-side from the already-fetched `FunnelData` state via the existing `xlsx` dependency (same library/pattern as the Tracker export) — not cached, reflects whatever's on screen at download time. Required extending `FunnelCandidate` (`lib/funnelview/types.ts`) and `getFunnelData()` (`lib/funnelview/data.ts`) with `score`/`hasFraudFlag`, sourced from the `screenings` table's `score`/`duplicate_flag`/`history_alert_type` columns.
- **Per-project breakdown added, 2026-07-09.** Blended org-wide funnel can hide one role converting badly, so `getFunnelData()` now also returns `byProject: FunnelProjectBreakdown[]` — same funnel shape (Total Screened → ... → Offer + archived/rejected), scoped per project. Implemented by extracting a shared `computeStages()` helper in `lib/funnelview/data.ts` so the blended and per-project numbers are computed by the exact same logic and can't drift apart. Projects are the union of `screening_batches.project_id` and candidates' `projectId` (a project can have only below-threshold batches with zero persisted `screenings` rows). Candidates/batches with no assigned project stay out of `byProject` but remain in the blended totals.
- **UI (revised same session, per Vlad's feedback):** first pass added a separate collapsible "By role" table; Vlad asked for a single dropdown instead. Now `/funnelview` has one role selector (next to "Export Excel Report") that drives everything at once — Funnel, Source split, and the Candidates table all switch to the selected role's numbers (blended when "All roles"), instead of a second table alongside the existing view. `activeProject`/`activeStages`/`activeCandidates`/etc. derived once from `selectedProjectId` and reused by every section, so there's one source of truth. Excel export scopes the same way: "All roles" still produces the 3-sheet workbook (Funnel Summary, By Project, All Candidates); picking a specific role produces a 2-sheet workbook (Funnel Summary, Candidates) scoped to just that role, filename includes the role name. Verified by direct code/type review, not `npx tsc`/`npm run build` — this sandbox's mount of the repo has been unreliable this session (bash sees a stale/truncated copy of files the Read tool shows as current and complete, e.g. `app/api/analyze-jd/route.ts` read as 41 lines via bash vs 45 via Read tool) — same class of flakiness noted in the Phase 2.1 entry above, not a new issue. Needs a real live-test pass on Vlad's machine, same as 2.1.

## What's built, stashed pending its own branch (2026-07-10, admin team deletion)

- **Delete team, added to the existing Teams UI on `/admin/users`.** New `deleteTeam()` in `lib/teams.ts`, new `DELETE /api/admin/teams/[id]` route (admin-gated, mirrors the `[id]/members` route's pattern). "Delete team" button next to each team name; `confirm()` dialog (matches this page's existing pattern for removing a user) warns about member count and that any projects/candidates on that team become unassigned rather than deleted.
- **No pre-check/blocking** — same "just delete, let FK constraints handle cleanup" pattern as `deleteProject`. `team_members` cascade-deletes (`ON DELETE CASCADE`); `projects.team_id`, `screenings.team_id`, `resume_fingerprints.team_id` are all `ON DELETE SET NULL` (set in the original Phase 1.3 migration) — nothing is destroyed, but an unassigned project/candidate drops out of every recruiter's team-scoped view (`teamIdsFilter` can never match `NULL`) until reassigned; only admins (who bypass team scoping) would still see it. This consequence is stated directly in the confirm dialog rather than silently happening.
- Verified by direct code/type review (same sandbox mount-flakiness caveat noted throughout this session).
- **Not yet live-tested or committed.** As of 2026-07-10, this is one of 6 files sitting in a `git stash` on top of `feat/cross-project-fit-suggestion` in Claude Code's environment (bundled with the FunnelView dropdown work below) — Claude Code separated it out rather than commit it alongside Phase 2.1. See open-questions.md for the exact recovery steps.

## What's built, stashed pending its own branch (2026-07-09/10, FunnelView per-project dropdown + Sourced/Applied rename)

- See the FunnelView sections above for the full feature detail (per-project breakdown → single dropdown selector, Sourced/Applied terminology, Excel export scoping).
- **Not yet live-tested or committed.** Same stash as admin team deletion above — 6 files total (`app/funnelview/page.tsx`, `lib/funnelview/data.ts`, `lib/funnelview/types.ts`, `lib/teams.ts`, `app/api/admin/teams/[id]/route.ts`, `app/admin/users/page.tsx`), stash message: "FunnelView per-project dropdown + admin team deletion, unrelated to Phase 2.1."

## Shipped, merged to main (2026-07-09/10, Phase 2.1 — Cross-Project Fit Suggestion) — final architecture, built by Claude Code this session on `feat/cross-project-fit-suggestion`

- **Cross-Project Fit Suggestion.** Per `HireView_Enterprise_Plan.md`'s Phase 2 — Intelligence Layer. On a below-threshold screening result (not yet saved to the pipeline), a "Check other active roles" link re-scores that resume against every other *active* project in the recruiter's own team, and surfaces the single best match if it clears that other project's own score threshold — e.g. FDE score 35 → suggests AI Developer at 73.
- **`app/api/cross-project-fit/route.ts`** — auth-gated, team-scoped via `getUserTeamIds()` (not `teamIdsFilter`, since this must always be team-bounded, even for admin — "same team" is the point of the feature). Calls the existing `scoreCandidate()` (imported, not edited) against each other active project's stored JD, 3 at a time (same concurrency cap as the primary screening route). `POST` response now carries the full scored `CandidateResult` and the target project's `jobDescription` alongside `{projectId, projectName, score}`, so a follow-up "Transfer" action can save directly without re-scoring.
- **Auto-fire rebuilt a fourth time, this time on a cheap Claude classification call — no local heuristic left standing.** Three consecutive local/free gates (must-have score, overall score, keyword overlap against other projects' must-haves) were each checked against a real failing test case (Alex Kim / AI Developer, mustHaveScore 32 → 82 elsewhere) and all three missed it. Root cause traced precisely for the keyword-overlap version: it matched against `result.careerTrajectory`, and `scoreCandidate`'s tool schema (`lib/scoreCandidate.ts`) explicitly generates that field scoped to "the role being hired for" — the CURRENT project. So the text being keyword-matched was never neutral; it's already filtered through the current role's lens and can omit a different role's relevant vocabulary regardless of résumé/JD length. Confirmed: only ~1/5 of the AI Developer JD's must-have vocabulary appeared anywhere in Alex Kim's current-role-scoped summary. This isn't a threshold problem — it's evidence that no cheap local heuristic reliably substitutes for actual semantic judgment.
- **New `POST /api/cross-project-fit/gate`** — re-extracts the real resume text (same `extractResumeText` the full check already uses, not the role-scoped summary) and makes one cheap Claude call with a minimal output schema (`{promising: boolean}`) against short role-name + must-have-list summaries (not full JDs) for every other active project. Real judgment, not free, but far cheaper than the full check's N complete scoring passes. Fails closed (`promising: false`) on any error.
- **`ResultCard.tsx`** — reverted to a simple `otherActiveCount: number` prop (the client-side skill data from the keyword-overlap attempt is no longer needed, now that the gate decision happens server-side); new `onCheckCrossProjectPromise?: () => Promise<boolean>` prop, called once via a guarded `useEffect` (states: `checkingGate`, `gateChecked`), chains straight into the existing `handleFindBetterFit()` when the gate says yes. UI shows the same "Checking other active roles…" text through both the gate phase and the real-check phase — no visible seam between them.
- **`app/projects/[id]/page.tsx`** — `onCheckCrossProjectPromise` routed through the same `fitQueueRef` single-flight queue as the real check, so gate calls and real checks never run concurrently either.
- **Explicitly not guaranteed** — Vlad asked directly; told him no classifier is 100%, but this doesn't share the specific structural blind spot (role-scoped summary matching) that killed the prior three attempts.
- **A real bug was found and fixed during this build:** `ResultCard.tsx`'s auto-fire `useEffect` listed `checkingGate` (state it sets inside itself) in its own dependency array, causing a self-cancelling loop that left "Checking other active roles…" stuck forever. Fixed with a `useRef`-based re-entry guard instead. See [[feedback_useeffect_self_cancel]] in the global memory vault — saved as a durable cross-session lesson.
- **Live-tested by Vlad after the fix — confirmed working.** The gate resolved correctly (not stuck) and correctly identified a real cross-project fit that all three prior gate mechanisms had missed (the Alex Kim / AI Developer case).
- **Committed and pushed, 2026-07-10.** Commit `66bf129` "feat: Cross-Project Fit Suggestion (Phase 2.1) — Claude-call auto-fire gate + Transfer action" on `feat/cross-project-fit-suggestion`, pushed to origin. File scope confirmed exact (the 4 code files above, plus the memory vault — bundled in per the same "interleaved prose" call made on earlier commits). Do-not-touch files (`screen-resumes/route.ts`, `scoreCandidate.ts`, `screenings.ts`, `analyzeJD.ts`, `parseResume.ts`, `calibrationExamples.ts`) confirmed zero diff. `npx tsc --noEmit` and `npm run build` both clean, route manifest confirms both `/api/cross-project-fit` and `/api/cross-project-fit/gate` compiled. **PR #10 merged to main 2026-07-10** (merge commit `a7f8a26`, on top of PR #9's `bf76126`) — confirmed via `git log origin/main` and `git merge-base --is-ancestor 66bf129 origin/main` after `git fetch`.
- **Not yet separately confirmed:** the manual "Check other active roles" link for a non-promising candidate, and the "Transfer to [Project]" button actually landing a candidate in the other project's pipeline — worth a pass before/alongside PR review, not blocking.
- **New: one-click "Transfer to [Project]" action**, not just a read-only suggestion. `FitSuggestion` (now exported from `components/ResultCard.tsx`, was a local-only interface) carries `{projectId, projectName, score, result, jobDescription}`. Clicking Transfer posts to the existing `/api/screenings/save-one` route (same one "Save anyway" already uses) with the suggestion's `result`/`jobDescription`/`projectId` — saves the candidate directly into the *other* project's pipeline, no re-scoring. On success the whole below-threshold banner is replaced by a "Transferred to [Project] ✓" confirmation linking into that project's pipeline (`/projects/[id]?tab=pipeline`). This is a real behavior change from the original "deliberately stateless, nothing persisted" framing — see decisions-log, the feature evolved from suggestion-only to suggestion-plus-one-click-save, though it still adds no new schema (reuses the existing `screenings` save path).
- **Do-not-touch boundary respected, reconfirmed at every step:** `app/api/screen-resumes/route.ts`, `lib/scoreCandidate.ts`, `lib/screenings.ts` all clean throughout. The original file is re-sent from the browser (still in memory from the initial upload, matched by filename — same pattern "Save anyway" already used) to the new route, which independently re-extracts resume text via `extractResumeText()`.
- **`npx tsc --noEmit` and `npm run build` both clean** — Claude Code verified this locally (not this sandbox), at each step of the change, not just at the end.
- Branch `feat/cross-project-fit-suggestion` created fresh off `main` (which already included FunnelView export, Outreach-drafting descope, and the shared-recruiter-lookup refactor). Now merged — see above. **Still open:** re-test the manual-only flow (link appears, click, suggestion or "no stronger fit" renders) and the Transfer button specifically (candidate actually lands in the other project's pipeline, confirmation banner renders, link navigates correctly) — not yet separately confirmed, lower priority than the auto-fire gate which is live-tested.
- Not added to the Prior Art document — reuses the existing scoring engine across multiple job descriptions rather than introducing a new technique, same assessment standard applied to 1.3/1.4/1.5.
- Vlad and Teti currently share the single default "General" team (per Phase 1.3 decisions-log), so in practice this feature suggests/transfers across both of their active projects, not just one recruiter's own roles — matches the Enterprise Plan's "same team" spec exactly, just worth being aware it's not scoped any tighter than that today.

**Note on concurrent uncommitted changes in the same working tree, for whichever session picks this up next:** while Claude Code was doing the above, this Cowork session was independently working in the same repo on unrelated features — the per-project FunnelView dropdown/Sourced-Applied rename (`app/funnelview/page.tsx`, `lib/funnelview/data.ts`, `lib/funnelview/types.ts`, see the FunnelView section above) and admin team deletion (`lib/teams.ts`, `app/api/admin/teams/[id]/route.ts`, `app/admin/users/page.tsx`, see the section above that). Both are legitimate, Vlad-directed, already documented in their own state.md sections and session-log entries — not something Claude Code needs to investigate, revert, or ask about. They're unrelated to Phase 2.1 and untouched by it.

## Shipped, merged to main (2026-07-10, Pre-Screen Duplicate Check)

- **Skips scoring entirely for resumes that already exist in the current project — a Claude-cost optimization, not just a UI dedupe.** Two free (zero-Claude-call) signals computed before any file reaches the scoring route: exact match on `resume_content_hash` (SHA-256 of extracted, normalized resume text — catches literal re-uploads/re-exports of the same content) and, as a fallback, a normalized-filename match (catches "might be an edited version of a resume already screened here"). Distinct from Phase 1.1's fingerprint matching, which is fuzzy/semantic and identity-scrubbed for fraud detection, always costs a Claude call, and exists for a different purpose entirely.
- **New `app/api/screen-resumes/check-existing/route.ts`** — the frontend calls this first with the full upload batch; it classifies each file (`new` / `duplicate` / `possible_update`) against the project's existing screenings. `app/api/screen-resumes/route.ts` and `scoreCandidate.ts` (both do-not-touch) are completely unmodified — the calling page (`app/projects/[id]/page.tsx`) removes anything flagged before the existing scoring route ever sees it, "build around, never through."
- **`lib/resumeContentHash.ts`** — new shared module, `hashResumeText()` and `normalizeFileName()`, used by both the check-existing route and `lib/screenings.ts` (which now stores the hash on every save, in the same best-effort block that already runs fingerprinting — one extra small `UPDATE`, no new Claude call).
- **UI: `AlreadyScreenedCard`** renders alongside normal `ResultCard`s in the results view for anything set aside. `duplicate` shows the existing saved data with a "Re-screen anyway" override (in case the recruiter has a real reason to force a fresh score on identical content). `possible_update` additionally embeds the existing Credibility Checker (`CrossReferenceChecker`, given a new optional `initialFile` prop so it's pre-filled with the new upload instead of requiring a second manual pick) — reuses the already-shipped `resumeDelta` field, built specifically for "second version of the resume, what changed," instead of inventing new comparison logic.
- **Migration:** `supabase-migration-resume-content-hash.sql` — additive nullable `resume_content_hash` on `screenings`, plus a `(project_id, resume_content_hash)` partial index for lookup performance at scale. No backfill — matches only start working going forward, same pattern as `team_id`/`history_alert_type` before it.
- **Migration confirmed run by Vlad, 2026-07-10** — `resume_content_hash` column + partial index now live in Supabase. **`npm run build` confirmed clean on Vlad's own machine, 2026-07-10 — no errors.** (This session's own sandbox `npx tsc --noEmit` returned the same mount-flakiness false errors documented throughout this session — every file, including untouched ones like `app/layout.tsx`, shows fake truncation errors — so it was never usable for real verification here; Vlad's machine is the real confirmation.)
- **Live-tested by Vlad, first pass, 2026-07-10.** Exact re-upload correctly flagged as `duplicate` and skipped scoring, existing card shown with "Re-screen anyway." Found a real gap: two genuinely *different* resume files for the same person (different filenames, different content) got no warning at all — neither hash nor filename matched, both scored independently with no link between them.
- **Extended same session — post-score candidate-name cross-check added.** `check-existing` now also returns `existingCandidates` (id + name of everyone already saved in the project); after scoring, the frontend compares each freshly-scored `candidateName` (normalized) against that list *and* against siblings in the same batch, and shows a lightweight amber notice on the `ResultCard` if matched — "a different resume file for a candidate named X already exists in this project." Purely informational (can't avoid the Claude cost — the name only exists after scoring), new `ExistingCandidateRef` type in `lib/types.ts`, `normalizeCandidateName()` added to `lib/resumeContentHash.ts`.
- **Investigated why Phase 1.1's fingerprint matching didn't also catch this pair — not a bug.** Read both actual PDFs (Vlad's own test files): one is an academic/technical resume (ML research, coursework, Python/C++/scikit-learn), the other a consulting-branded resume built around HireView/FlowOps (Operations Management, Business Analysis). Near-zero overlap in actual skills/responsibility content — two genuinely different resume framings for the same person, not "same resume, identity swapped." Phase 1.1 is built specifically for the latter (fraud pattern) and correctly declined to flag the former. No fix needed there.
- **Second live-test round, same session — name check confirmed working, found the signal wasn't persistent.** Vlad's screenshot showed the amber `ResultCard` notice firing correctly for the two-different-resumes case. But it's purely client-side/session-local (never written to the DB) — once saved, the candidate showed up on the Pipeline tab with no badge at all, which breaks the project's own standing rule (00-index.md: fraud/duplicate signals must appear as colored badges on the collapsed pipeline card, matching Phase 1.1's `duplicateFlag`/Phase 1.4's `historyAlertType` pattern).
- **Fixed same session — name match is now persisted.** New nullable `name_match_id` on `screenings` (migration `supabase-migration-name-match.sql`), computed for free in `saveScreening`'s existing best-effort block (`findNameMatchInProject`/`markNameMatchPair` in `lib/screenings.ts`) — pure `candidate_name` comparison, no Claude call, mutual link like `markDuplicatePair`. Skipped when this save already became a content duplicate (`becameDuplicate`), since that pairing already implies a name match — avoids two badges pointing at the same target. New "Name match" badge (zinc/neutral, distinct from the rose "fraud" and amber "previously seen" tiers) added to both the Pipeline tab card (click-to-jump, same pattern as `duplicateFlag`) and the All Candidates card (static — this page spans all projects and doesn't reliably have the matched sibling loaded, so no click-to-jump there, matching how `duplicateFlag` is already treated non-interactively on that specific page).
- **Also fixed same session — Pipeline tab didn't refresh its tab-count/header-count after a delete.** Root cause: `PipelineTab` forks `screenings` into its own local `useState` on mount; `handleDelete` correctly updated that local copy (the list itself updated immediately) but never told the parent, whose *separate* `screenings` state drives both the "Pipeline (N)" tab label and the "N candidates screened" header — both stayed stale until a full reload. Added an `onDeleted` callback prop, wired the same way `onStatusChange`/`onStageChange` already are. Checked the All Candidates page for the same bug pattern — it doesn't have one, its delete already updates the single top-level state directly.
- **Migration outage, same session — `name_match_id` added to `SCREENING_COLUMNS` (shared select used by `listScreenings`/`getScreeningsByIds`) before the migration was confirmed run. Candidates disappeared entirely (Pipeline + All Candidates both empty) — a repeat of the 2026-07-09 incident, same root cause, now reinforced in the global memory vault's `feedback_migration_sequencing` entry with a concrete rule to prevent a third occurrence.** Vlad ran `supabase-migration-name-match.sql`, candidates reappeared immediately, no other fix needed.
- **Third live-test round, same session — persisted "Name match" badge confirmed working.** Both `Vlad Vashchuk` cards (18 and 27) now show a persistent "NAME MATCH" badge on the Pipeline tab after a reload, surviving navigation as intended — closes the gap found in round 2.
- **Committed and merged, 2026-07-10.** Claude Code found it was on the wrong branch (`feat/funnelview-dropdown-and-team-delete`, stale from earlier work) — stashed the uncommitted changes, synced `main` (had moved further; the FunnelView-dropdown and team-deletion stash items below were already merged separately), created `feat/pre-screen-duplicate-check` fresh off it, popped the stash back with an exact file match. `npx tsc --noEmit` and `npm run build` both clean; `/api/screen-resumes/check-existing` compiles and appears in the route manifest. Do-not-touch diff confirmed zero changes on all five files. Full diff review confirmed every file matches the description above. Commit `55bfb8e`, 14 files, pushed to `origin/feat/pre-screen-duplicate-check`, merged into main via PR (compare-URL flow, `gh` still not on PATH).
- **Still open, lower priority:** the delete-refresh fix, `possible_update` credibility-comparison path, and genuinely-new-resume path were fixed/built this session but not separately re-confirmed after the final merge — worth a live pass next time the app is used, not blocking.

## Built this session, not yet migrated/tested/committed (2026-07-10, Save-All + Rejection History)

- **Teti's feedback, three parts.** (1) Remove "Save anyway" — every candidate is saved regardless of score now (see Pre-Screen Duplicate Check section above for the last threshold-gated behavior; this replaces it). (2) System-wide rejection history: if a name-matched candidate was rejected anywhere in the system before (any project, any team — deliberately not team-scoped like every other match signal), any recruiter should see it + the reason. (3) Threshold question — kept as a pipeline filter, not a save gate; adaptive/calibration-based auto-tuning discussed and deferred to backlog (too much complexity/drift risk for now).
- **`app/api/screen-resumes/route.ts`** — removed the `score >= scoreThreshold` gate; `saveScreening` now always runs. `scoreThreshold` still read and still drives `saveScreeningBatch`'s `passedCount` stat.
- **`components/ResultCard.tsx`** — removed `onSave`/`handleSave`/save state entirely (nothing to save-anyway). New `belowThreshold` prop replaces the old `savedId === undefined` gating for the cross-project-fit block. That block is now its own section, moved to render immediately after the header/name-match banners and before Career Trajectory (was previously nested inside the now-deleted "unsaved" banner, below Career Trajectory) — Vlad's ask, so a recruiter sees the fit suggestion + Transfer button first.
- **`app/projects/[id]/page.tsx`** — the four `result.id === undefined` gates on `onSave`/`onFindBetterFit`/`onCheckCrossProjectPromise`/`onTransferToProject` replaced with `result.score < project.scoreThreshold` (now that `result.id` is always defined, that old gate would have broken cross-project fit entirely).
- **Cross-project fit already spans every team a recruiter belongs to** — verified, not a new change. `getUserTeamIds()` returns the full array of a user's teams; `/api/cross-project-fit`'s GET and POST both call `listProjects(teamIds)` against that full array, not just one team. So Teti's ask ("check active roles across every team a multi-team user is in") already works as built in Phase 2.1.
- **Rejection reason** — new nullable `reject_reason` column on `tracker` (`supabase-migration-reject-reason.sql`, **not yet run**), mirrors the existing `on_hold`/`on_hold_reason` pattern. Captured via a new rose-tinted textarea in the Tracker drawer, shown only when `stage === "Reject"`. `upsertTrackerEntry` writes it conditionally (same pattern as `onHoldReason` — inert until someone actually saves a reason).
- **Migration confirmed run by Vlad, 2026-07-10.** Follow-up read-side wiring landed same day: `reject_reason` added to `getFullTrackerEntries`' select and row mapping in `lib/screenings.ts`, so a saved rejection reason now survives a full page reload, not just the same browser session. Also added a "Rejection Reason" column to the Tracker's Excel export.
- **System-wide rejection lookup** — new `listRejectionHistory()` in `lib/screenings.ts`, three plain isolated queries (tracker rows where `stage = 'Reject'` → matching `screenings` rows → matching `projects` rows), joined in JS, no team/project filter at all. Fails closed to `[]` on any error (including "column doesn't exist yet" before the migration runs) — safe to call immediately, never throws, never touches `SCREENING_COLUMNS`.
- **Wired into the existing pre-screen check flow** — `check-existing/route.ts` now also returns `rejectionHistory: RejectionHistoryEntry[]` (system-wide, independent of `projectId`) alongside the existing `existingCandidates`. `app/projects/[id]/page.tsx` compares each freshly-scored candidate's normalized name against it post-score (same pattern as the existing name-match check, new `findRejectionMatches` helper) and passes any hit to `ResultCard` as `rejectionHistory`, rendered as a rose banner: "Previously rejected for [Project] — '[reason]'."
- **Not yet tested** — this session's own sandbox `npx tsc --noEmit` returned the same mount-flakiness false errors seen all session (every file, including untouched ones like `app/layout.tsx`, shows fake truncation errors); verified by direct code/type review only, same caveat as every other feature this session.
- **Needs from Vlad:** real `tsc`/`build` on his own machine, then live-test: score a below-threshold candidate and confirm the fit-suggestion banner now appears above Career Trajectory with no "Save anyway" button; confirm a rejected-then-resubmitted candidate (same name, any project/team) shows the rose "Previously rejected" banner; confirm entering a rejection reason in the Tracker drawer saves and survives a page reload (not just the same session, now that the migration's run and the read side is wired); confirm the Excel export's new "Rejection Reason" column. Then commit/push/PR through Claude Code.
- **Same session, small follow-on requests bundled in:**
  - **Tracker drawer's Role field now auto-fills with the project name** when nothing's been saved yet (still freely editable, saves normally on blur) — `TrackerTab`/`DrawerBody` both gained a `projectName` prop threaded from the page's `project.name`. Excel export's Role column falls back to `projectName` the same way.
  - **Screen tab's LinkedIn toggle label renamed** from "LinkedIn profiles" to "Sourced (LinkedIn)" for terminology consistency with FunnelView's existing Sourced/Applied split.
  - **Two real bugs found and fixed in the LinkedIn document viewer** (Vlad reported: resume always previewed fine, LinkedIn always showed a black screen). First fix (content-type): `/api/history/[id]/linkedin`'s GET handler had `Content-Type` hardcoded to `application/pdf` regardless of what was actually stored — the cross-reference upload accepts PDF or DOCX, so a stored DOCX would render broken; now reads the real type off the downloaded Blob (same idea as the resume route's dedicated `resume_mime_type` column, just sourced from Supabase Storage's preserved content-type instead of a new DB column). Vlad confirmed this fix was correct in isolation but the black screen persisted — root cause was actually the `Content-Disposition` header baking in a literal em-dash plus the raw candidate name (both invalid in a raw ASCII HTTP header value), which made the whole response fail to parse in the browser — exactly a black iframe over the page's dark background. Fixed with an ASCII-sanitized `filename=` fallback plus a proper RFC 5987 `filename*=UTF-8''...` for the real Unicode name. Vlad confirmed this fixed it. Also hardened `assess-credibility/route.ts`'s cross-reference-doc storage upload to log (not silently swallow) failures.
- **Do-not-touch exception, called out explicitly:** `app/api/screen-resumes/route.ts` WAS modified this session (the score-gate removal for save-all-candidates) — unlike every prior handoff, which confirmed this file untouched. This was Vlad's own explicit request, not scope creep; flag it clearly in the next commit/PR description so it doesn't read as an accidental do-not-touch violation. `scoreCandidate.ts`, `analyzeJD.ts`, `parseResume.ts`, `calibrationExamples.ts` remain untouched as before.
- **Committed, 2026-07-10.** Same wrong-branch recovery pattern as the last two handoffs (was sitting on the now-merged `feat/pre-screen-duplicate-check` — stashed, resynced `main`, created `feat/save-all-and-rejection-history` fresh, popped clean). `npx tsc --noEmit` and `npm run build` both clean. Do-not-touch diff confirmed zero on `scoreCandidate.ts`/`analyzeJD.ts`/`parseResume.ts`/`calibrationExamples.ts`; `screen-resumes/route.ts` diffed and eyeballed in full — confirmed the only change is the score-gate removal, nothing else crept in. Commit `967d159` (12 files), pushed to `origin/feat/save-all-and-rejection-history`. **Merged to main by Vlad, 2026-07-10, and live-test checklist fully confirmed** (fit-suggestion placement, rose rejection-history banner, reason persistence, Excel column all working). Nothing outstanding from this batch.

## Fixed, 2026-07-10 (JD upload bug + legacy .doc support)

- **Root cause of "Word documents don't upload" found and fixed.** `app/projects/page.tsx`'s `handleFileUpload` had a mangled fetch URL — `fetch("\api\extract-jd-text", ...)` (backslashes, not `/`). JS silently drops unrecognized `\x` escapes, so the literal runtime string was `apiextract-jd-text` with no slashes at all, which resolved to a nonexistent relative path and 404'd for every file type, not just Word. A neighboring Tailwind class (`shadow-violet-500\25`) had the same backslash-for-slash corruption, both likely from the same bad find/replace. Both already fixed in this session (fetch URL restored to `/api/extract-jd-text`, class restored to `shadow-violet-500/25`) before this note was written — see this session's earlier turns.
- **Separately, added real legacy `.doc` support** (previously only `.pdf`/`.docx`/`.txt` were accepted anywhere in the app — an old `.doc` resume or JD was invisible in every file picker, a real gap independent of the bug above). New dependency `word-extractor` (pure JS, OLE-format parser, no native binaries — verified safe for Vercel serverless). `lib/parseResume.ts`'s `extractResumeText` gained a `.doc` branch; new ambient `lib/word-extractor.d.ts` (package ships no types). Widened `accept` on all five file inputs that take resumes/JDs (`app/projects/page.tsx`, `app/projects/[id]/page.tsx`, `components/ResumeUploader.tsx`, `components/CredibilityChecker.tsx`, `components/CalibrationPanel.tsx`) to include `.doc`. `ResumeUploader`'s drag-and-drop path previously silently dropped unsupported files with zero feedback — added a rose "Skipped — only PDF and Word (.doc/.docx) are supported: [filenames]" notice.
- **Verified in an isolated scratch environment, not this session's project mount** — the mount of `C:\Portfolio\HireView` inside this sandbox's `bash` tool is a stale read-only snapshot (confirmed via `stat`: shows June 30/July 2 mtimes on files edited today, doesn't reflect this session's writes at all — worse than the "truncated file" flakiness noted in every earlier entry this session, this one just doesn't update). Real `npm run build`/`npx tsc --noEmit` against the actual project **could not be run this session** for that reason. Instead: installed `word-extractor` in an isolated scratch npm project, generated a real `.doc` file via `soffice --convert-to doc`, copied the actual (post-edit) `lib/parseResume.ts` + the new `.d.ts` there, ran `tsc --noEmit` (clean) and then the compiled JS's `extractResumeText("sample.doc", ...)` for real — correctly returned the sample text. This confirms the logic is sound; it is not a substitute for a real build in the actual project tree.
- **Needs from Vlad:** run `npm install` (picks up `word-extractor`) and a real `npm run build`/`npx tsc --noEmit` on his own machine before commit/push/PR — this session could not do that verification itself, unlike every prior feature this session where the sandbox's `tsc` at least ran (with false-positive errors) against the real tree. Also worth a live test: upload an actual legacy `.doc` resume and JD through both the New Role modal and the Screen tab.
- **Follow-up, same session, Vlad's direct request:** the New Role modal's JD upload was click-only (button → hidden file input), unlike `ResumeUploader` which already supported drag-and-drop. `app/projects/page.tsx`'s `NewRoleModal` now supports both — `handleFileUpload` split into a reusable `extractFile(file)` plus a thin `onChange` wrapper, new `handleDrop` calls the same function. Drag state (`isDragging`) drives a violet dashed-border overlay on the textarea itself while dragging, matching `ResumeUploader`'s visual language.
- **Real production bug surfaced once Vlad ran `npm install` and tested live: `POST /api/screenings/save-one` 500'd with `StorageApiError: Invalid key` on a candidate resume named `JORDAN~1.DOC`.** Root cause unrelated to the `.doc`-parsing work above — `lib/screenings.ts`'s `saveScreening` built the Supabase Storage upload path directly from the raw candidate filename (`resumePath = \`${randomUUID()}/${result.fileName}\``), and Supabase Storage's key validator only allows `\w / ! - . * ' ( ) space & $ @ = ; : + , ?` — `~` (and accents, `%`, etc.) fails outright. This is a pre-existing latent bug (not introduced by the `.doc` work), just surfaced by a real filename containing a tilde (looks like a Windows 8.3 short-name artifact). Fixed with a new `sanitizeStorageFileName()` helper in `lib/screenings.ts` that replaces any character outside that allowed set with `_` before building the path — the human-readable name is unaffected since it's stored separately in the `file_name` column and was never derived from the storage path. `lib/screenings.ts` is not on the do-not-touch list, so this was a direct fix, not an exception.
- **Same exact bug pattern exists in `lib/calibrationExamples.ts` (`calibration/${randomUUID()}/${fileName}`) — left unfixed.** That file IS on the do-not-touch list; flagged in open-questions.md for Vlad/Claude Code rather than silently edited.
- **Do-not-touch note, caught late:** `lib/parseResume.ts` (the `.doc` branch added earlier this session) is also on the do-not-touch list. This was Vlad's own explicit request, same category of deliberate exception as the `screen-resumes/route.ts` score-gate removal earlier this project — just wasn't flagged as an exception in the moment. Flagged now in open-questions.md.
- **Second real regression found live: "Checking other active roles…" was getting stuck forever again — same symptom as the Phase 2.1 `useEffect` self-cancel bug, different trigger.** `components/ResultCard.tsx`'s auto-fire gate effect had already been fixed once (swapped a `checkingGate`-in-deps loop for a `gateStartedRef` guard, see decisions-log 2026-07-10 / [[feedback_useeffect_self_cancel]]) — but `onCheckCrossProjectPromise` (an inline arrow function `app/projects/[id]/page.tsx` recreates every render) was still in that effect's dependency array. Any parent re-render while the gate call was in flight reran the effect; the ref correctly blocked a second API call, but the ORIGINAL run's cleanup still fired, flipping its `cancelled` flag, so the original call silently discarded its result on resolve — `setCheckingGate(false)` never ran. Fixed by replacing the per-run `cancelled` closure with a `mountedRef` set once via its own `[]`-deps effect (true unmount only); a same-instance rerun from prop-identity churn no longer discards the in-flight call's result. Full reasoning + a general "don't do this" writeup added to the global memory vault's `feedback_useeffect_self_cancel` entry.
- **Third bug from the same "widen .doc support" root cause: the Credibility Checker's drag-and-drop silently dropped `.doc` files.** Vlad asked to confirm drag-and-drop works on the cross-reference doc slot — it already did (label-level `onDragOver`/`onDragLeave`/`onDrop` in `components/CredibilityChecker.tsx`'s `FileSlot`), but the file it drops into is filtered by a hardcoded regex, `/\.(pdf|docx)$/i`, which never got updated when the `accept` attribute was widened earlier this session. Drag-and-drop bypasses `accept` entirely (that's an input-element-only affordance), so the regex was the *only* real gate for a dropped file — a dropped `.doc` silently vanished with zero feedback, exactly the class of bug already fixed in `ResumeUploader`. Fixed the regex to `/\.(pdf|docx?)$/i` and added a visible error ("Only PDF and Word (.doc/.docx) files are supported.") for a rejected drop, reusing the existing error paragraph already rendered under `FileSlot` in both of `CrossReferenceChecker`'s call sites (the standalone Credibility Checker and `AlreadyScreenedCard`'s embedded compare).
- **Fourth bug found via targeted verification, this one real and long-standing: rejection reason was never actually persisting, despite state.md previously claiming it survived a reload.** Traced the full chain end to end: `DrawerBody`'s textarea → `saveTrackerField` → `onTrackerDataChange` → `PATCH /api/tracker/[screeningId]` → `upsertTrackerEntry` → `reject_reason` column, plus the read side (`getFullTrackerEntries`). Every link was correctly wired *except* the PATCH route handler itself — it explicitly whitelists which body fields get forwarded into `upsertTrackerEntry` (`stage`, `company`, `role`, `onHoldReason`, etc.), and `rejectReason` was never added to that list. The field was silently dropped server-side on every save; the UI still showed "Saved" because that's driven by optimistic local state (`setTrackerData`) in `handleTrackerDataChange`, not by anything server-confirmed — so a same-session test would look correct while a real reload would always lose it. This explains why the earlier "confirmed reload-persistent" note was wrong: whatever test happened at the time likely didn't actually reload, or reloaded before the PATCH's silent failure was checked closely. Fixed by adding `...(body.rejectReason !== undefined && { rejectReason: body.rejectReason })` to `app/api/tracker/[screeningId]/route.ts`'s PATCH handler, alongside the other whitelisted fields. Confirmed only one call site for `upsertTrackerEntry` exists, so no other route needed the same fix.
- **Committed and pushed, 2026-07-15.** Same wrong-branch recovery pattern as prior handoffs (was sitting on the now-merged `feat/save-all-and-rejection-history` — stashed with `-u`, resynced `main` — now at PR #14's merge `69daefc` — created `feat/legacy-doc-support-and-bugfixes` fresh, popped clean, exact same 15 files + `lib/word-extractor.d.ts`). Do-not-touch check: `scoreCandidate.ts`/`analyzeJD.ts`/`screen-resumes/route.ts` confirmed zero diff; `calibrationExamples.ts` confirmed zero diff (the twin storage-key bug is still deliberately unfixed there, per open-questions.md); `parseResume.ts`'s diff reviewed in full and confirmed scoped to exactly the `WordExtractor` import + the new `.doc` branch, nothing else touched. This time `npx tsc --noEmit` and `npm run build` both ran for real against the actual project tree (no mount flakiness this session) — both clean, all 42 routes generated. Commit `e7a162a` (16 files), pushed to `origin/feat/legacy-doc-support-and-bugfixes`. **Merged to main same day** — PR #15, merge commit `56bf32c`, confirmed via `git log origin/main`.

## Fixed, 2026-07-15 (Teti feedback batch — filename collision, missing candidate name, company-alias false positives)

- **Context: Teti (colleague/recruiter) sent a feedback batch** — a screenshot of a pipeline card showing "Unknown (resume name not provided)," a diagnostic sample resume (`Priyanka_Ponnam_DE_2 (1).pdf`), and three numbered issues, plus a separate pasted meeting-summary describing a larger pipeline restructuring (see the Design section below — not built this session). Vlad approved a plan first (per the Blueprint methodology), then approved building the three confirmed bugs.
- **Bug 1 — filename-collision data loss, fixed.** `app/projects/[id]/page.tsx`'s upload-batch filter previously routed any `check-existing` match (`duplicate` OR `possible_update`) into the "matched, skip scoring" bucket. `possible_update` is a weak filename-only signal (two genuinely different people can both upload a file literally named `Resume 16.pdf`) — routing it the same as `duplicate` (a strong content-hash match) meant a second real candidate with a generic filename silently never got scored or saved at all, exactly Teti's report. Fixed: only `duplicate` skips scoring now; `possible_update` still scores normally and separately surfaces a `possibleUpdateMatches` amber banner on the `ResultCard` (`filenameMatch`/`filenameMatchFile` props) with an inline "Compare" action embedding the existing `CrossReferenceChecker`, informational only, never blocks the save.
- **Bug 2 — missing candidate name ("Unknown (resume name not provided)"), fixed.** Root cause confirmed directly against Teti's actual sample PDF: `pdf-parse` extracted 18,387 characters of real body text but zero occurrences of the candidate's name anywhere in it — the file's `Producer` metadata is `"Skia/PDF m148 Google Docs Renderer"`, and Google Docs' PDF export can place the header/title area (name, contact info) outside the extractable text layer even though it's fully visible in any normal viewer. Not a parsing bug in `lib/parseResume.ts` (do-not-touch, untouched) — the text genuinely isn't there to extract. Tried rendering the page to an image first (`pdfjs-dist` + `@napi-rs/canvas`, to avoid the classic `canvas` package's native-binary problems on Vercel) — hit a real incompatibility (`Error: Value is none of these types \`String\`, \`Path\`` in `CanvasGraphics.fill`) and abandoned it as not shippable. Fixed instead with the Anthropic SDK's native PDF support (already in use elsewhere in this project, zero new dependency): new `lib/extractCandidateNameFallback.ts` (`extractCandidateNameFromPdf`, `looksLikeMissingName`) sends the raw PDF as a `document` content block and lets Claude read the page visually, no rendering needed on our end. Wired into `lib/screenings.ts`'s `saveScreening` (not do-not-touch): when `scoreCandidate`'s normal name output looks missing/placeholder AND the file is a PDF, this fallback fires once and mutates `result.candidateName` in place before the DB insert — deliberately mutating the existing object (not reassigning a local copy) so both do-not-touch callers (`screen-resumes/route.ts`, `screenings/save-one/route.ts`), which already push this same `result` into their own response and rely on exactly this mutate-by-reference pattern for `result.id`, pick up the fix immediately without either route being touched. Verified via an isolated `tsc --noEmit` compile check (clean) and a direct re-read of the integrated `saveScreening` — not yet live-tested against a real upload of the sample PDF.
- **Bug 3 — company-name aliases flagged as false-positive discrepancies, fixed.** Teti's example: "Hypermedia Systems = HMS, so it shouldn't be flagged." Root cause: `lib/assessCredibility.ts`'s `CREDIBILITY_TOOL` schema and prompt gave Claude no guidance that company names can legitimately vary (abbreviations/acronyms, legal-entity suffixes, punctuation, known rebrands/acquisitions) — it was applying the same strict match/discrepancy logic to company names as to dates or titles, correctly by the letter of the original instructions but not usefully. Fixed by extending both the `status` field's tool-schema description and the prompt's instruction #1 with explicit guidance and the exact Hypermedia Systems/HMS example: same-underlying-company variations are `match`, not `discrepancy`; only flag `discrepancy` when the names plausibly refer to genuinely different companies. Not yet live-tested against a real cross-reference pair.
- **Not yet live-tested; needs from Vlad:** re-upload the sample PDF (or a similarly Google-Docs-exported one) and confirm the candidate name now populates instead of showing "Unknown"; upload two different resumes sharing a generic filename and confirm both get scored with the amber "filename matches" banner on the second one; run a credibility cross-reference where the two documents use an abbreviation/rebrand for the same company and confirm it's no longer flagged as a discrepancy. Then commit/push/PR through Claude Code (do-not-touch check: `scoreCandidate.ts`, `analyzeJD.ts`, `screen-resumes/route.ts` should be zero diff; `parseResume.ts` zero diff this time — Bug 2's fix lives entirely in `screenings.ts` + the new fallback file, not in `parseResume.ts`).
- **Committed and pushed, 2026-07-15 (same day, separate session).** Claude Code ran the handoff. Found itself on `feat/legacy-doc-support-and-bugfixes` (already merged as PR #15) — stashed with `-u`, resynced `main` (now at `56bf32c`), created `fix/teti-feedback-batch` fresh, popped clean, exact same 8 modified files + `lib/extractCandidateNameFallback.ts`. Do-not-touch check: `scoreCandidate.ts`/`analyzeJD.ts`/`parseResume.ts`/`calibrationExamples.ts`/`screen-resumes/route.ts` all confirmed zero diff. Full diff review of all four changed files (`app/projects/[id]/page.tsx`, `components/ResultCard.tsx`, `lib/assessCredibility.ts`, `lib/screenings.ts`) plus the new fallback file matched the description above exactly — including confirming `roleContext` was a pre-existing `ResultCard` prop (not newly added) and `CheckExistingResult` already existed in `lib/types.ts`. `npx tsc --noEmit` and `npm run build` both ran for real this time (first genuine build check these changes got) — both clean, all 42 routes generated. Commit `eab3250` (9 files), pushed to `origin/fix/teti-feedback-batch`. `gh` still not on PATH — compare URL given to Vlad: `https://github.com/vashVlad/HireView/compare/main...fix/teti-feedback-batch?expand=1`. **Not yet merged** — awaiting Vlad's live-test pass per the checklist above.

## Fixed, 2026-07-15 (status dropdown missing on ResultCard right after screening)

- **Vlad's ask:** the status dropdown should be usable directly on the ResultCard right after screening, not only from the Pipeline tab — saves a trip and makes it harder to lose track of a candidate.
- **Turned out to be already fully wired, just silently gated off.** `components/ResultCard.tsx`'s `StatusSelect` render, `onStatusChange` prop, and `app/projects/[id]/page.tsx`'s `handleStatusChange` (PATCH `/api/history/[id]`) were all already built and passed into the Screen tab's `ResultCard`s. The gate `savedId !== undefined && result.status !== undefined && onStatusChange` was failing on `result.status` specifically: `lib/screenings.ts`'s `saveScreening` never set `status` on the insert (relied on an implicit DB column default) and never mutated it back onto the `CandidateResult` object the API route returns — so the freshly-scored `result.status` was always `undefined` in the immediate response, even though the DB row itself had a real status.
- **Fixed in `lib/screenings.ts`** (not do-not-touch): the insert now explicitly writes `status: "new_applicant"` (previously implicit), and `result.status = "new_applicant"` is mutated onto the same `result` object right after, using the same mutate-by-reference pattern already established for `result.id` (both do-not-touch callers) and Bug 2's `result.candidateName` fix. No changes needed to `ResultCard.tsx` or `app/projects/[id]/page.tsx` — the dropdown wiring there was already correct.
- **Not yet live-tested.** Needs: score a resume, confirm the status dropdown now appears on the card immediately in the Screen tab (not just after navigating to Pipeline), change it there, and confirm the change is reflected on the Pipeline tab after navigating (proves the PATCH actually persisted, not just local state).

## Fixed, 2026-07-15 (Bug 1's "Compare" ran the credibility check backwards + leaked into the wrong candidate's record)

- **Vlad's report, with screenshots:** on Robert Hayes' `ResultCard`, the amber "Filename matches Nicholas Keenan" banner's "Compare" button produced a credibility assessment labeled "Resume: Nicholas Keenan ... / Cross-ref: Robert Hayes ..." — backwards from what the card implies (Robert Hayes is the one being screened right now; Nicholas Keenan is the prior match).
- **Root cause: Bug 1's "Compare" embed (added earlier 2026-07-15, see the Teti feedback batch section above) called `CrossReferenceChecker` with `screeningId={filenameMatch.id}` (the OTHER, previously-saved candidate) and `initialFile={filenameMatchFile}` (the CURRENT candidate's own upload).** `/api/assess-credibility` always treats `screeningId` as "the resume" and the second document as "the cross-reference" — so the roles were inverted relative to the card the recruiter was looking at. The 7 flags shown were still factually real (genuinely different people — different name, city, email, employer, education), just mislabeled about which side was which.
- **Worse than a labeling bug: real data corruption.** `assess-credibility`'s route persists the cross-reference doc's storage path onto `screeningId` via `updateScreening(screeningId, {linkedInPdfPath})`. Because `screeningId` was the EXISTING candidate (Nicholas Keenan), every "Compare" click was writing Robert Hayes' resume file into Nicholas Keenan's own DB record as if it were *his* LinkedIn/cross-reference document — silently corrupting an unrelated, already-saved candidate's data, not just displaying a confusing label.
- **Fixed in three files:**
  - **`app/api/assess-credibility/route.ts`** — added a second mode: accepts `crossRefScreeningId` (another existing screening's ID) as an alternative to the existing `crossRefDoc` (uploaded File). When used, fetches that other screening's stored resume server-side via `getScreeningResume` and extracts its text as `crossRefText` — no upload needed. Deliberately skips the `crossRefPath`/`linkedInPdfPath` persistence step entirely in this mode: there's no new external document, just an internal comparison between two already-stored resumes, so neither screening's record should be touched.
  - **`components/CredibilityChecker.tsx`** — `CrossReferenceChecker` gained `crossRefScreeningId`/`crossRefLabel` props. New `CrossRefTargetChip` shows "Comparing against {name}'s resume" in place of the file-picker when `crossRefScreeningId` is set and no file has been manually chosen. A manually-picked file (e.g. via "re-run with a different document") still takes priority over the fixed target on any re-run.
  - **`components/ResultCard.tsx`** — the filename-match "Compare" block now calls `CrossReferenceChecker` with `screeningId={savedId}` (this candidate's own record, e.g. Robert Hayes) and `crossRefScreeningId={filenameMatch.id}` (the other candidate, e.g. Nicholas Keenan) — matching what the card visually implies, and no longer at risk of writing into the other candidate's record. Compare button gate also now requires `savedId !== undefined` (the current candidate must actually be saved) in addition to the existing `filenameMatchFile` check.
- **Related, NOT fixed at the time: `components/AlreadyScreenedCard.tsx` has the same inverted pattern** (`screeningId={existing.id}`, `initialFile={file}`, in its "possible_update" pre-score comparison — see the Pre-Screen Duplicate Check section, 2026-07-10) — but that flow can't use the same `crossRefScreeningId` fix, because the newly-uploaded file there was set aside *before* scoring and has no `screeningId` of its own yet to be the "resume" side. **Superseded a few hours later the same day — see the "filename-matching retired" section directly below.** Turned out this code path was already unreachable (dead) since Bug 1 shipped, and the whole filename-matching feature it belonged to got removed rather than fixed.
- **Superseded, same day: the `filenameMatch` prop/Compare block on `ResultCard.tsx` described in the bullet above (point 186) no longer exists** — see the "filename-matching signal retired" section below. Kept this section's history intact rather than rewriting it, since the `crossRefScreeningId` mechanism built here (assess-credibility route + CredibilityChecker props) is exactly what the replacement (`nameMatch`'s Compare button) now runs on — the fix wasn't wasted, just re-pointed at a different, more reliable trigger.
- **Live-test status carried forward:** the original Robert Hayes / Nicholas Keenan "Compare" scenario from the screenshots can no longer be reproduced via a filename match (that signal is gone) — it would now need a real *name* match to trigger. Still worth Vlad confirming Nicholas Keenan's `linkedin_pdf_path` wasn't left corrupted from whichever earlier run(s) happened before this was caught (manual DB check, not blocking).

## Retired, 2026-07-15 — filename-matching signal removed; Compare moved to nameMatch

- **Context: Vlad, looking at the same Robert Hayes/Nicholas Keenan screenshots, asked why the filename match fired at all given they're "two completely different resumes."** Traced it precisely: `normalizeFileName()` (`lib/resumeContentHash.ts`) strips extensions and trailing `" (N)"` suffixes so real edited-resume filenames still match (`John_Smith_Resume.pdf` vs `John_Smith_Resume (1).pdf`) — but run on `Resume (16).pdf`, it strips down to the bare word `"resume"`. Every resume literally named `Resume.pdf`, `Resume (1).pdf` ... `Resume (16).pdf` (the default browser/OS auto-rename for saving multiple downloads called "resume" to the same folder — plausibly the single most common resume filename shape in existence) normalizes to that same generic string and matches every other one, regardless of actual candidate identity.
- **First proposal considered and rejected: blocklist generic terms (resume, cv, document, etc.) and suppress the match only for those.** Discussed with Vlad; in the course of that discussion, a stronger option emerged: `nameMatch` (`findNameMatchInProject` in `lib/screenings.ts`, ships automatically post-score for every saved candidate, unconditionally) already answers the exact same underlying question — "is this the same real person as someone else already in this project" — using actual extracted candidate identity instead of an incidental filename string. It runs regardless of whether `possible_update` fires, so it isn't a new signal, just an existing one that was being underused. Key insight: when `filenameMatch` fires WITHOUT `nameMatch` also firing (exactly the Robert Hayes/Nicholas Keenan case), that's close to definitionally "different people, coincidental filename" — a lone filename match, once you have a working name-match check running in parallel, is mostly noise, not signal.
- **Decision: retire filename-matching entirely rather than patch it with a blocklist.** Vlad's choice, offered as the "Recommended" option alongside a blocklist-based alternative. Simpler than maintaining a generic-terms list going forward, and closes off this whole class of false positive structurally instead of chasing individual generic filenames as they come up.
- **What changed:**
  - **`app/api/screen-resumes/check-existing/route.ts`** — `possible_update` classification removed. `normalizeFileName`-based lookup (`byNormalizedName`) deleted. Route now only ever returns `"duplicate"` (exact content-hash match) or `"new"`.
  - **`lib/types.ts`** — `CheckExistingResult["status"]` narrowed from `"new" | "duplicate" | "possible_update"` to `"new" | "duplicate"`.
  - **`lib/resumeContentHash.ts`** — `normalizeFileName()` deleted (confirmed via repo-wide grep it had no other callers).
  - **`components/AlreadyScreenedCard.tsx`** — simplified to duplicate-only: `status` prop removed, the `possible_update` JSX branch (embedded `CrossReferenceChecker`, "Compare against the previous resume") deleted along with its now-unused `assessment` state and the `CrossReferenceChecker`/`roleContext` imports. This branch turned out to already be dead code by the time it was removed — see the note above.
  - **`app/projects/[id]/page.tsx`** — `possibleUpdateMatches` state, its construction in the upload-batch filter loop, and the `filenameMatch`/`filenameMatchFile` props passed to `ResultCard` all removed. The `possible_update`-vs-`new` branch inside the filter loop collapsed (both now just fall into `newFiles.push(f)` — nothing left to distinguish). `AlreadyScreenedCard`'s call site no longer passes `status`/`roleContext`.
  - **`components/ResultCard.tsx`** — the standalone `filenameMatch` banner (amber "Filename matches X" text + Compare button + embedded `CrossReferenceChecker`) removed entirely, along with its `filenameMatch`/`filenameMatchFile` props and `showFilenameCompare`/`filenameCompareAssessment` state. The existing `nameMatch` banner (previously plain, non-interactive text) gained the same Compare button + embedded `CrossReferenceChecker` the filenameMatch banner used to have — `screeningId={savedId}`, `crossRefScreeningId={nameMatch.id}`, `crossRefLabel={nameMatch.candidateName}` — using the exact `crossRefScreeningId` mechanism built a few hours earlier in this same session for the (now-removed) filenameMatch Compare, so that work carried over directly rather than being wasted. New state: `showNameCompare`/`nameCompareAssessment`.
- **Net effect:** Robert Hayes never gets a spurious match/Compare prompt at all (his real name doesn't match Nicholas Keenan's). A genuine same-person resume re-upload still gets the exact same Compare capability, just triggered by the more trustworthy `nameMatch` signal instead of a filename string. No blocklist to maintain going forward.
- **Not yet live-tested.** Needs: upload two different resumes sharing a generic filename ("Resume (16).pdf" style) and confirm NEITHER shows any match banner (both score independently, no noise); upload two resumes for the same real candidate under different filenames and confirm the `nameMatch` banner appears with a working Compare button that correctly shows this candidate as "Resume" and the matched one as "Cross-ref."

## Shipped (code-complete, NOT yet live-tested), 2026-07-15 — pipeline status restructuring

Design was locked earlier the same day (spec below, kept for context), then built the same session per Vlad's "let's keep going with that then."

- **Spec (verbatim, 2026-07-15):** "Interview -> Screening. The drop-down will be under screening now. Remove interview. The sequence is: New Applicant, Recruiter screen, Contacted, Screening (TA, L1, L2, in-person, offer, reject), Archived." Plus: old Screening-status rows land unplaced in Tracker ("set them as new in tracker tab"); auto-archive below-threshold saves directly to Archived; Location field lives on `tracker`, manually entered in the drawer; Role moves under the photo, click-to-edit; status+stage rendered as one connected control instead of two separate dropdowns; Next Step removed entirely (not automated).
- **`CandidateStatus`** (`lib/types.ts`) narrowed from 6 values to 5 — `interview` removed, `screening` is now the sole container status for the whole TA/L1/L2/In-Person/Offer/Reject arc. `CANDIDATE_STATUSES`/`CANDIDATE_STATUS_LABELS` updated to match. `TrackerEntry`/`FullTrackerData` gained `location: string`, lost `nextStep: string`. `ProjectSummary.interviewCount` renamed to `inTrackerCount` (now counts `status === "screening"`).
- **Two new migrations, must run in this order before deploying:**
  1. `supabase-migration-backfill-interview-status.sql` — `UPDATE screenings SET status = 'screening' WHERE status = 'interview'`. No schema change (`status` is plain text). Existing `interview` rows keep whatever `TrackerStage` they already have in `tracker`; rows with none show up unplaced.
  2. `supabase-migration-tracker-location.sql` — `ALTER TABLE tracker ADD COLUMN IF NOT EXISTS location text`.
  **Neither has been run yet as of this write.**
- **New merged control:** `components/StatusStageControl.tsx` replaces the old side-by-side `StatusSelect` + `TrackerStageSelect` pair (one joined pill, stage segment only shown when status is "screening"). `components/TrackerStageSelect.tsx` deleted (fully orphaned once both call sites — `app/candidates/page.tsx`, `app/projects/[id]/page.tsx` Pipeline tab — were migrated). `StatusSelect.tsx`'s `STATUS_COLORS` map exported so the new control reuses the same colors instead of duplicating them; its `interview` color entry removed (required for TS now that `CandidateStatus` dropped that value).
- **Drawer changes (`app/projects/[id]/page.tsx`'s `DrawerBody`):** Role field moved to directly under the profile photo, converted to click-to-edit (static text + pencil-icon button; input only appears after clicking, defaults to the project name same as before). New Location field added lower in the form (plain text input, manually entered — no auto-extraction). Next Step field, its state, and its save call removed entirely.
- **Auto-archive:** `lib/screenings.ts`'s `saveScreening` gained an optional `scoreThreshold` param; when provided and `result.score < scoreThreshold`, the insert's `status` is `"archived"` instead of `"new_applicant"` (both the DB write and the in-place `result.status` mutation used by the immediate API response). Both do-not-touch callers (`app/api/screen-resumes/route.ts`, `app/api/screenings/save-one/route.ts`) updated with a flagged do-not-touch exception to pass `scoreThreshold` through — `screen-resumes/route.ts` already computed it locally; `save-one/route.ts` gained a new `getProject()` lookup for it (mirrors the existing pattern in `screen-resumes/route.ts`).
- **Renamed for clarity while touching this code:** `interviewScreenings`/`interviewCount`/`iIds`/`interviewIds` → `trackerScreenings`/`trackerCount`/`trackerIds` throughout `app/projects/[id]/page.tsx` and `app/candidates/page.tsx`. Excel export's "Next Step" column replaced with "Location"; column-width array adjusted to match.
- **Deliberately NOT done yet, matching the `reject_reason` precedent** ([[feedback_migration_sequencing]]): `location` is written by `upsertTrackerEntry` but NOT added to `getFullTrackerEntries`'s shared SELECT — that shared select feeds the whole Tracker tab, so wiring it in before the migration is confirmed run would 500 every Tracker load, not just the one save. Follow-up: once Vlad confirms both migrations ran, add `location` to that SELECT's column list and mapped return object.
- **Verified:** `npx tsc --noEmit` clean across the whole repo (no type errors) after all of the above.
- **Two distinct groups after the backfill migration runs — do not conflate them:**
  - **Already-in-Tracker candidates (old `interview` status, real `TrackerStage` assigned in the `tracker` table):** the migration only touches `screenings.status`, never the `tracker` table, so their `TrackerStage` is completely untouched. They must land in the exact same swimlane column they were already in — Vlad's explicit requirement, 2026-07-15 ("move people that are in the tracker to stay in tracker just exactly where they are"). This was already true by construction (nothing in this restructuring writes to `tracker.stage`), but call it out explicitly here since the checklist below was ambiguous about it on first write.
  - **Old `screening`-status candidates (pre-restructuring meaning — "being screened," never had a `TrackerStage` since the old `TrackerStageSelect` only rendered for `status === "interview"`):** these newly qualify for the Tracker tab once `screening` becomes its container status, but have no stage assigned, so they land unplaced. This is the ONLY group that should show up unplaced — per Vlad's original "set them as new in tracker tab."
- **Not yet live-tested — checklist for the next session or Vlad directly:**
  1. Run both migrations, in the order listed above.
  2. Confirm a below-threshold screening now saves as Archived, not New Applicant.
  3. Confirm every candidate who was already sitting in a Tracker stage (old `interview` status) shows up in that exact same stage after the migration — not reset, not moved, not unplaced.
  4. Confirm an old `screening`-status candidate (never previously in the Tracker) shows up unplaced, not in some stage.
  5. Confirm Role click-to-edit works in the drawer (shows project name default, edit button reveals input, saves on blur).
  6. Confirm Location saves in the drawer and — once `getFullTrackerEntries` is updated per the note above — persists across reload.
  7. Confirm the merged status/stage pill renders and both segments work (status change, stage change, stage segment appears/disappears correctly with status).
  8. Confirm Next Step is gone everywhere, including the Tracker Excel export.

## Shipped (code-complete, NOT yet live-tested), 2026-07-15 — resume viewer fix + archive reason

- **Resume viewer black screen / silent auto-download, fixed twice same day.** First pass (fetch-based mime-type check before rendering) fixed the docx case but regressed the common PDF case — Vlad: "it still wants me to download it... it worked before." Root cause of the regression: the fetch to `/api/history/[id]` for the mime type failed closed (assumed non-previewable) whenever it was slow or didn't carry auth cleanly in the popup window's own session. Final fix: the caller (candidates/page.tsx, projects/[id]/page.tsx — both `window.open` call sites) now passes `resumeMimeType` and `fileName` as URL query params (`?mime=...&name=...`) since it already has them in memory — the document viewer (`app/interview/[id]/document/page.tsx`) reads them synchronously via `useSearchParams()`, no fetch, no race. No param (or an unrecognized one) means "assume previewable," matching the original always-iframe behavior; only a confirmed non-PDF mime type falls back to a "Download to view" panel. Required wrapping the page in `<Suspense>` per Next.js's `useSearchParams()` requirement.
- **Archive reason.** New fixed reason list — Tech skills, Domain knowledge, Failed cross-reference check, Not interested — capturable when a candidate's status is Archived, mirroring how the Reject tracker stage already captures `rejectReason`. Lives on `screenings.archive_reason` (not `tracker`, since Archived is reachable from any status, including candidates who never entered the Tracker — e.g. auto-archived below threshold). New `ARCHIVE_REASONS` const + `ScreeningRecord.archiveReason` (`lib/types.ts`), `updateScreening()` extended (`lib/screenings.ts`), `PATCH /api/history/[id]` accepts `archiveReason`, and `StatusStageControl.tsx` grows a third optional segment (reason dropdown) shown when `status === "archived"`, joined onto the same pill exactly like the existing stage segment for `status === "screening"`.
  - **Design defaults used — not confirmed with Vlad, AskUserQuestion tool errored out twice ("Tool permission stream closed") when asked:** (1) reason picker lives inline on the status pill itself (`StatusStageControl`), not in a separate drawer/detail view; (2) auto-archived (below-threshold) candidates start with no reason — recruiter sets one anytime from the same dropdown, no automatic 5th "below threshold" reason. **Flag these to Vlad next session if not already confirmed** — both were my best-guess defaults, explicitly stated as such when delivered.
  - Same migration-sequencing caution as `location`/`reject_reason`: `archive_reason` is NOT yet in the shared `SCREENING_COLUMNS` select — write path ships now, read-path wiring is a deliberate follow-up once the migration is confirmed run.
- **Verified:** `npx tsc --noEmit` clean across the whole repo. Not yet live-tested by Vlad.

## Shipped (committed, build-verified, NOT yet merged), 2026-07-17 — team-scoped access control, Word resume preview, FunnelView metrics fixes, Pipeline polish, SiteHeader polish

Handoff described 5 branches from a Cowork session; the actual working tree had ~18 distinct follow-ons stashed together, only 5 of which were asked for this pass. Confirmed with Vlad which approach to take (asked directly rather than guessing): careful surgical extraction of only the 5 named branches, leaving everything else stashed and unaddressed until a future session. The other ~13 follow-ons (in-app feedback form, fraud/duplicate "ring" clustering UI, stop-button for in-progress screening batches, smart candidate filters, notes field replacing Generate Question, must-have/nice-to-have filter styling, several more) are described in `memory/session-log.md`'s follow-on entries but remain uncommitted — **preserved in a single git stash, not lost.**

- **Branch 1 — `fix/team-scoped-access-control`** (`a9dcb5f`): real cross-team data leak closed. Every by-ID API route was missing team-membership checks (only list endpoints had them) — any authenticated user on any team could read/edit/delete another team's project, screening, resume, photo, LinkedIn PDF, or tracker entry by guessing a sequential id. New `canAccessScreening()`/`canAccessProject()`/`canAccessCalibrationExample()` in `lib/auth.ts`, wired into ~13 routes. Also fixes `deleteScreening()` leaving dangling duplicate/history-alert/name-match badge pointers, and admin-gates the previously wide-open `regenerate-trajectories` maintenance route. **Highest-severity fix this batch — merge first.**
- **Branch 2 — `feat/resume-word-preview`** (`cbc04f2`): Word resumes (.docx/.doc) now preview inline instead of forcing a download — new `GET /api/history/[id]/resume/preview` (mammoth for .docx, word-extractor plain text for .doc), persistent "Download original" button regardless of preview state. Depends on Branch 1's `canAccessScreening` — branched directly off Branch 1 (not merged yet); rebase onto `main` once Branch 1 merges, or merge Branch 1 first.
- **Branch 3 — `fix/funnelview-metrics-accuracy`** (`ff26239`): "Passed Threshold" was silently equal to "Total Screened" (no real threshold filter existed); "Reached Out" was overcounting archived candidates who were never actually contacted (fixed via `previous_status`). **Extraction note:** `lib/funnelview/data.ts` also carried an unrelated, unrequested rewrite (switching `totalScreened` from `screening_batches` to a live `screenings` count) mixed into the exact same function — deliberately excluded from this branch since it wasn't part of the ask; `screening_batches`-based totals are untouched here.
- **Branch 4 — `fix/pipeline-card-polish`** (`7be5f72`): Pipeline tab's plain "LI" text badge swapped for the real LinkedIn SVG mark (matches All Candidates); pre-existing archived candidates (archived before page load, no reason yet picked) now correctly sink into the Archived section via session-local `justArchivedIds` state instead of waiting forever for a reason nobody's going to add. **Extraction note:** `app/projects/[id]/page.tsx` had by far the heaviest mixing of any file this session (6 unrelated follow-ons in one 194-line diff — a smart-filters/notes-field pass, a stop-button for in-progress batches, fraud-cluster ring UI, and a pop-out-hover effect all interleaved with the two changes this branch actually wanted). Extracted by reconstructing from `main` and manually re-applying only the two relevant hunks, verified against the full diff line-by-line before committing.
- **Branch 5 — `feat/site-header-polish`** (`d099bb1`): header container widened to match the rest of the app (main cause of the cramped look), account button became a colored-initial avatar with a violet admin dot (same pattern as team chips), matching resting-state background added to ThemeToggle, header made sticky with a translucent blur. **Extraction note:** `components/SiteHeader.tsx` also carried an entirely unrelated in-app feedback form (new dropdown state, `POST /api/feedback` call, a whole alternate dropdown-content branch) woven into the exact same JSX block — excluded entirely; the dropdown's actual contents (email display, Change password, Sign out) are untouched, still exactly `main`'s version.
- **Do-not-touch clean on all five branches.** Every branch's `tsc`/`npm run build` verified in genuine isolation (stashed everything else, ran fresh) — not just as part of the combined tree.
- **NOT runtime-tested by this extraction — no network to Supabase, no live browser in the sandbox that wrote the original session's code.** Before merging Branch 1 specifically: click through your own account (resume/photo/tracker/credibility checks) to confirm nothing legitimate now 403s; if you have a second real team to test with, confirm cross-team access correctly 403s.
- **Compare URLs:**
  - `https://github.com/vashVlad/HireView/compare/main...fix/team-scoped-access-control?expand=1`
  - `https://github.com/vashVlad/HireView/compare/fix/team-scoped-access-control...feat/resume-word-preview?expand=1` (stacked on Branch 1 — compare against Branch 1, not `main`, until Branch 1 merges)
  - `https://github.com/vashVlad/HireView/compare/main...fix/funnelview-metrics-accuracy?expand=1`
  - `https://github.com/vashVlad/HireView/compare/main...fix/pipeline-card-polish?expand=1`
  - `https://github.com/vashVlad/HireView/compare/main...feat/site-header-polish?expand=1`
- **After all five merge:** update this file's "What's shipped" section for the new access-control model and resume-preview feature (per the original handoff's own instruction). Also: `tracker.screening_id`'s `ON DELETE CASCADE` rule is still an open item only Vlad can check (Supabase dashboard, Table Editor → tracker → screening_id → FK delete rule) — see open-questions.md.
- **Still stashed, not addressed this pass — a real decision point for a future session:** in-app feedback form (new `feedback` table + migration, `POST /api/feedback`, SiteHeader dropdown UI), fraud/duplicate "ring" clustering (`lib/matchClusters.ts`, union-find across match signals, colored Ring N badges), stop button for in-progress screening batches, smart candidate filter bar upgrade (multi-select status, fraud-signal toggle, score/date ranges), Notes field replacing the removed Generate Question tool, must-have/nice-to-have filter panel restyling, several smaller fixes (Analytics date labels + tooltips, team-picker viewport-collision flip, recruiter-attribution fix for admin-run screenings, pop-out hover on expanded cards, narrow-width text wrapping, AlreadyScreenedCard status chip). All still sit in a single git stash (message: "2026-07-17-handoff: full working tree, pending 5-branch surgical split") on top of `main` — recoverable, not lost, but will need its own scoping pass (likely several more branches) before it can ship.

## Shipped (committed, build-verified), 2026-07-16 — tracker interview-date/scheduled merge, admin picker portal fix, Phase 2.4 LinkedIn Comparison, misc width/spacing

Handoff described 3 sets of uncommitted changes; a `git diff` against `origin/main` (not the stale local branch tip) turned up 2 additional small, unrelated diffs not in the handoff — a Candidates-page width change and a ScoreBadge spacing fix, both explained by `memory/session-log.md`'s own follow-on entries (#17, #18) as real, separate, already-decided changes that just hadn't been called out in this handoff. Split into 4 branches instead of 3 for that reason:

- **`fix/tracker-interview-date-combined`** (`ae59f19`) — `app/projects/[id]/page.tsx` only. Matches the handoff description exactly.
- **`fix/team-member-picker-portal`** (`35eeadc`) — `app/admin/users/page.tsx` only. Hit a real merge conflict recovering this file from the stash (its 3-way merge base predated yesterday's admin/users redesign merge, and that file changed enormously between the two) — rather than resolve 6 conflict blocks by hand, reset the file to `main`'s clean version and manually re-applied the exact reviewed portal-fix diff (createPortal import, position-tracking state, scroll-close listener, both button handlers, both portal render blocks) via targeted edits, then verified the result was byte-for-byte the same diff against `origin/main` I'd reviewed before the conflict happened. Also dropped an unused `useRef` import that the original diff carried over from an earlier draft (same dead-import pattern spotted and fixed in yesterday's version of this file too).
- **`feat/linkedin-comparison`** (`a8baa02`) — `lib/types.ts`, `lib/assessCredibility.ts`, `app/api/assess-credibility/route.ts`, `components/CredibilitySection.tsx`, `memory/session-log.md`. Matches the handoff description; also carries a same-file bonus fix (not mentioned in the handoff, found via `memory/session-log.md`'s follow-on #17) for a real double-counting bug — the credibility check's "undisclosed employment" reverse-check was incorrectly generalizing to education, producing two rows for the same cross-reference degree entry. Scoped that check to employment-only and made the education rule explicit about exactly one row per degree.
- **`chore/candidates-width-and-scorebadge-spacing`** (`3aeddbb`) — the 2 unmentioned stray files. Candidates page width (`max-w-3xl` → `max-w-6xl`) was Vlad's own deliberate override of an earlier design-system judgment call (session-log follow-on #18) — not a stray edit, just uncommitted. ScoreBadge spacing (`gap-0.5`/`leading-none` on the score/delta lines) was a real Vlad-reported "smushed" visual bug (follow-on #17).
- **Do-not-touch check clean on all four branches** (`scoreCandidate.ts`, `analyzeJD.ts`, `parseResume.ts`, `calibrationExamples.ts`, `screen-resumes/route.ts`, `save-one/route.ts`).
- **Every branch verified in true isolation, not just as part of the combined tree** — stashed the other branches' files each time and re-ran `tsc --noEmit` + `npm run build` against just that branch's commit. All four passed clean in isolation. This is what caught the Branch 2 merge-conflict situation before it became a bad commit.
- **Real, still-open gap found while investigating, not part of any branch above:** `memory/session-log.md`'s follow-on #19 describes a diagnosed root-cause fix for the Team-page popover overlap bug — changing `app/globals.css`'s `@keyframes fade-in-up` end state from `transform: translateY(0)` to `transform: none` (a non-`none` transform makes an element a permanent CSS containing block once `animation-fill-mode: both` holds it after the animation ends, which is the actual mechanism behind the popover-overlap bugs fixed piecemeal in #14/#19/#21). That specific code change was **never actually committed** — confirmed directly against both `origin/main` and this session's working tree, both still have `translateY(0)`. Branch 2's portal fix works around the symptom for the two admin pickers specifically (portaling bypasses stacking context entirely), but the underlying bug still affects every other `.animate-fade-in-up` use (`ResultCard.tsx`, `ResumeUploader.tsx` per that same log entry) — not fixed in any of today's 4 branches since it wasn't part of what was asked. Flagged to Vlad rather than fixed unilaterally.
- **All 4 merged same day, 2026-07-16** — PR #21 (`fix/tracker-interview-date-combined`), #22 (`fix/team-member-picker-portal`), #23 (`feat/linkedin-comparison`), #24 (`chore/candidates-width-and-scorebadge-spacing`), confirmed via `git log origin/main`.
- **Live-test status:** Branch 2 (portal fix) already live-tested by Vlad per the handoff. Branch 1 (tracker date) and Branch 3 (LinkedIn — needs a real LinkedIn PDF upload) were not confirmed live as of merge; worth a pass whenever the app is used next.
- **"Phase 2.3" mentioned in the handoff as the next thing to build, but no scope was given** — nothing else in this handoff or in memory describes what Phase 2.3 is. Not started this session; Vlad said Cowork will pick it up.

## Shipped (committed, real build verified, NOT yet live-tested in the app), 2026-07-15 — Cross-Reference/Credibility Checker accuracy overhaul + score impact (Phase 2.4)

Vlad's ask: the existing Credibility Checker (built mid-June, extended with company-alias tolerance 2026-07-15 earlier this session — see the Teti feedback batch fix above) was still over-flagging real candidates. He gave a concrete example: a candidate who genuinely passed a cross-reference check got 5 flags, none of them real fraud — a staffing-agency-vs-client company name difference, a company short-name vs full-name ("GCB" vs "GCB Services"), a LinkedIn-simplified job title, a 6-month end-date difference that reads as normal LinkedIn imprecision, and a PhD "Expected 2029" that the checker incorrectly flagged against a LinkedIn range ending December 2029 — genuinely the same fact. He also flagged a real miss: a freelance role (SecureTheCloud) that only appeared on LinkedIn, overlapping other jobs, that the resume listed as a portfolio item rather than employment — the checker wasn't checking that direction at all. Also asked for the check to actually affect the score (visually, on the same ring, before/after in different colors) and for the save-reliability and "look at the old plan" items. This maps to Roadmap Phase 2.4 ("LinkedIn Comparison ... extends existing Credibility Checker") — first real build against that line item.

- **`lib/assessCredibility.ts` — full prompt/tool-schema rewrite.** Every `CredibilityRow` with `status: "discrepancy"` now also requires a `severity: "material" | "minor"` field. The tool schema spells out exactly what's "minor" (informational, not a real flag): staffing/consulting agency-vs-client-site naming (explicitly told the model this recruiter works in IT staffing, so this pattern is expected, not suspicious), company name variants beyond acronyms (short form vs fuller name, e.g. "GCB" vs "GCB Services" — extends the earlier company-alias fix from being acronym/rebrand-only), title phrasing differences for the same role/company/dates, LinkedIn's month-only date granularity (~2-month tolerance), and education single-year-vs-date-range comparisons (an "Expected YYYY" or graduation year is consistent with a range whose end lines up). "Material" stays reserved for genuinely hard-to-explain mismatches. Also added a second instruction pass: check the reverse direction — cross-reference entries with no resume counterpart at all (prefixed `"Undisclosed: "` in the row's `field`), especially ones overlapping another job in time; if the resume already surfaces the same activity in a non-employment section (portfolio, side projects — Vlad's SecureTheCloud example), the model is told to mark that minor, not material.
- **`computeCredibilityScoreDelta()`** — new pure function in the same file, NOT model-decided: counts `severity: "material"` discrepancy rows, deducts 8 points each, capped at -25 total. Runs after every `assessCredibility()` call and sets `assessment.scoreDelta`. Deterministic on purpose — same underlying facts always produce the same deduction, auditable, and a credibility check can dock a strong score but never invert it.
- **`lib/types.ts`** — `CredibilityRow.severity?: "material" | "minor"` and `CredibilityAssessment.scoreDelta?: number` added. No migration needed — `credibility` is already a JSONB column, so both new fields just ride along in the existing blob.
- **Score visual — `components/ScoreBadge.tsx`** gained an `adjustedScore` prop. When set and lower than `score`, the ring draws the original score's full reach in a muted rose arc, then the adjusted (post-deduction) score's reach in the normal tier color on top of it — same ring, before/after in two colors, per Vlad's explicit ask ("add the score from the cross-reference check to it... like addition to an actual score... a circular diagram"). The big number shown is always the adjusted score, with a small "82→74"-style label underneath. Wired into all three places a candidate's ScoreBadge renders with credibility data in scope: `ResultCard.tsx`, `app/projects/[id]/page.tsx`'s pipeline card, `app/candidates/page.tsx`'s All Candidates card.
- **`components/CredibilitySection.tsx`** — material discrepancy rows now render rose (matches the "significant_concerns" convention already used for the overall signal badge); minor ones stay amber but get a small "Minor" tag so a recruiter can tell at a glance which flags are real. Header badge turns rose (not amber) once any material flag exists, and a `-N score` chip appears next to the flag/match counts when `scoreDelta` is nonzero.
- **Save reliability fix.** All three real credibility-save call sites (`ResultCard.tsx`'s main checker, `app/projects/[id]/page.tsx`, `app/candidates/page.tsx`) used to do `fetch(...).catch(() => {})` — a dropped PATCH silently vanished with the UI still showing the result as if it saved. `CrossReferenceChecker`'s `onComplete` prop now returns `boolean | Promise<boolean>` (true = actually persisted); on `false` the component holds the just-computed assessment separately from the confirmed-saved one, shows a "Not saved yet — will be lost if you leave this page" banner, and offers a "Retry save" button that re-submits the same assessment without re-running the Claude call. The nameMatch-compare instance on `ResultCard.tsx` (an intentionally ephemeral, never-persisted comparison) is unchanged — `onComplete` there still just sets local state, which the new `void | boolean | Promise<boolean>` return type treats as success.
- **Fraud-signal interview questions (`app/api/history/[id]/interview-questions/route.ts`) also fixed** — it was feeding every `status === "discrepancy"` row into the fraud-probing prompt regardless of severity; now filters to `severity === "material"` only, so Phase 1.5's interview questions won't probe a candidate about a staffing-agency naming difference as if it were a real red flag. Found during a post-build verification pass, not part of the original ask, but directly undermined the point of the severity field if left unfixed.
- **Verified:** `npx tsc --noEmit` clean across the whole repo (re-verified after every file touched this session was individually resynced — see the bash-mount note below). `npm run build` could not run in this Cowork sandbox (no network access to download the Next.js SWC binary — a pre-existing sandbox limitation, not a code issue); needs a real build check via Claude Code on Vlad's machine, same as every other feature this session.
- **Not yet committed, not yet live-tested.** Suggested as its own branch/PR (`feat/credibility-accuracy-and-scoring`) — unrelated to the pipeline-status-restructuring work above, shouldn't stack onto that branch.
- **New failure mode found in the sandbox's stale-mount issue: trailing NUL-byte corruption, not just tail-truncation.** `lib/screenings.ts` (untouched this turn) showed `tsc` errors at a line number past its real end — turned out the bash-mounted copy had ~124 stray `\x00` bytes appended after otherwise-correct content, not a truncated tail. Fixed by finding the first NUL byte offset and truncating the file there (Python, since `sed`/`cat` don't handle embedded NULs cleanly). Six other files (including this turn's own edits to `lib/types.ts`, `app/candidates/page.tsx`, `app/projects/[id]/page.tsx`) had the ordinary tail-truncation flavor instead and were fixed with the established heredoc/tail-append technique. **New lesson: when `tsc` reports an error at a line number that doesn't exist in a fresh `Read`, check for embedded NUL bytes (`python3 -c "b'\x00' in open(f,'rb').read()"`) before assuming it's the familiar truncation pattern** — same root cause (stale sandbox mount), different corruption shape, different fix.
- **CRITICAL correction, same day, later in session: the bash-mount was never a disposable stale mirror — it's a live mount of Vlad's real files with a read-side sync lag.** An earlier `cat >>` append to `app/candidates/page.tsx`, done under the belief the file was truncated, actually duplicated real content and broke Vlad's real dev server (`Expression expected... 602 | );`). Fixed via the `Edit` tool (guaranteed to hit the real file); every other bash-touched file this session was individually re-audited via `Read` and confirmed clean. **Standing procedure from here on: never use `cat >>`/`sed -i` on a file to "fix" what looks like truncation from a bash `tail`/`wc -l` mismatch against a fresh `Read` — `Read` output is the only trustworthy source of file state in this sandbox. Full heredoc rewrites of exact fresh-Read content remain safe (idempotent). Treat flaky `tsc` output on a just-edited file with skepticism and re-verify via `Read`, not further bash writes.**
- **Follow-on fixes, same day (still Phase 2.4):**
  - `components/CredibilitySection.tsx` — default state changed from closed to open (`useState(true)`) per Vlad's ask ("keep the credibility/cross-reference tab open after it gives the result everywhere; just keep the option to close it"). The header button still toggles closed. Applies everywhere this component renders (ResultCard, Pipeline, All Candidates) since it's the one shared component.
  - `lib/assessCredibility.ts` — education-date rule, current state after three rounds of tightening this same day (Vlad: "if education lands in the same years, then it's a check", then two re-tests each surfacing a case still wrongly flagged): a resume's single graduation/expected year is `status: match` — never a discrepancy, never even Minor — if it falls within the cross-reference's date range, equals either endpoint, OR lands within ~12 months before/after either endpoint (covers the very common "official conferral date is months-to-a-year after the enrolled-term dates LinkedIn shows" pattern). Only years genuinely more than ~12 months outside both endpoints get flagged (Minor). This rule is now stated three times for redundancy — in the `status` field description, the `severity` field's education item, and its own top-level numbered instruction in the main prompt — because the first version (buried only in the schema) wasn't reliably followed by the model, and the second version (12-month window = minor rather than match) was too narrow per Vlad's actual expectation. Not yet re-confirmed against either of Vlad's two test examples (UIUC 2024/Nov-2023-range, PhD Expected-2029/Dec-2029-range) — flag to re-test both.
  - **Archive-reason picker added to the post-screening `ResultCard`** — previously only `StatusStageControl` (Pipeline/All Candidates cards) let a recruiter pick why a candidate was archived; the post-screening card (`components/ResultCard.tsx`, rendered right after scoring in the Screen tab) showed a bare status dropdown with no reason capture. `components/StatusSelect.tsx` gained optional `archiveReason`/`onArchiveReasonChange` props, rendering the same joined-pill reason segment as `StatusStageControl` when status is "archived" and a handler is supplied; falls back to the original plain select otherwise. `lib/types.ts`'s `CandidateResult` gained `archiveReason?: string` (mirrors `ScreeningRecord.archiveReason`, no migration needed — same existing column). `ResultCard.tsx` now holds local `archiveReason` state and wires it into `StatusSelect`. `app/projects/[id]/page.tsx`'s `ScreenTab` gained a `handleArchiveReasonChange` function mirroring the existing `handleStatusChange` (optimistic local update + `PATCH /api/history/[id]` with `{ archiveReason }`), wired into the `<ResultCard>` call site. No API route changes needed — `/api/history/[id]/route.ts`'s PATCH handler already accepted `archiveReason` in its body whitelist from the earlier Pipeline archive-reason feature.
  - All edits verified via `Read` only, per the corrected bash-mount procedure above.

- **Verification pass, next session — the "three tightening passes" education rule did NOT actually hold; found the real cause and fixed it with a 4th change.** Ran the two test cases Vlad specified against the *real* `assessCredibility()` function (not a re-typed copy) via `npx tsx`, repeatedly: the PhD/Expected-2029-vs-Dec-2029 case matched cleanly every time (5/5), but the MS-CS-2024-vs-Nov-2023 case was genuinely inconsistent — 3 of 6 runs correctly matched, 3 of 6 wrongly flagged a "minor" discrepancy, despite the prompt's own worked example explicitly stating this exact case should match. Not a fluke — reproduced deliberately.
- **Root cause, found by re-running with the model's own reasoning visible: it wasn't a math error, it was tool-schema field ORDER.** After tightening the rule to plain integer-year subtraction (see below) rather than fuzzy "~12 months," the model's `note` field (generated AFTER `status` in the schema) started showing perfectly correct arithmetic every single run — "2024−2023=1, in range, match" — while `status` still said `"discrepancy"` in all 5/5 of those runs. Claude fills a tool call's fields in the order the schema declares them; `note` came after `status`, so the correct reasoning was being generated too late to influence the decision it was meant to justify.
- **The actual fix: a new `reasoning` field inserted BEFORE `status` in `CredibilityRow`'s schema** (`lib/assessCredibility.ts`, plus `crossRef`→`reasoning` in `required`), explicitly instructing the model to work out the answer there first and match it in `status`/`severity` below. Also tightened the education rule itself alongside this: replaced "~12 months before/after" (ambiguous when the resume side is a bare year with no month) with plain integer-year subtraction against the cross-reference's start/end year — `resumeYear − startYear` and `resumeYear − endYear`, match if either is in `{-1, 0, 1}`, minor at exactly ±2, material at ±3 or more. Removes the month-estimation step entirely rather than just repeating the same ambiguous instruction a fourth time.
- **Re-verified after the fix: 12/12 clean.** Both test cases matched cleanly across 6 repeated runs each (was 3/6 failing on the MS-CS case before). Also ran a deliberate negative control (a resume year 5 years outside the range) 3 times to confirm the fix didn't overcorrect into leniency — all 3 correctly came back `discrepancy`/`material`/`scoreDelta: -8` with reasoning showing the right arithmetic (`2028−2023=5`, `≥3`, material). The fix discriminates correctly in both directions.
- **`lib/types.ts`** — `CredibilityRow` gained `reasoning?: string`, documented as model scratch space only; not read anywhere in the UI (`CredibilitySection.tsx` doesn't reference it), purely a generation-order fix.
- **Also recovered a real orphaned commit from the prior branch, unrelated to credibility:** `feat/pipeline-status-and-archive-reason` had been merged as PR #17, but two commits pushed after that merge (`920a658` wiring `location`/`archive_reason` into the shared read paths, `cf28da3` its docs) were never actually merged — confirmed directly against `origin/main`'s `lib/screenings.ts`, which still lacked both. Recovered onto their own branch `fix/location-archive-reason-read-wiring` (created pointing at the existing commits, no rebase needed since their merge-base with `origin/main` is exactly the already-merged commit) and pushed for its own separate PR. **Practical impact while unmerged: a recruiter can save a Location or archive reason and it silently doesn't survive a reload** — not data loss in the DB, just never read back. Flagged to Vlad directly; needs its own review/merge independent of this branch.
- **This session's sandbox Bash tool hit a transient classifier outage** partway through (heavier commands — `npm run build`, `next build`, plain `node` — failed for ~15 minutes while `echo`/`tsc` kept working); resolved after Vlad ran `npm run build` himself once, confirming it wasn't a real code problem. Not a repo issue, noted here only because it explains the gap between "do-not-touch check done" and "build confirmed" in the session log.
- **Committed and pushed, 2026-07-15.** `git status`/diff confirmed for real against the actual repo (the handoff's warning about a prior session's unreliable git output didn't apply here — this sandbox's git was accurate throughout). Do-not-touch check clean on all six files (four true do-not-touch, two previously-flagged `scoreThreshold` exceptions, both unchanged this round). Full diff review of all 11 changed files matched the handoff description exactly. `npx tsc --noEmit` and `npm run build` both clean (build confirmed real, not from a stale cache — all 42 routes generated). Branch `feat/credibility-accuracy-and-scoring`, created fresh off `main` (one clean prose conflict resolved in `memory/decisions-log.md` during the stash pop — the "upstream" side had no new content at that insertion point, so it was a straight keep-the-stash resolution). Compare URL: `https://github.com/vashVlad/HireView/compare/main...feat/credibility-accuracy-and-scoring?expand=1`.
- **Manual verification note:** the two education test cases were re-run against the real `assessCredibility()` function directly (via `npx tsx`), not just read-reviewed — this is real verification, not a code-review guess. The UI-only checklist items (CredibilitySection open-by-default, archive-reason picker width, Pipeline/All Candidates archiving unaffected) were confirmed by direct code inspection only — this sandbox has no browser automation, consistent with the standing project note that live UI testing needs a human clicking through it. Still needs: Vlad's own click-through pass, plus his sign-off on the `-8`/`-25` scoreDelta magnitude (open question already logged) once he's seen it live against a real material discrepancy.

## Shipped (committed, build-verified), 2026-07-15, end-of-day consolidated batch — three-epic feature batch + Team page redesign + design system + logo + latency pass

Everything below (originally follow-ons #12 through #16, all same day) sat as one continuous uncommitted working tree with no branch/commit boundaries anywhere in it. Split into 2 PRs by actual file-disjointness rather than the originally-suggested 4, since #12/#13/#14 are sequential rewrites of the exact same ~1000-line `app/admin/users/page.tsx` with no real seam to split at, and #15/#16 layer directly onto #12's `app/candidates/page.tsx`/`app/projects/[id]/page.tsx` the same way — reconstructing intermediate states that were never separately committed would mean risky, untestable manual surgery on a single file's diff for no real review benefit. The two PRs that resulted ARE genuinely file-disjoint (confirmed, not assumed) except for one dependency caught during isolation-testing:

- **PR 1 — `feat/team-project-management`** (commits `e28dddb`, `4026305`): the full Team/Project Management epic — admin drag-and-drop UI, add-member/add-project popovers, the CSS containing-block bug fix, team renaming, project↔team reassignment. Files: `app/admin/users/page.tsx`, `lib/teams.ts`, `app/api/admin/teams/[id]/route.ts`, `lib/projects.ts`, `app/api/projects/[id]/route.ts`, `app/globals.css`, plus `components/PageHeader.tsx`.
- **PR 2 — `feat/pipeline-scoring-design-system-and-latency`** (commit `ce92633`): ScoreBadge redesign, Archived pipeline section, Interview Date auto-schedule, FunnelView recruiter chips, the app-wide design-system pass + new logo, ScrollToTopButton, and the two credibility-check latency fixes. Files: `app/analytics/page.tsx`, `app/api/assess-credibility/route.ts`, `app/auth/set-password/page.tsx`, `app/candidates/page.tsx`, `app/funnelview/page.tsx`, `app/login/page.tsx`, `app/projects/[id]/page.tsx`, `app/projects/page.tsx`, `components/ScoreBadge.tsx`, `components/SiteHeader.tsx`, `lib/assessCredibility.ts`, plus new `components/Logo.tsx`, `components/PageHeader.tsx`, `components/ScrollToTopButton.tsx`, `app/icon.svg`.
- **Dependency caught while verifying PR 1 in true isolation** (stashed PR 2's files, re-ran `tsc` against PR 1 alone): `app/admin/users/page.tsx` already imports the new `components/PageHeader.tsx` from the design-system pass (follow-on #15 touched this file too, after #12-#14 — it wasn't as disjoint as the file list first suggested). Added `PageHeader.tsx` to PR 1 as well (commit `4026305`) — identical content on both branches, so they merge into `main` in either order with zero conflict on that file.
- **Do-not-touch check:** zero diff on all six do-not-touch files (`scoreCandidate.ts`, `analyzeJD.ts`, `parseResume.ts`, `calibrationExamples.ts`, `screen-resumes/route.ts`, plus the previously-flagged `save-one/route.ts` exception — unchanged this round).
- **Build verification, both real this time:** `npx tsc --noEmit` clean on the full combined tree, and separately re-confirmed clean on PR 1 in true isolation (PR 2's files stashed away) after the PageHeader fix. `npm run build` was clean on the full combined tree (43 routes, including the new `/icon.svg` route) — a repeated ~15-minute sandbox classifier outage (unrelated to this repo; heavier commands like `npm run build`/`node` failed while `echo`/`tsc` kept working) blocked re-confirming the full build separately on each isolated branch, but the type-level check (which would have caught the PageHeader dependency, and did) passed on both.
- **Credibility-check latency claim verified directly, not eyeballed:** ran three real `assessCredibility()` calls back-to-back via `npx tsx` and inspected the actual Anthropic API response's `usage` field rather than trusting wall-clock timing (too noisy over a network call to be conclusive on its own) — every call showed `cache_read_input_tokens: 2325`, `cache_creation_input_tokens: 0`, confirming the tool-schema cache is genuinely being read on repeat calls, not just present in the code with no effect.
- **Visual/UI click-through — NOT done, and said so plainly rather than guessing.** No browser automation tool is available in this session (same standing limitation noted throughout this project's history) — cannot verify logo rendering, ScoreBadge hover tooltip, ScrollToTopButton fade/position, drag-and-drop interactions, popover visuals, or general layout across Projects/Candidates/Analytics/FunnelView. Everything above was verified by direct code reading against the stated behavior, plus the one thing genuinely testable without a browser (the caching claim, verified via real API calls). **Needs Vlad's own click-through** per the checklist the handoff specified: Team page add-member/add-project popover persistence + real outside-click dismissal; archived-card dimming/expand behavior + ScrollToTopButton fade and scroll-to-top on both pages; logo rendering on header/favicon/login/set-password in light and dark mode; general visual pass on Projects/Candidates/Analytics/FunnelView.
- **Not yet merged.** Compare URLs: `https://github.com/vashVlad/HireView/compare/main...feat/team-project-management?expand=1` and `https://github.com/vashVlad/HireView/compare/main...feat/pipeline-scoring-design-system-and-latency?expand=1`.

## Built this session, not yet migrated/tested/committed (2026-07-15, three-epic feature batch — Pipeline & Scoring UI / Tracker & Funnel / Team & Project Management)

**Superseded by the section directly above — kept for the original build detail (design decisions, exact file-level reasoning) but "not yet committed" below is stale; this shipped as part of the two PRs described above, both build-verified.**

- **Context:** Vlad gave a large verbatim three-epic spec in one message after the credibility branch's live UI click-through (see the credibility verification note above) passed clean. No schema changes — everything below is UI/query-layer only. Four ambiguous design questions resolved via `AskUserQuestion` before building: ring colors → **component color** (yellow=screening, red=cross-reference), not tier color; card dimming scope → **archived status only**, not "undefined" cards too; archive trigger → same trigger as today (no change to the auto-archive threshold logic); team drag-and-drop → **admin-only, drop replaces the project's current team** (no multi-team-per-project).
- **Pipeline & Scoring UI (3 of 3 done).**
  - **Card visuals** — `app/projects/[id]/page.tsx`'s Pipeline tab card and `app/candidates/page.tsx`'s All Candidates card both dim (`opacity-50 saturate-[0.6]`, `hover:opacity-90` to restore full readability on demand) when `s.status === "archived"`. Archived-only, per the confirmed scope.
  - **`components/ScoreBadge.tsx` fully redesigned** — the ring no longer colors by score tier (was emerald/sky/amber/rose by magnitude). It now always renders the "screening" component in amber/yellow; when `adjustedScore` is present and lower than `score` (a credibility deduction happened), a red arc is drawn first at the raw score's full reach so the leftover tail (adjustedScore→score) reads as the deduction — same underlying geometry as the previous split-ring, just recolored to be component-based instead of tier-based. New hover state (plain `useState`, no extra deps) shows a small breakdown card: Screening `{score}`, Cross-reference `−{deduction}` (only when there's a deduction), Final `{displayScore}`. Prop signature unchanged (`score`, `size`, `adjustedScore`) — all four call sites (`ResultCard.tsx`, `AlreadyScreenedCard.tsx`, Pipeline tab, All Candidates) needed no changes.
  - **Archived section on the Pipeline tab** — `PipelineTab`'s sort now always sinks `status === "archived"` candidates below every active one (score sort still applies within each group), and the render map inserts a labeled divider (`Archived (N)`) right before the first archived card, using `Fragment` per list item so the divider and the card share one map iteration without a JSX restructure of the ~350-line card body. No separate `<ul>`/duplicated card markup — kept as one list, divider-separated, to avoid the risk of drifting the two card renderings apart.
- **Tracker & Funnel Enhancements (2 of 2 done).**
  - **Interview Date auto-schedules** — `app/projects/[id]/page.tsx`'s `DrawerBody`, the Interview date input's `onBlur` now also sets `scheduled: true` (local state + saved field) when a non-empty date is entered and it wasn't already scheduled. One-directional by design — clearing the date does NOT auto-unschedule, since a confirmed interview's date might need clearing for re-confirmation without losing "scheduled" status. Matches Vlad's literal ask ("saving an interview date should automatically toggle the status to Scheduled").
  - **Recruiter visibility on FunnelView** — new `activeRecruiters` derived list + a chips row (colored initial avatar, reusing `lib/avatarColor.ts`, truncated email on hover) added to the Funnel card header on `app/funnelview/page.tsx`, above the existing StageBar.
- **Team & Project Management (3 of 3 done).**
  - **Project↔team reassignment** — `lib/projects.ts`'s `updateProject` gained `teamId?: number | null`; `app/api/projects/[id]/route.ts`'s PATCH admin-gates this specific field (`getAuthUser`/`isAdmin`) before passing it through. Previously a project's team was set once at creation (auto-assigned to the creator's primary team) with no way to change it — this was the actual gap behind Vlad's "Project Mobility" ask.
  - **Team renaming** — new `renameTeam()` in `lib/teams.ts`, new admin-gated `PATCH /api/admin/teams/[id]` (existing `DELETE` handler preserved unchanged below it).
  - **Drag-and-drop admin UI** — `app/admin/users/page.tsx` rebuilt as a two-column grid (`lg:grid-cols-[1fr_300px]`): left column keeps all existing team/member management verbatim, adds inline click-to-edit team names and a per-team dashed-border drop-zone showing assigned projects as removable violet chips; right column is a new scrollable Projects rail, each project `draggable`, showing its current team or "Unassigned." Native HTML5 drag-and-drop (no library), matching the pattern already used for the JD upload drop zone. Optimistic update + revert-on-failure for both rename and reassign, matching the page's existing pattern for member add/remove.
- **Verified by direct `Read` after every edit only** — no `tsc`/`npm run build` run this session (this sandbox cannot fetch the SWC binary needed for `next build`, confirmed again this session while unrelated to a separate Claude-Code build blocker Vlad mentioned; pre-existing, documented limitation, not new). **Needs from Vlad: a real `npm run build`/`tsc --noEmit` on his own machine, then a live click-through** of all eight items above before commit/push/PR — none of this has been tested against the actual running app yet, unlike the credibility branch which got a full live pass first.
- **Not yet committed** — sitting as uncommitted changes in the working tree as of this note. Needs its own branch (one feature = one branch = one PR per the standing rule — this is arguably 3 features bundled by Vlad's own request in one message, worth asking whether he wants 1 branch or 3 before pushing).

## What's NOT shipped yet

(nothing currently — Outreach Drafting was descoped, see below; FunnelView is now fully validated)

## Impact metrics

- **Validated 2026-07-09** — see Impact Report (`docs/HireView-Impact-Report.docx`): 78 real candidate screenings, funnel numbers confirmed, business value calc added ($136.50 on one role from 3.9 hours saved, conservative per-role floor). The BLUEPRINT's older 60% throughput figure is untouched/unvalidated against this specific dataset — still worth reconciling per open-questions.md. TA false positive rate still not measured.

## Descoped

- **Outreach drafting** — removed from the roadmap 2026-07-09 per Vlad, not needed. Was listed as "Phase 3" (CLAUDE.md) / "Phase 4" / "Phase 5" (dev log, inconsistently) since Phase 0 and never started. See decisions-log.

## Deploy / migration status

**Pending (as of 2026-07-15):** `supabase-migration-backfill-interview-status.sql` then `supabase-migration-tracker-location.sql`, in that order — required for the pipeline status restructuring above. Also `supabase-migration-archive-reason.sql` (order-independent of the other two — different column, same `screenings` table). None of the three confirmed run yet.

**Already applied** (cleaned up 2026-07-09 — this list had accumulated years of already-run migrations still marked "pending"; removing entries only when there's a direct confirmation in session-log.md, not just assumption): `supabase-migration-history-alert.sql` (Feature 1.4, confirmed run — resolved the 2026-07-09 app-wide outage, then live-tested successfully), `supabase-migration-teams.sql` (Feature 1.3, confirmed by Vlad same session), `supabase-migration-screening-actions.sql` (Feature 1.2, confirmed live-tested), `supabase-migration-fingerprints.sql` (Feature 1.1, confirmed live-tested), `supabase-migration-interview.sql`, `supabase-migration-multiuser.sql`, `supabase-migration-threshold.sql`, `supabase-migration-batches.sql` — all implied done since the features they back (interview view, multi-user auth, configurable threshold, analytics) are in daily use and this session's own testing depended on auth/team features working.

**Still unconfirmed:** the two `team_id` straggler-backfill queries (screenings, resume_fingerprints) given to Vlad during the 2026-07-09 outage — see open-questions.md.

## Repo layout note (2026-07-09)

Narrative `.docx` docs moved from repo root to `docs/`. `HireView_Prior_Art_Document.pdf` also moved into `docs/` (was previously in `memory/`, which is meant for session-continuity files, not narrative/external documents). All `supabase-migration-*.sql` files moved from repo root to `supabase/migrations/` (no renaming, just relocated). Root now only holds `CLAUDE.md`, `README.md`, `AGENTS.md`, and standard project config. Not yet committed — see open-questions.md.

## Stack

Next.js (App Router) · Tailwind · Anthropic API (`claude-sonnet-4-6`, server-side only) · Supabase · Vercel. TypeScript.

## Recruiter-facing workflow (as of the redesign)

Projects page → "New role" → paste/upload JD → analyze → name it → land on Filters tab → Screen tab (upload resumes, scoped to role, no re-entering JD) → Pipeline tab (review/flag/note) → Tracker tab (move through interview stages) → All Candidates for cross-role search.
