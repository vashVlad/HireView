# Cirot — Product Roadmap
**Owner:** Vladyslav Vashchuk
**Last Updated:** August 15, 2026 (brought current — see note below)
**Strategic Position:** Trust & Verification Layer for AI Recruiting

**Note on this update:** this file had not been touched since July 8, while most of Phase 1, FunnelView, and half of Phase 2 shipped in the meantime — the actual living log of that work is `session-log.md`/`open-questions.md`/`state.md`, which stayed current the whole time. This rewrite brings the table below in line with what `00-index.md`/`state.md` and this session's own code-accuracy pass confirm is actually shipped, and folds in everything decided in the 2026-08-14/15 roadmap sessions. Treat `session-log.md`/`open-questions.md` as the source of narrative truth going forward; this file is the prioritized summary view.

---

## North Star
Cirot is not an ATS. It is the verification layer that makes any sourcing tool safe to use at scale. Every feature must answer: does this increase trust, reduce fraud, or improve recruiter decision quality?

---

## PHASE 1 — FRAUD PREVENTION — **COMPLETE, MERGED TO MAIN**

| # | Feature | Problem Solved | Status |
|---|---------|---------------|--------|
| 1.1 | Duplicate Resume Detection | Catches word-for-word resumes under different names. Match on skills hash, responsibility vectors (text, not embeddings), metric claims, career arc — NOT names/contact info | **Shipped 2026-07-08** |
| 1.2 | Recruiter Attribution | Every action logged with name + timestamp | **Shipped 2026-07-09** |
| 1.3 | Teams Architecture | Admin → Team → Projects → Users, full isolation | **Shipped 2026-07-09** |
| 1.4 | Candidate History Alert | Reuses 1.1's fingerprints — "Previously seen" / "Known fraud pattern" banners | **Shipped 2026-07-09** |
| 1.5 | Fraud-Aware Interview Questions | Duplicate/credibility flag → interview questions probe the specific inconsistency | **Shipped 2026-07-09** |

All of 1.1–1.5 confirmed merged via PRs #1–#6.

---

## FUNNELVIEW — MANAGER VISIBILITY — **SHIPPED**
Admin-only funnel view (`app/funnelview/`, `lib/funnelview/`), live data, no CSV/external tools. Excel export includes recruiter attribution, target-company match flags, and (per this session) is getting a "Signals" column — see Phase 2.5 below.

---

## PHASE 2 — INTELLIGENCE LAYER

| # | Feature | Status |
|---|---------|--------|
| 2.1 | Cross-Project Fit Suggestion | **Shipped 2026-07-10** — auto-fire gate went through several rebuilds, live-tested, merged (PR #10) |
| 2.2 | Contextual Search (semantic synonyms) | **Superseded by Global Talent Search, Phase 2.5 below** — same underlying need, upgraded from keyword synonyms to real embeddings |
| 2.3 | Smart Unified Candidate View | **Shipped in substance** — `/candidates` (All Candidates) already has cross-project filtering (status, project, flagged, fraud, blacklisted, score range, date range) |
| 2.4 | LinkedIn Comparison | **Shipped** — `assessCredibility.ts`'s `linkedInSignals` field, LinkedIn-specific prompting, detected via `detectLinkedIn()` |

**Also shipped since the last version of this doc, not originally listed here:** Archive Fits (reuse archived candidates across projects), Transfer (move a candidate between projects with optional re-score), Calibration feedback loop (thumbs up/down → `calibration_examples`), Blacklist, Target-company score boost, current company/title/trajectory fields on every screening.

---

## PHASE 2.5 — TRUST & PRECISION LAYER (decided 2026-08-14/15, Cowork planning sessions, NOTHING BUILT YET)
*Full narrative and reasoning trail lives in `session-log.md`'s 2026-08-14/15 entries; build-ready specs live in `open-questions.md`. This table is the priority summary — read those files for the actual detail before building.*

| # | Feature | Problem Solved | Depends on | Status |
|---|---------|----------------|------------|--------|
| 2.5.1 | JD Checklist ("Trust badge") | Enriches must-have/nice-to-have into a precise, individually-editable, individually-reasoned checklist per JD. Critical path — two other items below need it. | Nothing (new file, consumes `analyzeJD.ts`'s existing output) | **Built 2026-08-17, live-verified/committed by Claude Code same day.** **Simplified 2026-08-17 (later, Cowork), not yet committed** — direct Vlad feedback on the shipped UI: the "Decrease score" / "Add score" tab split was confusing (positively-phrased labels like "Python used in AI or software project" sitting under a "Decrease" tab, with inverted fired=true-means-the-gap-is-present logic behind it) and he wanted the checklist to only ever build score up, never down — real deductions belong to the credibility check alone. Now a single additive-only list: `generateChecklist.ts` no longer emits a category distinction, `computeChecklistScoreDelta` (lib/evaluateChecklist.ts) always adds on fire regardless of any legacy stored `category` value (no data migration needed — any already-generated project checklist with old "decrease" items now scores them identically to "add" the moment they fire), Filters tab UI collapsed to one list/one "+ Add item" button. `category` kept on the type only for backward-compatible display of already-frozen historical evaluations. `tsc` clean, 6/6 checklist unit tests pass (2 new cases specifically covering the legacy-decrease-still-adds guarantee). **Scoring changed again same day (later, Cowork):** Vlad — "I though that I want to have only the checklist." When a project has a checklist, the candidate's SCORE is now `computeChecklistPercentageScore` (fired points ÷ total possible points × 100), overriding the AI's own 0–100 judgment entirely — AI summary/strengths/concerns/careerTrajectory still generated and shown for context, per Vlad's explicit choice. Target-company boost kept as a stacked bonus on top (Vlad: "I'd keep the companies boost"), applied after the checklist-percentage base. No checklist configured → unchanged AI-score + target-company-boost behavior. Rescreen route (`app/api/history/[id]/rescreen/route.ts`) updated to mirror this — it bypasses `saveScreening()` entirely and had zero checklist/target-company logic before this fix, a real correctness gap under the new model. `tsc` clean, 14/14 checklist unit tests pass (8 new). See decisions-log.md's matching entry. Not yet live-tested or committed. |
| 2.5.2 | Credibility trajectory overlay | Replaces `assessCredibility.ts`'s field-by-field `rows` with a real second trajectory (cross-ref document run through the same extraction), overlaid on the same graph, diffed in code, mismatches reasoned over by one small scoped AI call | Nothing — the stated dependency on 2.5.1 (checklist) turned out not to apply to the design actually built; see decisions-log.md's 2026-08-17 entry | **Built, live-verified, and committed 2026-08-17 (commit `88e08dc`)** — decided design (rewrite, not overlay-on-existing-`rows`) after finding the real blocker was a missing structured data model, not the `rows` mechanism itself: `careerTrajectory` has only ever been freeform prose, no per-role structured data existed anywhere to graph or diff. New `trajectoryEntries` field on `scoreCandidate.ts` (do-not-touch exception), `lib/matchTrajectoryEntries.ts` (deterministic diff, 20/20 tests), `lib/compareEducationYear.ts` (education tolerance moved to pure code, 11/11 tests), `lib/assessCredibility.ts` restructured additively (new branch only when cross-ref + stored trajectory both present, old single-call flow untouched as fallback), `components/TrajectoryGraph.tsx` (hand-built timeline, no chart dependency), `components/CredibilitySection.tsx` wired to reuse the existing Flags/Matches tabs via a mapper. **Live-verified against the real Anthropic API and a real browser** — real rescreen produced a correctly-shaped 15-entry trajectory, a real credibility check exercised confident matches, a diff-then-reclassified-back-to-match case, the positional judgment merge across 4 real flagged rows, and the education pure-code path catching a real discrepancy; fallback path confirmed unregressed for old candidates. **One real bug found and fixed live:** the Rescreen route wasn't passing `trajectoryEntries` through to the save — would have silently blocked every existing candidate's only path to gaining trajectory data (no backfill script exists). Two known, non-blocking characteristics: extraction occasionally splits a compound title into two entries (degrades gracefully, not surfaced as a false flag) and the judgment call shows real model non-determinism on borderline severity calls (a 4-month gap graded "minor" once, "material" once) — both worth knowing, neither is a code bug. |
| 2.5.3 | GitHub/skills corroboration (Gate 4) | Folds a free GitHub API fetch into the *existing* cross-reference check as a third evidence source — not a separate feature/button | Nothing — 2.5.2 is now built, no longer a blocker | **Partially built 2026-08-17** — `lib/githubCorroboration.ts` (username extraction + Voyage-style best-effort fetch), attached as a pure code-side passthrough (`CredibilityAssessment.githubSignal`) shown as a neutral info panel, deliberately NOT fed into `assessCredibility.ts`'s prompt. Folding it into the actual AI judgment call (now the new `judgeTrajectoryComparison` step, not the old single-call flow) is the deferred remainder of this item — unblocked now that 2.5.2 shipped, but not attempted this round; a distinct, well-scoped next increment. |
| 2.5.4 | Consolidated candidate card | One shared card component replacing 3 separate implementations (`ResultCard.tsx`, Pipeline tab, All Candidates page) | Nothing — 2.5.2/2.5.3 base work is built | Unblocked as of 2026-08-17, not yet started. |
| 2.5.5 | Archive-fit two-stage matching | Fixes the current mechanism (an AI-guessed title matching another AI-guessed title, no real evidence) — stage 1 stays as-is, new stage 2 does cheap deterministic matching against the checklist | 2.5.1 | **Built 2026-08-17** — `lib/matchArchiveFitToChecklist.ts`, coverage-based deterministic matching, unit-tested (6/6). Wired into `lib/archiveFits.ts`/`app/projects/[id]/page.tsx` as "Evidence: ..." chips. |
| 2.5.6 | FunnelView "Signals" column | Renames the unused "Total experience" export column to "Signals" — strongest strength + weakest concern, both already generated, zero new AI calls | Nothing (one small do-not-touch ordering fix on `strengths`) | **Built 2026-08-15** |
| 2.5.7 | Tracker Reject column collapse | Reject column grows unbounded — collapses to a count + expand toggle | Nothing | **Built 2026-08-15** |
| 2.5.8 | Shared loading animation | One generative scrolling-line component for every wait state (screening, cross-ref check, archive-fit check, suggested-role-fit) — several currently have no indicator at all | Nothing | **Built 2026-08-17** — `components/ScoringLoader.tsx`, wired into every real wait state app-wide (confirmed via repo-wide grep: zero `animate-spin` instances remain). |
| 2.5.9 | Global talent search | Natural-language search across every candidate/project via embeddings (Voyage AI) — new infrastructure, no vector/embedding capability exists in this codebase today | Nothing structurally, but the biggest single lift | **Backend + UI both built, live-verified in a real browser, and committed** (`5911708`, `4e38b64`, `f0ef0b8` on `feat/jd-checklist`). UI redesigned into one search box with a mode toggle (Name/Skills) + a collapsed Filters panel per Vlad's direct feedback, 3 iteration rounds, all confirmed rendering correctly via real Playwright screenshots (not blind sandbox guesses). Ranking logic itself proven correct against real data in an earlier pass (correct top result at 0.63 vs 0.46 similarity, etc.). **Remaining blocker: the embedding backfill (~397 candidates) is still running — 118/398 as of last check** — search currently returns real `200 {results: []}` (not broken, just racing the backfill for Voyage's 3 req/min budget). **CRITICAL, still not done: `one-off-reindex-screenings-embedding.sql` must be re-run once the backfill finishes** (only run against 2 test rows so far). Best path to finish the backfill: run `npx tsx scripts/backfill-candidate-embeddings.ts` directly from a real terminal — Claude Code's own background-task runner keeps getting killed every ~2min, not a script problem. Once backfill + reindex are done, one final positive-match search screenshot closes 2.5.9 entirely. |

**Explicitly excluded from this round (need access Vlad doesn't have):** Gate 5 (identity realness — needs a paid identity-resolution API like Pipl) and Gate 6 (criminal check — needs a paid background-check vendor).

**Batch-load note:** of everything above, only 2.5.9's embedding call touches the automatic screening path (`screen-resumes/route.ts`, CONCURRENCY=3/maxDuration=300) — it would run as a third parallel call alongside the existing score+fingerprint pair. Everything else is recruiter-triggered, one-off, and never runs during a batch upload. Worth timing the real embedding call once built — this exact route already needed its timeout raised once (60s→300s) when per-resume work quietly grew past the ceiling.

---

## PHASE 2.6 — TWO-GATE SCREENING & TRAJECTORY REDESIGN (decided 2026-08-19, Cowork planning session, NOTHING BUILT YET)
*Trigger: Vlad anticipating high-volume automatic intake from Brillio ("2000 applicants... the checklist must be a main gate") — full point-by-point reasoning in `decisions-log.md`'s two 2026-08-19 entries. This table is the build-order sequencing; read those entries for the actual detail before building. Supersedes 2.5.1's "checklist IS the score" model and 2.5.2's graph Y-axis for the pieces below.*

**Tier 1 — foundational, build first (everything else in this phase reads off this):**

| # | Feature | Problem Solved | Depends on | Status |
|---|---------|----------------|------------|--------|
| 2.6.1 | Checklist `tier` field (must-have/nice-to-have) | Points already imply priority but aren't reliable enough to sort by; display needs must-haves listed first | Nothing | **Fully built 2026-08-19 (later still).** Type + schema + generation (`lib/types.ts`, `lib/generateChecklist.ts`) plus denormalization onto `ChecklistItemResult` (`lib/evaluateChecklist.ts`, same pattern as `label`/`category`/`points`). Display: `app/projects/[id]/page.tsx`'s `FiltersTab` (NOT `FilterSetView.tsx` — corrected a wrong guess in the architecture doc) now sorts must-have first and shows a small "must" badge per item, purely a render-time sort — doesn't mutate the saved item order. `tsc` clean. |
| 2.6.2 | Gate 1 → Gate 2 pipeline wiring | Checklist becomes a hard pass/fail gate BEFORE the expensive full score runs, not an after-the-fact score override. Reorders `screen-resumes/route.ts`: `evaluateChecklist()` runs first; below the project's existing `scoreThreshold` → archive immediately with the checklist percentage as the shown number, skip `scoreCandidate()`/fingerprint/embedding entirely; clears it → full Gate 2 pipeline as today, checklist no longer overrides that score. Also needs the "prefer false when unclear" bias in `evaluateChecklist.ts` to lean lenient — a false negative now silently ends a candidate's chances, not just a lower informational number. **Card UI, decided 2026-08-19 (later):** a gate-1-archived candidate gets a visibly distinct, simpler card — no AI summary/strengths/concerns/trajectory (never generated, Gate 2 never ran) — just the checklist score plus a matched/didn't-match list of every item, so it reads as "wasn't deep-screened" at a glance rather than looking like missing data on a normal card. | 2.6.1 (tier field informs display, not the gate logic itself, but ships together) | **Backend built and verified 2026-08-19 (later)** — `app/api/screen-resumes/route.ts` (do-not-touch exception, gate branch replaces the old 3-way `Promise.all`), `lib/screenings.ts` (`saveScreening`'s `gate1Only` param, score-override condition changed), new `lib/buildGate1ArchivedResult.ts`, new text-based name fallback in `lib/extractCandidateNameFallback.ts` (`extractNameHeuristic` unit-tested 11/11, `test_extract_name_heuristic.mjs`), leniency wording in `evaluateChecklist.ts`. `tsc --noEmit` clean, all 8 test files pass, do-not-touch diff clean except the flagged exception. **Not live-tested** (no real Anthropic/Supabase access from this sandbox) — needs a Claude Code pass. **`gate1Only` persistence: resolved by inference, not a migration** — new `lib/isGate1OnlyResult.ts` (`gate1Only === true` OR `checklistEvaluation` present with an empty `summary`) covers both the immediate in-memory case and a post-reload read, no new DB column. **Archived-card UI built 2026-08-19 (later still)** — `components/ResultCard.tsx` re-adds a matched/unmatched checklist breakdown (must-have badge, same tier sort as the Filters tab), gated strictly on `isGate1OnlyResult()` so a normal Gate-2 candidate is completely unaffected; the existing career-trajectory/strengths/concerns sections already degrade to nothing on empty data, no separate suppression needed. `tsc` clean, all 8 tests pass, do-not-touch diff clean. **Tier 1 fully built.** Not live-tested. |

**Tier 2 — depends on Tier 1's gate existing:**

| # | Feature | Problem Solved | Depends on | Status |
|---|---------|----------------|------------|--------|
| 2.6.3 | Fingerprint skip for Gate-1 failures | Gate-1 failures skip the paid AI `generateFingerprint()` call (least likely group to be sophisticated fraud) but still get the free `resume_content_hash` dedup check | 2.6.2 | **Already built** — fell directly out of 2.6.2's `score()` restructure (the `gate1Only` branch never calls `generateFingerprint()`); the free hash check already ran unconditionally inside `saveScreening()` regardless. Bookkeeping fix only, 2026-08-19 (even later) — no separate code change needed, this row was just never marked done. |
| 2.6.4 | Lazy fit-suggestion for archived candidates | Gate-1 failures get a "better fit?" suggestion computed on-demand the first time a recruiter opens their card — reuses the live Cross-Project Fit call (`app/api/cross-project-fit/route.ts`) against the recruiter's other ACTIVE projects only, single best match, "no better fit found" if nothing clears the bar. Never computed automatically at archive time for all failures — that would burn the exact cost the gate exists to avoid. | 2.6.2 | **Built 2026-08-20.** New `supabase-migration-gate1-fit-suggestion.sql` (`screenings.gate1_fit_suggestion jsonb`, NOT YET CONFIRMED RUN), new `StoredFitSuggestion` type (`lib/types.ts`), `updateScreening`'s `gate1FitSuggestion` field + `getGate1FitSuggestion`/`getScreeningFitContext` reads (`lib/screenings.ts`, same deferred-column pattern as `checklistEvaluation`). `app/api/cross-project-fit/route.ts` POST rewritten: accepts an alternate `screeningId` input (auth via `canAccessScreening`), checks the persisted suggestion FIRST and returns it unchanged if present (never recompute — Vlad: "once"), otherwise re-reads the resume from storage, runs a new checklist pre-filter per candidate project (same cheap-before-expensive shape as Gate 1 itself — drops a project only if its own checklist score would miss its own threshold, fails open on any read/eval error) before the real `scoreCandidate()` fan-out, then persists via a `respond()` helper used at every exit point (not just the final one — an earlier draft only persisted at the end, which silently broke "never recompute" for a candidate with zero eligible other projects). Acceptance rule replaced: `s.score >= s.threshold + FIT_ACCEPT_MARGIN` (flat 15, both flows) instead of the old `currentScore`-beat comparison, which didn't make sense for a Gate 1 candidate's checklist-percentage "score." `app/projects/[id]/page.tsx`'s live flow no longer sends `currentScore` (stale comment fixed too). UI wiring: `app/candidates/[id]/page.tsx` (the "view an already-saved candidate" page — the real "recruiter opens the card" moment for a reopened Gate 1 archive) now fetches `otherActiveCount`, computes `eligibleForFitCheck`, and wires `onFindBetterFit` through the new `screeningId` path; `onCheckCrossProjectPromise` is a trivial always-`promising:true` stub (this path's real gate is the server-side checklist pre-filter, not a client Claude call) that reuses `ResultCard`'s existing auto-fire-on-mount effect unchanged. `onTransferToProject` deliberately NOT wired there yet — needs a `screeningId` path added to `app/api/screenings/save-one/route.ts` (do-not-touch), flagged as a real follow-up, not silent. **Real bug found and fixed along the way:** `lib/toCandidateResult.ts` never mapped `checklistEvaluation` from `ScreeningRecord` onto `CandidateResult`, even though the DB read already attaches it (`attachChecklistEvaluations`) — meant `isGate1OnlyResult()` always read false and Tier 1's archived-checklist-breakdown card never actually rendered on `/candidates/[id]`, only in the same-session live results view. `tsc --noEmit` clean, all 9 test files pass (no new pure-function test file needed — this tier's new logic lives entirely in route handlers with DB/Claude calls, consistent with this repo's "only standalone lib functions get `test_*.mjs`" convention), do-not-touch diff clean except `screen-resumes/route.ts`'s already-flagged Tier 1 exception. Not live-tested (no real Anthropic/Supabase access from this sandbox). |

**Tier 3 — independent, no dependency, can build anytime:**

| # | Feature | Problem Solved | Depends on | Status |
|---|---------|----------------|------------|--------|
| 2.6.5 | Regenerate-checklist warning | "Regenerate from JD" replaces the whole list after a generic one-line confirm — needs to list which items will actually be dropped | Nothing | **Built 2026-08-19 (even later)** — `app/projects/[id]/page.tsx`'s `FiltersTab` regenerate-confirm banner now lists every current item's label. Bookkeeping fix — was built alongside the Tier 1 UI pass, just filed under the wrong tier heading originally. |
| 2.6.6 | Target-company alias matching | Flat substring match misses a target entered as "Google LLC" failing to match a resume that just says "Google". | Nothing | **Built 2026-08-19 (even later)** — plan corrected mid-build (see the architecture doc): `companiesLooselyMatch` didn't actually fit (shape mismatch, two-company-name comparison vs. a raw resume-text scan). Built `stripLegalSuffix()` in `lib/targetCompanyBoost.ts` instead — strips a trailing legal-entity suffix (LLC/Inc/Corp/etc.) off the TARGET side only (the reverse direction already worked via plain substring matching, zero changes needed there). Unit-tested 14/14, `test_target_company_boost.mjs`. Deliberately still NOT catching true rebrands ("Google" vs "Alphabet") — no alias list built, matches the "loose match only" decision. |

**Tier 4 — trajectory graph redesign (own big lift, no dependency on Tiers 1–3):**

| # | Feature | Problem Solved | Depends on | Status |
|---|---------|----------------|------------|--------|
| 2.6.7 | Per-role step-direction field on `scoreCandidate.ts` | New do-not-touch exception — AI judges each role transition as a step up/down/lateral from title + experience + responsibilities, feeds the graph's new Y-axis | Nothing | **Built 2026-08-20.** `TrajectoryEntry` gained `stepDirection?: "up"\|"down"\|"lateral"\|"first"` + `stepReasoning?: string` (`lib/types.ts`). `scoreCandidate.ts`'s `SCORE_TOOL` trajectoryEntries item schema gained the matching fields (do-not-touch exception, additive, NOT in `required` — zero disruption to the existing contract). Passes through automatically via the existing `toolUse.input as Omit<CandidateResult, ...>` cast, no separate mapping code needed. |
| 2.6.8 | Employment-gap detection (pure code) | Deterministic date math off `trajectoryEntries`' existing `startDate`/`endDate` — zero AI cost, real gaps show as a dip/broken segment. This is the actual answer to "how does the line go down" alongside 2.6.7. | Nothing (existing `trajectoryEntries` field) | **Built 2026-08-20.** New `lib/detectEmploymentGaps.ts` — reuses `parseYearMonth`/`toMonthRange` from `lib/matchTrajectoryEntries.ts` (already exported for reuse), same ~2-month tolerance that file's `datesLooselyMatch` already established. Sorts entries chronologically, flags any consecutive-role gap beyond tolerance. 14/14 unit tests, `test_employment_gaps.mjs`. |
| 2.6.9 | Step-direction judgment added to cross-reference extraction | **Newly discovered requirement, not explicitly asked as its own decision** — the secondary (cross-reference) line was decided to be a full second trajectory on the SAME axis, not a delta track (see decisions-log.md's later 2026-08-19 entry). That only works if the cross-reference's own trajectory gets the same step-direction judgment 2.6.7 gives the resume — so `assessCredibility.ts`'s existing `TRAJECTORY_EXTRACTION_TOOL` needs the same field added. Flagging this explicitly since it wasn't asked as a standalone yes/no — it falls out necessarily from the "full second line, same axis" choice. | 2.6.7 (same field shape) | **Built 2026-08-20, without a separate re-ask.** Built directly under the standing "stick to the architecture plan precisely" instruction — this item is a mechanical, unavoidable consequence of the already-locked "full second line, same axis" decision (2026-08-19), not a new judgment call requiring its own sign-off. `TRAJECTORY_EXTRACTION_TOOL`'s item schema (`lib/assessCredibility.ts`, not do-not-touch) gained the identical field pair, cross-referenced by comment to `scoreCandidate.ts`'s copy for hand-sync going forward. |
| 2.6.10 | `TrajectoryGraph.tsx` rebuild (7th revision) | Y-axis becomes trajectory direction + gaps instead of checklist points. Reuses the 2026-08-18 build's overlap/divergence/amber-ring rendering almost as-is (only the Y-value computation changes) — real overlap when resume and cross-ref agree, genuine divergence exactly where they don't, no artificial offset. Adds hover/click detail per point: role title/dates, which checklist items fired there, reasoning behind that role's direction call. | 2.6.7, 2.6.8, 2.6.9 | **Built 2026-08-20.** New `computeTrajectoryValues()` inside `TrajectoryGraph.tsx`: first role baselines at 0, each subsequent role accumulates +1/0/−1 (up/lateral-or-undefined/down), a real gap (2.6.8) forces the value strictly below the previous role's regardless of stepDirection. Y-axis scale generalized to a signed min/max range (was an assumed 0..maxPoints) — a "down" step can now render below the baseline. Old `rolePoints`/`secondaryRolePoints` (checklist-points-driven Y, superseded) props removed entirely; the plain Gantt-duration-bar fallback rendering (only ever reachable when `rolePoints` was absent) removed too since chart-mode is now unconditional whenever entries exist — real dead-code cleanup, not just an unused branch left in place. New `checklistByRole`/`secondaryChecklistByRole` props (`lib/attributeChecklistToRoles.ts`'s new `attributeChecklistItemsToRoles`, sibling to the existing points-only function which stays untouched/still tested) carry checklist evidence as hover/click-only detail, no longer feeding Y. Hover via native `<title>` tooltips (extended text); click toggles a small detail panel below the chart (role, dates, step-direction + reasoning, gap note, fired checklist items) — the "click" half of "hover/click interactivity," since touch devices have no hover. `components/CredibilitySection.tsx` (the only caller) updated to match. 13/13 new unit tests for the core accumulation logic, `test_trajectory_values.mjs`. |

**Tier 5 — blocked or backlog, not sequenced with the above:**

| # | Feature | Problem Solved | Depends on | Status |
|---|---------|----------------|------------|--------|
| 2.6.11 | Automatic Brillio intake | Resumes arrive and trigger screening automatically instead of manual upload — Brillio's careers run on SmartRecruiters + Lever (both support real webhooks), webhook-first with a later reconciliation poll as safety net, manual Brillio-role-ID-to-project mapping | **Blocked — Vlad doesn't have API/webhook credentials from Brillio yet** | Designed, blocked on external access |
| 2.6.12 | Calibration example pattern analysis | Surface the most common patterns across a project's accumulated calibration examples (frequent skill gaps / rejection reasons), feed back into checklist wording — keep individual items concrete/checkable, leave broader experience judgments general | Nothing structurally, but vaguest-scoped item this round | Backlog, not scoped in detail |

**Explicitly NOT changed by this phase:** the AI's qualitative summary/strengths/concerns/careerTrajectory generation, the mustHave×0.65 + niceHave×0.35 Gate 2 weighting, the calibration trust curve (4/8 thresholds), the credibility score deltas (−8/−25 / +5/+15) — all confirmed fine as-is this session, see decisions-log.md.

---

## PHASE 3 — SCALE & DEFENSE (POST-DEMO, unchanged, still FUTURE)

| # | Feature | Problem Solved | Priority |
|---|---------|---------------|----------|
| 3.1 | Proxy Interview Detection | Compare interview performance against first-week work output signals | FUTURE |
| 3.2 | Reval/ATS Integration | Accept submissions from Reval/Lever → run through Cirot's fraud layer | FUTURE |
| 3.3 | Role-Based Analytics | Director sees team-wide signals; recruiter sees only their own pipeline | FUTURE |
| 3.4 | Candidate Social Graph | LinkedIn mutual connections, activity patterns as fraud signals | FUTURE |

---

## NEVER BUILD
- Autonomous AI interviewing (Reval's play — doesn't solve human-in-the-loop problem)
- Full ATS replacement (Hivemind's play — too broad, not our market)
- Job posting or sourcing (commodity — not defensible)
- Gate 5/6 (identity realness, criminal check) until real access to a paid vendor exists — do not half-build against infrastructure that isn't there

---

## Competitive Position
| Competitor | Their Strength | Their Gap | Cirot Advantage |
|-----------|---------------|-----------|-------------------|
| Reval ($8k/hire) | Autonomous sourcing + AI screening | Zero fraud detection, scores fake resumes | Fraud fingerprinting, credibility verification, human-in-loop trust |
| Hivemind | Full ATS workflow | No fraud layer, no verification | Fraud prevention, calibration learning, attribution |
