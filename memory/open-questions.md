# HireView — Open Questions / Needs Vlad's Input

Things a session should surface to Vlad rather than silently deciding.

- ~~Auto-archive default reason doesn't match any real picker option~~ — **fixed 2026-07-17.** "Role alignment" restored as a real 5th `ARCHIVE_REASONS` entry, `DEFAULT_AUTO_ARCHIVE_REASON` now points at it. On `fix/archive-reason-and-linkedin-badge-consistency`, pushed, not yet merged.
- ~~LinkedIn source-indicator badge is inconsistent across all three places it renders~~ — **fixed 2026-07-17, same branch.** All three (Pipeline tab, `app/candidates/page.tsx`, `components/ResultCard.tsx`) now render the same SVG "in" mark.
- **2026-07-17 handoff: only 5 of ~18 uncommitted follow-ons were in scope; Vlad chose surgical extraction over doing everything or bundling.** Asked directly (AskUserQuestion) rather than guessing, since several files (`app/projects/[id]/page.tsx`, `components/SiteHeader.tsx`, `lib/screenings.ts`) had the 5 requested changes mixed inline with ~13 other unrelated follow-ons with no clean line boundary. Chose: extract only the 5 named branches by hand, leave everything else stashed. All 5 pushed and individually build-verified in true isolation — see state.md's 2026-07-17 section for the full branch list, extraction notes per file, and compare URLs. **Needs Vlad:** review/merge Branch 1 first (highest severity, real cross-team data leak), then Branch 2 (depends on Branch 1's new helper). Branches 3-5 are independent, can merge in any order.
- **The other ~13 follow-ons from that same session are NOT lost — sitting in one git stash on top of `main`, message "2026-07-17-handoff: full working tree, pending 5-branch surgical split."** Includes a whole in-app feedback form (with its own migration, not yet run), fraud/duplicate "ring" clustering UI, a stop button for in-progress screening batches, a smart candidate filter bar upgrade, and several smaller fixes — full list in state.md's 2026-07-17 entry. Will need its own scoping/branching pass, likely several more branches given how entangled some of it is with what just shipped.
- **`tracker.screening_id`'s `ON DELETE CASCADE` rule is unverifiable from any sandbox — needs Vlad to check the Supabase dashboard directly** (Table Editor → tracker → screening_id column → FK delete rule). The `tracker`/`screenings`/`calibration_examples`/`access_requests`/`feedback` tables predate the migration-file convention (created directly in the dashboard), so there's no CREATE TABLE to check in code. If there's no cascade, deleting a screening either leaves a harmless orphaned tracker row or throws a swallowed FK-violation error — worth knowing which before it matters.
- ~~Three-epic feature batch (2026-07-15) — built, verified only via code-read, not yet build-checked or live-tested~~ — **merged, 2026-07-16.** Both `feat/team-project-management` (PR #19) and `feat/pipeline-scoring-design-system-and-latency` (PR #20) confirmed merged into `main`.
- ~~2026-07-16 handoff produced 4 branches, not the 3 described~~ — **all 4 merged same day.** PR #21-#24, confirmed via `git log origin/main`. Still needs live-test: Branch 1 (tracker date) and Branch 3 (LinkedIn — needs a real LinkedIn PDF upload).
- ~~Real, still-open bug found while investigating the 2026-07-16 handoff: `fade-in-up` keyframe should end on `transform: none`, not `translateY(0)`~~ — **fixed 2026-07-17, then found re-regressed by the same stale-base-branch pattern and fixed again the same day.** See state.md's 2026-07-17 recovery entry.
- ~~"Phase 2.3" was named as the next thing to build in today's handoff, with zero scope given~~ — **Vlad confirmed Cowork will pick up Phase 2.3, not Claude Code.** No action needed here.
- **Live bug on `main` right now (found 2026-07-15): saving a Location or archive reason on a candidate silently doesn't survive a reload.** PR #17 (pipeline status restructuring) merged early and stranded two already-pushed commits (`920a658`, `cf28da3`) that wire `location`/`archive_reason` into the shared read paths — confirmed directly against `origin/main`'s `lib/screenings.ts`, which still lacks both fields on read. Not data loss (the write side works fine, it's in the DB) — just never read back, so the UI shows it reverted after refresh. Fix recovered onto `fix/location-archive-reason-read-wiring` and pushed as its own PR (unrelated to the credibility branch below) — needs Vlad's review/merge. Worth prioritizing since it's live and user-visible right now, not a code-complete-but-unmerged item like the rest of this list.
- **Credibility Checker accuracy overhaul + score impact (2026-07-15) — the exact scoring formula's numbers are my judgment call, not something Vlad specified.** He confirmed the *policy* via AskUserQuestion (only material discrepancies count, capped deduction) but not the specific magnitude. I picked -8 points per material discrepancy, capped at -25 total (`computeCredibilityScoreDelta()` in `lib/assessCredibility.ts`) — reasonable-feeling defaults, not derived from any stated requirement. **Confirm with Vlad** whether -8/-25 feels right once he's seen it live-tested against a real material discrepancy (e.g. does a single real red flag docking 8 points feel proportionate, or should one material discrepancy hurt more/less?). Committed to `feat/credibility-accuracy-and-scoring` and real-build-verified (`npm run build` clean, not the sandbox mount) — still needs Vlad's own click-through pass in the app plus this magnitude confirmation before merge.
- ~~Education date-tolerance rule needs re-verification against Vlad's two test cases~~ — **verified, found it didn't hold, fixed for real, 2026-07-15.** Ran the actual `assessCredibility()` function repeatedly (not a read-through) — the PhD/Expected-2029 case matched cleanly every time, but the MS-CS-2024 case was genuinely ~50/50 across 6 runs despite the prompt's own worked example covering it. Root cause was tool-schema field order (the model's `note` field showed correct arithmetic but came after the `status` field it should have justified), not the wording itself — fixed with a new `reasoning` field ordered before `status`. Re-verified 12/12 clean on both cases plus 3/3 correct on a deliberate negative control. See decisions-log.md for the full trail.
- **Teti feedback batch (2026-07-15) — 3 bugs fixed, needs live-test + commit/push/PR.** Bug 1 (filename-collision skip-scoring), Bug 2 (missing candidate name via Claude PDF-vision fallback), Bug 3 (company-alias false positives in credibility check) — see state.md's "Fixed, 2026-07-15" section for full detail and the exact live-test checklist. None of the three has been live-tested yet (Bug 2 only verified via an isolated `tsc --noEmit` compile check + direct code re-read); this session's sandbox mount was not used for `npm run build`/`tsc` against the real tree this pass. Needs Vlad's own machine for real build verification, then the live-test checklist, then Claude Code handoff for branch/commit/PR (do-not-touch note: `parseResume.ts` should be zero diff this round — Bug 2's fix lives in `screenings.ts` + the new `extractCandidateNameFallback.ts`, not in the do-not-touch parser).
- ~~`components/AlreadyScreenedCard.tsx`'s "possible_update" credibility comparison has the same backwards resume/cross-ref direction and data-corruption side effect as the ResultCard bug~~ — **resolved 2026-07-15, by removal rather than by fixing the direction.** Investigating this turned up that the whole `possible_update` classification was dead weight: Bug 1's fix (2026-07-15) already meant `app/projects/[id]/page.tsx`'s upload filter only ever routes `status === "duplicate"` items to `AlreadyScreenedCard` — so its `possible_update` branch (and the backwards/corrupting Compare button inside it) had been unreachable code since that fix shipped, not a live bug. Separately, Vlad questioned whether filename-matching was even a good signal at all, given it flagged unrelated candidates who both happened to have generic download filenames like "Resume (16).pdf" (normalizes to the bare word "resume" — zero identity signal). Decided to retire filename-matching entirely rather than patch it: `possible_update` removed from `CheckExistingResult`, `normalizeFileName()` deleted, `AlreadyScreenedCard` simplified to duplicate-only, and the `ResultCard.tsx` Compare action moved onto the more reliable post-score `nameMatch` signal (real identity comparison, not a filename string) instead. See decisions-log.md and state.md's 2026-07-15 entries for the full reasoning trail. Not yet live-tested.
- ~~Pipeline status restructuring (Interview removed, Screening becomes the TrackerStage container) — spec captured, not built~~ — **built same day, 2026-07-15, per Vlad's "let's keep going with that then."** See state.md's "Shipped (code-complete, NOT yet live-tested)" section for the full build (CandidateStatus enum change, merged StatusStageControl, drawer changes, auto-archive, two migrations). Confirmed via `npx tsc --noEmit` clean, but genuinely NOT live-tested — needs Vlad to run both migrations (in order) and work through the 8-item checklist in state.md before this is considered done. Vlad also corrected a misunderstanding about this build the same day: candidates already sitting in a Tracker stage keep their exact stage after the migration (the migration never touches the `tracker` table) — this was already true by construction, just clarified in state.md's checklist wording after he flagged it.
- **Archive reason (2026-07-15) — two design decisions made without Vlad's confirmation because `AskUserQuestion` errored out twice.** (1) The reason picker was placed inline on the `StatusStageControl` status pill rather than in a separate drawer/detail view. (2) Auto-archived (below-threshold) candidates start with no reason selected rather than getting an automatic "Below threshold" 5th option. Both were the "Recommended" options in the question that failed to send — see decisions-log.md for the full reasoning. **Confirm with Vlad next session** whether these match what he actually wanted; if not, (1) is a StatusStageControl.tsx change and (2) is a one-line change in `lib/screenings.ts`'s auto-archive branch.
- ~~Save-all + rejection-history feature needs commit/PR + live-test~~ — **fully closed, 2026-07-10.** Commit `967d159` on `feat/save-all-and-rejection-history`, merged to main. Build clean, do-not-touch diff clean. Vlad confirmed the full live-test checklist (fit-suggestion banner above Career Trajectory with no "Save anyway," the rose "Previously rejected" banner, reason-field persistence across a reload, the Excel export's new column) — all working.

- ~~Phase numbering is inconsistent across documents~~ — **partly resolved 2026-07-09.** The main source of ambiguity, outreach drafting being called "Phase 3" in some places and "Phase 5" in others, went away when outreach drafting itself was dropped from the roadmap (see decisions-log). Still worth noting: the dev log separately uses "Phase 2/3" for the architecture redesign (problem statement + locked design phases, a different scheme entirely from roadmap phases) — low priority since it hasn't caused real confusion.
- **Impact metrics still not measured.** The BLUEPRINT and Impact Report reference a "60% throughput improvement" as validated, but the dev log's own "what's next" lists "measure screening time per role and TA false positive rate" as still outstanding as of the last entry. Worth checking whether the Impact Report's number predates real measurement or if measurement happened outside the dev log.
- **`supabase-migration-interview.sql` and `supabase-migration-projects.sql` show as an uncommitted local deletion in the working tree on `main`** (as of 2026-07-08). Vlad confirmed both are intentional/already-run — low priority, but the deletion has never actually been staged/committed, so `git status` will keep flagging it until someone runs `git rm` + commit, or restores the files.
- ~~Migration SQL files could be grouped into `supabase/migrations/`~~ — **done 2026-07-09, committed.** All 11 `supabase-migration-*.sql` files moved to `supabase/migrations/`, no renaming. The four root-level `.docx` narrative docs and `HireView_Prior_Art_Document.pdf` (previously in `memory/`) all moved to `docs/`. The two stale `supabase-migration-interview.sql`/`supabase-migration-projects.sql` files were restored via `git show main:...` into `supabase/migrations/` rather than left deleted. All of this landed in commit `b22b2ab` on `phase-1-3-teams-architecture` (committed by Claude Code, confirmed via `git status` — this session's own working-tree `mv` never got committed directly, Claude Code redid/confirmed it cleanly).
- **Teams migration (`supabase-migration-teams.sql`, Phase 1.3) puts every existing user into one "General" team before Vlad has a chance to split them.** Since team-based visibility is shared (not per-user like today), running the migration as-is means Teti immediately sees all of Vlad's existing projects/candidates and vice versa, the moment it's run — not a bug, just a consequence of the backfill needing *some* default. Before running it, decide: stay in one shared team (fine if that's actually the intent), or create a second team and move one recruiter into it right after migrating, before real use resumes.
- **Phase 1.4's migration is now run** (confirmed 2026-07-09, after it caused an app-wide candidate-visibility outage — see decisions-log). Still needs live-testing for the team-isolation boundary specifically: same resume/swapped-identity into two projects in the same team should show "Previously seen"/"Known fraud pattern"; into a project in a *different* team should show nothing. The second case matters more — a leak across the 1.3 team boundary would be a real privacy issue, not just a missing feature.
- **Confirm the `team_id` backfill queries were run.** Two straggler-cleanup queries were given to Vlad during the 2026-07-09 incident: one for `screenings.team_id` (given before the outage was diagnosed) and one for `resume_fingerprints.team_id` (only runnable after the 1.4 migration). Unclear if the first one actually got run before attention moved to the bigger outage — worth Vlad re-running both now that the migration is in, to make sure no screening/fingerprint rows are still sitting on `team_id = NULL` from the `getProject()` bug window.
- ~~No PR was ever actually created for `phase-1-3-teams-architecture`~~ — **wrong, corrected 2026-07-09.** Vlad was in fact creating and merging a separate PR after each Claude Code push (via the compare-URL fallback each session gave, since `gh` was never on PATH in that environment) — PR #4 (1.3), #5 (1.4 + team chips), #6 (1.5), on top of #1–#3 for earlier features. He just didn't narrate each merge back into this conversation, so from this side it looked like nothing had happened, leading to a redundant "create one combined PR" instruction on already-merged work. Confirmed via GitHub's PR list: 0 open, 6 closed, all merged, main fully caught up through Phase 1. **Lesson:** when a PR's fate is unclear across a gap in the conversation, ask the user to check GitHub's PR list directly (open AND closed) before drafting more instructions — don't assume silence means nothing happened.
- ~~`HireView_Prior_Art_Document.pdf` corrupted on disk~~ — **fixed 2026-07-09.** The disk copy was truncated (no `startxref`/`%%EOF`, broken trailer) but an intact version existed in git at commit `3d78ea5` (dated July 7, 2026) — restored via `git show 3d78ea5:memory/HireView_Prior_Art_Document.pdf`, verified clean with `qpdf --check`.
- ~~Missing "Novel Implementation C" entry~~ — **added 2026-07-09.** No generator script existed in the repo, so the document was rebuilt from scratch with `reportlab` (Platypus), reproducing Sections 1–2 and Implementations A/B verbatim from the restored text, adding IMPLEMENTATION C (Identity-Blind Content Fingerprinting for Cross-Submission Fraud Detection — conception July 2026, reduction to practice July 8, 2026, extended July 9, 2026 for cross-project matching), two new Evidence Timeline rows, and updated Section 6 to cover a provisional patent application for C alongside A. Document date field, declaration paragraph, and signature date all updated to reflect the July 9, 2026 revision while preserving the original July 7, 2026 date for context. Verified with `qpdf --check` and by rendering all 7 pages to images and visually reviewing them. Assessment for 1.3–1.5 stands: only 1.1/C needed a new entry — 1.3 (Teams) is standard multi-tenancy, 1.4 directly reuses 1.1's fingerprinting technique, 1.5 is prompt composition over existing signals.
- ~~FunnelView is merged to main but still needs live-testing~~ — **confirmed 2026-07-09.** Validated against 78 real candidate screenings; funnel numbers confirmed accurate. Non-admin 403 and mobile layout weren't separately re-confirmed in this pass — low priority given the page gate hasn't changed since it was built.
- ~~Phase 2.1 (Cross-Project Fit Suggestion) needs a fresh live-test round before commit/push/PR~~ — **resolved 2026-07-10.** The auto-fire gate was rebuilt four times (must-have score → overall score → keyword overlap → cheap Claude classification call) before one worked — full trail in decisions-log.md/state.md. A real `useEffect` bug (self-cancelling dependency loop) was also found and fixed along the way. Vlad confirmed the final version working on the exact previously-failing candidate. Committed as `66bf129`, pushed to `feat/cross-project-fit-suggestion`, **PR #10 merged to main 2026-07-10** (merge commit `a7f8a26`, confirmed via `git log origin/main` and `git merge-base --is-ancestor 66bf129 origin/main`). **Still open:** a pass on the manual link + Transfer button specifically (not yet separately confirmed, lower priority than the gate itself).
- **Correction, 2026-07-10:** an earlier check this session read the local `main` branch ref (stale, never pulled) and concluded 66bf129 was *not* merged — wrong. The GitHub compare page showing empty was the actual signal it *was* already merged (no diff between an already-merged branch and main). Lesson: when checking merge status in this sandbox, compare against `origin/main` (after `git fetch`), not local `main` — the local mount's git state has been unreliable all session (see the two entries below) and can silently be behind.
- **FunnelView dropdown / admin team-deletion recovery attempt (2026-07-10) landed a PR, but not the actual code.** Vlad ran the handoff commands on his own machine and pushed `feat/funnelview-dropdown-and-team-delete`; PR #12 merged to main (`206c9b1`) — but checking the PR's Files Changed on GitHub showed only 3 files under `memory/` (+33/-7), i.e. this session's own doc edits that happened to be sitting dirty in the working tree. None of the 6 target files (`app/funnelview/page.tsx`, `lib/funnelview/data.ts`, `lib/funnelview/types.ts`, `lib/teams.ts`, `app/api/admin/teams/[id]/route.ts`, `app/admin/users/page.tsx`) are in the diff. Likely cause: `git stash pop` didn't apply cleanly (or applied to the wrong branch) and `git add -A && git commit` silently committed only what was actually present. **Status unknown, low priority — not blocking anything live.** Vlad was asked to run `git stash list` to check whether the original stash entry still exists (safe, nothing lost) or is now empty (6 files of work were dropped and would need rebuilding); as of 2026-07-10 he deprioritized checking this ("let's keep moving") since neither feature is urgent. Revisit `git stash list` next session before assuming the work is gone.
- **This session's sandbox couldn't reliably run `git` or `tsc` against the mounted `C:\Portfolio\HireView` repo** — `git rev-parse HEAD` failed with the commit object missing from `.git/objects` even though `refs/heads/main` points to a real-looking SHA, and `tsc` read several files (including untouched ones like `app/layout.tsx`) as truncated mid-line, throwing cascading fake syntax errors. The Read/Edit file tools showed the same files as complete and correct, so this looks like a mount-layer artifact specific to the shell, not real repo corruption — but it means Phase 1.3's changes were never committed, branched, or PR'd from this session. Vlad needs to review the diff and handle branch/commit/PR from his own machine.
- **Confirmed 2026-07-10, extends to `.git/config` itself.** `git status`/`git branch -f` failed with "bad config line 1 in file .git/config" plus an unremovable `.git/config.lock` (permission denied), while the Read tool showed `.git/config` as complete and well-formed. Read-only git queries (`fetch`, `log`, `branch -a`, `merge-base --is-ancestor` against `origin/*` refs) worked fine and gave trustworthy results — it's specifically write/mutating operations and the local `main`/working-tree state that can't be trusted this session. This is why the stash recovery (item above) was handed off to Vlad's machine instead of attempted here.
- **Also observed 2026-07-10: an Edit tool call that reported success against `open-questions.md` did not actually persist** — a follow-up `Read` in the same session showed the pre-edit text still on disk. Re-applied the edit a second time and verified it landed before moving on. Worth a quick sanity-check (re-read after edit) any time a memory-file edit in this project matters and the mount has been acting up — don't assume a success message means the write landed.
- **`app/api/screenings/regenerate-trajectories/route.ts` has no caller anywhere in the app** (confirmed via repo-wide search, 2026-07-10). It re-runs Claude's trajectory generation for every screening in scope, unconditionally — no check for whether a trajectory already exists — so if it's ever invoked (manually, or from a debug tool not in the repo), it burns a full Claude call per candidate even for ones that don't need it. Left in place rather than removed unilaterally since it's unclear whether Vlad still uses it manually (curl, admin script) — worth a quick check: if it's dead, delete it; if it's a real manual tool, either wire it to a real UI control or add a "skip if trajectory already exists" guard.
- ~~Pre-Screen Duplicate Check needs build-check + live-test~~ — **committed and merged, 2026-07-10.** Migration run, build clean, three live-test rounds done (exact-duplicate skip, post-score name cross-check, persisted "Name match" badge all confirmed working). Claude Code committed `55bfb8e` (14 files) on a fresh `feat/pre-screen-duplicate-check` branch off current `main` (had to recover from being on a stale branch first — stashed, resynced, popped clean) and merged via PR. Do-not-touch files and full diff both reconfirmed clean. **Still open, low priority:** the delete-refresh fix, `possible_update` credibility-comparison path, and genuinely-new-resume path weren't separately re-confirmed after this final merge — worth a quick pass next time the app is used, not blocking.
- **Repeat incident, 2026-07-10: added `name_match_id` to `SCREENING_COLUMNS` (the shared select used by `listScreenings`/`getScreeningsByIds`) before the migration was confirmed run, causing candidates to disappear entirely (Pipeline + All Candidates both empty) — this is the exact same failure mode as the 2026-07-09 outage already logged under [[feedback_migration_sequencing]] in the global memory vault.** Vlad reported "can't see candidates at all"; root cause diagnosed immediately from the pattern match, gave him the migration SQL to run as the fix (`ALTER TABLE screenings ADD COLUMN IF NOT EXISTS name_match_id ...`). **This is a repeated mistake, not a new class of bug** — the standing rule was already documented and should have been checked before adding the column to a live shared select. Going forward: never add a new column to `SCREENING_COLUMNS` (or any other shared select list) in the same step as writing its migration — land the migration, confirm it ran, only then wire the column into a shared query path.
- **Do-not-touch exception: `lib/parseResume.ts` was directly modified 2026-07-10** (new `.doc` branch via `word-extractor`, see state.md). `parseResume.ts` is on 00-index.md's explicit do-not-touch list ("validated core, build around them never through them"). This wasn't caught in the moment — Vlad's own explicit ask ("add real .doc parsing support," chosen directly over two other options), same category of deliberate exception as the `screen-resumes/route.ts` score-gate removal, but unlike that change it was never flagged as an exception at the time. Flagging now so it doesn't read as an accidental do-not-touch violation in the next diff review. Worth Vlad confirming after his own `tsc`/build pass that the pdf/docx/txt branches are still byte-for-byte unchanged (only the new `doc` branch + `WordExtractor` import were added).
- **Twin bug found, not fixed: `lib/calibrationExamples.ts` has the identical unsanitized-filename storage-key bug** that was just fixed in `lib/screenings.ts` (`resumePath = \`calibration/${randomUUID()}/${fileName}\`` — same "Invalid key" failure mode for any filename with `~`, accents, `%`, etc.). `calibrationExamples.ts` is also on the do-not-touch list, so this was deliberately left unfixed rather than silently edited — needs Vlad's explicit go-ahead (or Claude Code) to apply the same `sanitizeStorageFileName`-style fix there. Low urgency until someone actually hits it via the calibration flow specifically (thumbs-down correction upload).
