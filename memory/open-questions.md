# Cirot — Open Questions / Needs Vlad's Input

Things a session should surface to Vlad rather than silently deciding.

- **2026-08-17 — Credibility trajectory overlay (roadmap 2.5.2), live-verified by Claude Code — two known, non-blocking characteristics worth being aware of, not action items.** (1) The `trajectoryEntries` extraction occasionally splits one compound-title role into two entries (e.g. "Sr. Director, Engagement & Delivery Leader / Director PMO" → two) — the pairing logic already degrades gracefully (the unmatched duplicate is silently dropped, never a false flag), so this is a candidate for a future prompt-tuning pass on the schema description, not a bug to fix now. (2) Real model non-determinism observed on a borderline judgment call: the same 4-month employment-date gap was graded "minor" on one credibility-check run and "material" on a repeat run of the identical input. This is inherent to an AI judgment step, not something the deterministic diff or the code around it can eliminate — if a recruiter ever re-runs the same check twice and gets a different severity, this is why, not a regression.
- **2026-08-17 — JD checklist built for real (Cowork), not just planned — one item needs your input, one earlier plan note was wrong and is corrected here.** (1) **Needs your call:** the Filters tab's "Regenerate from JD" button replaces every checklist item, including manual edits, after a one-line confirm — is that the right level of friction, or should it warn more specifically (e.g. list which items will be lost)? (2) **Correction to the 2026-08-14 roadmap entry's item 3:** that entry assumed checklist scoring would need a `scoreCandidate.ts` do-not-touch exception. It doesn't — built instead as a fully separate parallel call (`lib/evaluateChecklist.ts`) with the score delta applied deterministically in `saveScreening()`, exact same pattern as the existing target-company boost. Zero new `scoreCandidate.ts` exceptions. (3) **Real logic-audit finding, not obvious from the plan:** `target_company_matches`/`current_company`/etc. are deliberately NOT in `SCREENING_COLUMNS` (the shared select behind `listScreenings`/`getScreeningsByIds`) — they only ever show on the immediate post-screening card, never survive a reload, by design (see `lib/funnelview/data.ts`'s `fetchCurrentRoleColumn` incident comment for why). `checklist_evaluation` needed to behave differently — a recruiter has to see which items fired every time they revisit a candidate, not just once — so it got its own isolated, scoped, best-effort query (`attachChecklistEvaluations` in `lib/screenings.ts`) merged in after the fact, rather than either following the existing pattern (would've made it invisible on reload) or being added directly to `SCREENING_COLUMNS` (would've broken the whole query pre-migration — confirmed against `lib/funnelview/data.ts`'s documented 2026-08-06 incident before writing this). Full detail: `memory/session-log.md`'s 2026-08-17 entry, `memory/claude-code-handoff-2026-08-17.md`, `docs/Cirot-Supabase-Migrations-To-Run-2026-08-17.md`.

- **2026-08-19 — resolved into a full sequenced plan, see `decisions-log.md`'s two 2026-08-19 entries and `Cirot_Roadmap.md`'s new Phase 2.6.** One real open item remains from that plan: Phase 2.6.9 (adding step-direction judgment to the cross-reference trajectory extraction, a scope gap discovered while finalizing the graph design, not yet explicitly confirmed) — needs Vlad's sign-off before building. Everything else in the original entry below is now decided, not just designed.
- **2026-08-19 — Automatic resume intake from Brillio's ATS designed (Cowork planning), NOT built — blocked on API/webhook credentials Vlad doesn't have yet.** Brillio's careers run on two ATS platforms (confirmed via web search): SmartRecruiters (`careers.smartrecruiters.com/BrillioInc`) and Lever (`jobs.lever.co/brillio-2`) — both support real webhook/API access for partners, so this is a real integration once credentials exist, not a scrape. Also decided the same session, two-gate screening architecture: the checklist becomes a hard first gate before the expensive deep call runs — every incoming application gets `evaluateChecklist()` first (cheap, doesn't touch score), and only candidates clearing the project's score threshold on the checklist proceed to the full `scoreCandidate()`/fingerprint/embedding pipeline (gate 2, the real score). Gate-1-failed candidates get archived immediately, zero deep-scoring cost. Real conflict found and resolved: this breaks the existing "suggested better fit" feature (`lib/generateRoleFit.ts`), which needs `summary`/`strengths`/`career_trajectory` — fields only `scoreCandidate()` produces. Fix: don't compute the fit suggestion automatically at archive time for gate-1-failed candidates; trigger it lazily on first open instead, reusing the exact mechanism the live Cross-Project Fit feature (`app/api/cross-project-fit/route.ts`) already uses — a fresh `scoreCandidate()` call per other active project straight off the stored resume text, no dependency on this project's own score. Same precision as that existing live feature, just deferred to view-time instead of computed for all archived candidates up front. API key sizing: Anthropic rate limits are per-key/tier, not per-recruiter — one key (or small pool) per team is enough once real Brillio volume is known; this is a throughput question, not "3-6 keys because 3-6 recruiters used to click the button." **NOT yet designed, needs a follow-up session once Vlad confirms what SmartRecruiters/Lever access is actually available:** the real webhook endpoint(s) and payload mapping, job-req-to-Cirot-project routing, and queueing/throttling for a continuous automatic inflow — `screen-resumes/route.ts`'s `CONCURRENCY = 3` pattern was built for one recruiter's manual batch upload, not an unattended pipeline, and Vercel function duration limits (already raised once, 60s → 300s, see the 2026-07-29 entry below) will need a real answer for a queue-drain approach, not just a bigger number.

- **2026-08-15 — Execution order + 3-way verification split for the whole Phase 2.5 roadmap (`Cirot_Roadmap.md`), decided this session.** Order: quick wins first (Signals export, Tracker Reject collapse, loading animation — all zero-dependency), then the checklist (critical path), then trajectory overlay + GitHub corroboration (same file, do together), archive-fit stage 2 can run in parallel with those two (only needs the checklist), consolidated card comes after the overlay/GitHub work so its tab content is final before the UI merge, talent search is the biggest standalone lift, do last or as its own track. **Verification split, 3 tiers:** (1) Cowork (this session) can run `tsc --noEmit`, diff do-not-touch files against `origin/main`, and write plain Node scripts (no new test framework, no dependency added) to test any pure/deterministic function — trajectory diff, checklist keyword matching, cosine similarity. Confirmed the sandbox has working node/npm/tsc and a clean baseline (`tsc --noEmit` passes today with zero errors) — but confirmed NO network access to external APIs from this sandbox (tried `api.github.com`, blocked by the allowlist), so nothing requiring a live call can be tested here. (2) **Claude Code** should handle anything needing a real key or real data — actually running the checklist generator against a real JD, actually calling GitHub's API, a real Playwright pass on the consolidated card/Tracker collapse (this project has used Playwright for real click-through testing before, see the 2026-07-29 cross-project/cross-team fix verification), hitting the real Supabase instance to confirm a migration applies cleanly. (3) **Vlad specifically:** final quality judgment on AI-generated output, and running any migration against production data himself, per his own already-established preference (see the 2026-07-27 prod-data-reassignment entry — he chose to review/run that himself rather than hand it to Claude Code's service-role key).

- **2026-08-15 — Batch-path audit for the whole roadmap above: only the embeddings call touches `screen-resumes/route.ts`'s CONCURRENCY=3/maxDuration=300 path, everything else is one-off/recruiter-triggered.** Checked against the real code (`Promise.all([scoreCandidate(...), generateFingerprint(...)])`, 3 at a time): the checklist doesn't add a call (enriches the existing `scoreCandidate()` prompt, not a second call), cross-ref trajectory comparison/archive-fit matching/GitHub corroboration are all outside this route entirely, recruiter-triggered on demand, never run during a batch upload. Only the talent-search embedding call needs to run at screening time (to have something to search against later), as a third parallel call alongside the existing two. Should be fine, embeddings are typically faster than the other two, but this is an expectation, not a measurement — **this exact route already had its timeout raised once (60s to 300s) because per-resume work quietly grew past the ceiling** (see the do-not-touch comment at the top of `screen-resumes/route.ts`, 2026-07-29). Time a real embedding call once built, don't assume it's free.
- **2026-08-15 — Gate 4 (GitHub/skills corroboration) decided: fold into the existing credibility/cross-reference check, not a separate feature.** One plain HTTP fetch to GitHub's public API (free, no new paid dependency) for a candidate's claimed GitHub URL, fed into the same `assessCredibility()` call the recruiter already triggers via the existing cross-reference check UI, alongside whatever document they uploaded. Same trigger, same AI call, richer evidence, one set of findings instead of a second feature to build/maintain. Explicitly not Gate 5 (identity realness, needs a paid identity-resolution API like Pipl) or Gate 6 (criminal check, needs a paid background-check vendor) — those still require access Vlad doesn't have and are deliberately excluded from this round.
- **2026-08-15 — Tracker view: Reject column should collapse, correcting an earlier note that pointed at the wrong existing pattern.** `TRACKER_STAGES` (`lib/types.ts`) = TA, L1, L2, In-Person, Offer, Reject — all six render as individual cards in `TrackerTab` (`app/projects/[id]/page.tsx`), so Reject grows unbounded as candidates get rejected. **Correction (2026-08-15, code-accuracy pass):** the "Multiple roles" collapse elsewhere in this app is NOT a generic count-and-expand pattern — it's driven by `computeMatchClusters()`, which groups screenings that are the same real person applying to multiple roles (identity matching). That doesn't apply here; rejected candidates aren't duplicates of each other. What Reject actually needs is simpler: a plain count + click-to-expand toggle, an array slice and a boolean, no clustering computation involved.
- **2026-08-15 (code-accuracy pass) — `assessCredibility.ts`'s existing `rows` comparison carries real tuning the trajectory-overlay redesign (item 2 above) must explicitly carry forward, not silently drop.** Read the full `CREDIBILITY_TOOL` schema: staffing-agency company-name tolerance, LinkedIn date-rounding tolerance (~2 months), exact integer-year-math rules for education dates, and a material-vs-minor severity classification — several of these exist because of real bugs found and fixed live (see decisions-log.md/session-log.md's 2026-07-15 education date-tolerance entry). Replacing `rows` with a trajectory-level comparison needs to port this tuning into the new comparison, not start over with something less battle-tested. One piece of good news: `assessCredibility.ts` is NOT on the do-not-touch list (`memory/00-index.md`), so redesigning it carries no special governance friction — only the reuse of `scoreCandidate()` (do-not-touch, reused as-is/unedited) to build the cross-reference document's own trajectory needs a note, and only because it's a new call site, not because it requires editing that file.
- **2026-08-15 (code-accuracy pass) — checklist (item 3) should be its own new file, not an edit to `analyzeJD.ts`.** Confirmed `analyzeJD.ts`'s `ANALYZE_TOOL` schema only outputs flat `mustHaveSkills`/`niceToHaveSkills` string arrays today, no weights or point structure — the checklist is genuinely new schema, not an extension of existing output. `analyzeJD.ts` is on the do-not-touch list; `lib/generateRoleFit.ts` already shows the right pattern for this (a separate file that takes existing generated output as input rather than editing the source that produced it). Keeps the do-not-touch footprint to just `scoreCandidate.ts`, where checklist results actually get consumed during scoring, instead of two files.
- **2026-08-15 (code-accuracy pass) — confirmed clean, no changes needed:** `ResultCard.tsx` has no existing tab mechanism (`InsightList` renders Strengths/Concerns directly, no `activeTab` state anywhere in the file) — the item-2 tab group is genuinely new UI work, consistent with what's planned, not a smaller tweak than expected. No `vector`/`embedding`-named migration exists yet in `supabase/migrations/`, so the talent-search column has a clean, conflict-free path in.

- **2026-08-15 — Global talent search designed (Cowork), not built. Requires new embeddings infrastructure — none exists in this codebase today.** Full narrative and reasoning trail in session-log.md's 2026-08-15 (later) entry. Build-ready spec: (1) new embedding call via Voyage AI (or similar — Claude doesn't generate embeddings itself), run as a third parallel call alongside `scoreCandidate()`/`generateFingerprint()` at screening time, input is concatenated `summary`+`strengths`+`concerns`+`careerTrajectory`. (2) New `pgvector` column on `screenings`, plus a backfill for the current 397 candidates. (3) Search: embed the query once, vector-similarity compare against stored candidate embeddings, fast/cheap at any pool size, no per-search LLM call. (4) No LLM "why this matched" explanation (Vlad's explicit call) — instead a deterministic pass highlights literal query-term overlaps in the result text; can legitimately show zero highlights on a real semantic match, that's expected, not a bug. (5) UI: `app/candidates/page.tsx`'s existing filter pills collapse into one "Filters" button/panel; talent search is its own separate, visible, explicit-submit control next to it, and stacks with whatever filters are already active rather than always searching globally. **Do NOT confuse with `lib/generateFingerprint.ts`'s `responsibility_vectors` field** — despite the name, that's plain text compared via word-overlap (Jaccard) for fraud detection, not a real embedding, nothing there is reusable for this.

- **2026-08-15 — Shared loading animation designed and confirmed (Cowork prototype), not built into the app yet.** One component instead of ad-hoc/missing spinners across screening, cross-reference checks, archive-fit checking, and suggested-role-fit during new role creation (the last one currently has no loading indicator at all). Spec: a generative wandering line (random walk, quadratic-bezier smoothed) rendered as an SVG, animated as an infinitely scrolling marquee (two identical copies of the same path side by side, whole thing translates left, resets — invisibly, since both halves match). Cirot blue (`#2563eb`), not a generic token. Confirmed working at 3 sizes with the same component: inline (next to a button, e.g. "Checking for fits…"), card (mid-screening state), full panel (e.g. "Running credibility check…" — surface-1 background, bordered, rounded 16px). Two real bugs found and fixed during prototyping, both required for the loop to actually look continuous rather than restarting: (1) the generated path's random endpoint didn't match its fixed starting value — fixed by drifting the whole path back toward the start value proportionally across its length as it's generated, so the last point lands exactly on the first. (2) the SVG's `viewBox` only spanned one segment's width while two segments (for the seamless scroll) were drawn inside it — fixed by doubling the viewBox to match the full two-segment content width. Full narrative in session-log.md's 2026-08-15 entry. **Needs:** a real React/SVG component built from this spec and wired into the existing loading states.

- **2026-08-14 — Candidate Insight roadmap: full scope decided across 5 pieces (Cowork planning session), none built yet.** Reviewed against real code + pilot scale (54 recruiters / ~18,900 candidates/month projected vs. 397/2 actual today) — everything below is recruiter-triggered/one-off, not batch, so none of it touches the `CONCURRENCY = 3` screening ceiling. Needs its own real implementation pass, likely several sessions given the size. Build-ready spec:
  1. **Consolidated card** — one shared card component replacing the 3 separate implementations (`ResultCard.tsx`, `PipelineTab`'s inline markup in `app/projects/[id]/page.tsx`, `app/candidates/page.tsx`'s inline markup). Content order: AI summary, trajectory graph, one tab group (Strengths/Concerns + Matches/Flags), activity history, notes field, existing buttons unchanged. A visible field/prompt suggesting a cross-reference check — deliberately not hidden inside a tab, Vlad wants it surfaced, not an extra click to discover.
  2. **Trajectory graph + cross-reference overlay.** Base graph is the resume's existing per-role trajectory (already generated live by `scoreCandidate.ts`, do-not-touch). When a recruiter runs a credibility check, the cross-reference document runs through the same extraction, producing its own trajectory, overlaid on the same graph. A code-level diff (not AI) finds every point of disagreement between the two. Only the mismatches, not the whole document, go to one small scoped AI call that judges each as a real discrepancy or a plausible explanation and writes a short plain-language note per notable shift — this replaces the old field-by-field `rows` list entirely, one narrative instead of two views of the same data. Matches reinforce; AI-judged real mismatches feed the existing capped score delta in `assessCredibility.ts` — same mechanism, now driven by the trajectory comparison instead of field rows. Cost: roughly doubles the AI spend of a single credibility check (one new extraction call + one small scoped reasoning call over just the mismatches) — real, but not a scale risk since checks are recruiter-paced, not batched. **Depends on item 3 (checklist) landing first** — both documents need to be scored against the same checklist for the comparison to be meaningful.
  3. **JD checklist ("Trust badge").** Enriches the existing must-have/nice-to-have checklist — explicitly not a parallel scoring system. Generated once per JD analysis/re-analysis, plus target companies/keywords pulled from the Filters page. Lives on the project's Filters page, unfoldable, two tabs: "Decrease score" and "Add score." Individual structured editable fields per check (short label + point value), not one freeform text block — matches the existing structured-output pattern in this codebase and keeps each check independently reasoned-about, same "case file not scorecard" principle already used elsewhere. Already-screened candidates are NOT auto-rescored when the checklist changes — manual rescreen only.
  4. **FunnelView export — rename to "Signals."** Current "Total experience" export column (sourced from `totalExperienceSummary`) becomes "Signals," combining the strongest strength and weakest concern into one cell. Confirmed via grep: `totalExperienceSummary` has no other live consumer anywhere in the app (not rendered on `ResultCard`, Pipeline, or `/candidates` — this export was its only remaining use) — comment at `app/funnelview/page.tsx:371` already flagged it as "the only remaining place it renders." Both source fields already exist on every screening: `concerns[0]` is already reliably the biggest concern (schema says "must-haves first"), but `strengths[0]` has no defined order today — needs one line added to the `strengths` field description in `scoreCandidate.ts` (small do-not-touch exception) so it reliably leads with the strongest. Zero new AI calls. Rename applies only to the export column and its underlying field name — `totalExperienceSummary` itself stays as-is in `scoreCandidate.ts`, it's accurately named for what it actually does, just no longer used in this one column.
  5. **Archive/Cross-Project Fit matching — picked back up same session, now fully scoped as a two-stage design.** Root cause confirmed by reading the actual code: `archive-fits/check/route.ts` today matches archived candidates against a new JD using only `suggested_role_fits`, a single generic 2-5 word title generated once at archive time by `lib/generateRoleFit.ts` (independent of any specific JD) — one AI call guesses a title, a second AI call guesses whether that title fits the new JD, no real skill evidence involved either time. Decided fix, two stages:
     - **Stage 1 (unchanged, already exists):** the current cheap title-only classification call across the whole archived pool, in `archive-fits/check/route.ts`.
     - **Stage 2 (new):** for just the candidates that pass stage 1, compare their existing `strengths[]`/`concerns[]` (already generated on every screening today, no new AI call to produce them) against the new project's checklist (item 3 above), once that checklist exists. Deterministic keyword/substring match, not AI — both sides are short structured phrases by then, not raw JD prose. Loose/partial matching, not exact string equality, since candidate phrasing ("FHIR/HL7: implemented at Abridge") won't always match checklist wording ("FHIR interoperability experience") verbatim — same failure shape as the old Cross-Project Fit bug, just on a much smaller, more controlled surface now. Optional safety net: one small AI fallback call only if the deterministic pass finds zero matches for a candidate that already cleared stage 1.
     - Net effect: near-zero added AI cost once the checklist ships (stage 1 same as today, stage 2 mostly free), and real skill evidence drives the match instead of a title guessing a title. **Depends on item 3 (checklist) shipping first** — until then, stage 2 has nothing structured to compare against.
     - Ruled out along the way: piggybacking on the existing `fingerprint` field (dedup-only, mixing concerns) and a new normalized table (row-per-check multiplies with recruiter/candidate volume — real complexity risk at 54-recruiter scale).

  No code touched this session (Cowork, plan-only per Vlad's explicit ask). Full narrative trail in session-log.md's matching 2026-08-14 entry.

- **2026-08-11 — `app/candidates/page.tsx` (All Candidates) has the identical "filters aren't in the URL" problem just fixed on the project detail page, not addressed this round.** Same shape, bigger: `query`, `statusFilter` (a `Set`), `projectFilter`, `flaggedOnly`, `fraudOnly`, `blacklistOnly`, `scoreMin`/`scoreMax`, `dateFrom`/`dateTo` are all local `useState`, none synced to the URL — so browser Back after following a link out of this page (e.g. into a candidate's detail page) won't restore this view either, for the same root-cause reason. Scoped out of this round since the reported bug was specifically the project page's "Also screened in" link; the same replaceState-based pattern (see `app/projects/[id]/page.tsx`'s `PipelineTab`) would carry over directly if Vlad wants this page covered too.

- **2026-08-11 — HireView → Cirot rename: 3 things deliberately left untouched, worth Vlad's attention.** (1) The 3 narrative `.docx` files (`docs/Cirot-Dev-Log.docx`, `Cirot-Case-Study.docx`, `Cirot-Impact-Report.docx`) were only renamed on disk — their actual Word content still says "HireView" throughout (dozens of mentions each); updating that is a real editing pass with the docx skill, not a text substitution, and wasn't in the approved scope this round. (2) The email notification fallback link (`app/api/access-requests/route.ts`, `app/api/feedback/route.ts`) still points to `https://hire-view.vercel.app` — that's the actual live Vercel deployment URL, not a display string, so renaming the text would have broken the link; it stays until/unless Vlad renames the Vercel project itself (a dashboard action, not code) and sets `NEXT_PUBLIC_APP_URL`. (3) `components/Logo.tsx`'s mark was designed around a "Hire = climb / Higher View = lens" pun tied to the old name — visual mark left as-is, only the label strings changed; flagged in the file's own comment in case Vlad wants a fresh design pass later.

- ~~FunnelView's Excel export: let a recruiter pick one specific role and get a report scoped to just that role~~ — **resolved, 2026-08-04.** Confirmed with Vlad: driving this through the existing on-screen role dropdown (rather than a separate export-only picker) satisfies the ask. `handleExport()` in `app/funnelview/page.tsx` already scoped to `activeProject` once a role is picked (skips "By Project" sheet, adds role slug to filename) — no code change was needed, just confirmation.
- **A chatbot for recruiters to pull information directly from Cirot data** — still a future project, no scope/design discussion yet. Vlad confirmed 2026-08-04 to keep this as a backlog note only, not scoped this round.
- **2026-08-06 — `supabase-migration-current-role.sql` (now 4 columns: current_company/current_title/total_experience_summary/linkedin_url) still needs to run — genuinely nothing has changed here except the column count.** Code degrades gracefully pre-migration (see session-log.md's round 72 entry for the real incident this caused when it lagged behind the code). **Needs Vlad to run it** — he explicitly chose to keep doing this himself rather than have Claude Code run it via the service-role key.
- ~~2026-08-04 — AI Builder project's existing candidates need the "Regenerate trajectories" button clicked once, after the migration runs~~ — **superseded, 2026-08-06: still true, but now ALSO covers totalExperienceSummary/linkedinUrl**, not just Current Company/Title (`getCurrentRoleStatus` was widened — see round 72 entry). Still safe to click repeatedly.
- ~~2026-08-04 — Current Company/Title/Total experience only populate via the backfill button, NOT the live scoring flow~~ — **resolved, 2026-08-06.** Vlad gave explicit sign-off to touch `scoreCandidate.ts`/the screening flow for this specific addition (AskUserQuestion, chose "during screening" over the report-generation-time alternative). New screenings now get all 4 fields automatically at scoring time, no button needed — see round 72 entry.
- **2026-08-07 — `supabase-migration-target-company-boost.sql` (adds `target_company_matches`) needs to run — but ONLY for the "which companies matched" display/badge, not for the score effect itself.** The +5 boost applies to `score` (an existing column, no migration needed) immediately once this round merges. Pre-migration, the boost still works, it just can't persist WHY a candidate's score moved for later viewing — `result.targetCompanyMatches` still shows on the immediate post-screening ResultCard (in-memory, no DB dependency), it just won't survive a reload until the migration runs.
- **2026-08-07 — Target-company matching is a plain case-insensitive substring check of the whole resume text, not scoped to an "Experience" section and not alias-aware** (e.g. "Google" won't catch "Alphabet Inc." or "Google LLC"). Deliberate simplification for auditability/simplicity, per Vlad's own literal wording ("if it matches with the candidate's resume") — flagging in case false positives (e.g. a company mentioned only in passing, not as an employer) turn out to be a real problem worth a more precise match later.
- **2026-08-07 — `app/api/screenings/save-one/route.ts` (the other `saveScreening()` caller) doesn't pass `targetCompanies` yet, so that path doesn't get the score boost.** Not a do-not-touch file, just out of scope this round — needs a matching small addition if Vlad wants the boost to apply there too.
- **2026-08-07 — Score boost companies UI lives in a new, separate card in the Filters tab, not inside the existing LinkedIn Recruiter filters view** (which was deliberately trimmed to 5 filters a while back, see [[jd_filter_output_feedback]], and never showed target companies). Worth confirming with Vlad this placement reads clearly — the underlying data (`wide`/`narrow.targetCompanies`) is shared with the LinkedIn filter export, editing one place affects the other.

- ~~Two migrations now needed before Transfer can commit at all: `supabase-migration-status-transferred-check.sql` (new, blocking) and `supabase-migration-transfer-to-project.sql` (non-blocking, degrades gracefully)~~ — **both confirmed run, 2026-07-29 system health audit.** Verified directly against the live schema (not just asked): `screenings.transferred_to_project_id`/`transferred_to_screening_id` both exist and are queryable. Every transfer mode and the "view destination" link should both work now.
- ~~Round 44 (superseded note): `supabase-migration-transfer-to-project.sql` still needs to run~~ — see the entry above, confirmed run. Still true and unaddressed: the "All Candidates" page (`app/candidates/page.tsx`) and `FunnelView` weren't wired with Transfer props/categorization — deliberate scope cut, Pipeline only for now.
- **2026-07-29 (system health audit, ahead of multi-team scaling) — new denormalization gap found: 50 `resume_fingerprints` rows have `team_id = NULL`, all in project_id=1, all created 2026-07-09 through 2026-07-15 (same rollout window as the earlier `screenings.team_id` gap).** This silently makes those 50 fingerprints invisible to Phase 1.4's cross-project fraud/history-alert matching (`findCrossProjectMatch` filters `.eq("team_id", teamId)`, which never matches NULL) — a real gap today, not just a future multi-team risk, though it becomes more consequential once more teams exist. Confirmed safe and unambiguous to backfill: every one of the 50 rows' own parent screening (via `screening_id`) already has a correct `team_id` (the `screenings.team_id` gap is now fully backfilled, 0 nulls) and no project_id disagreements. Script written: `supabase/migrations/one-off-backfill-null-team-id-resume-fingerprints.sql`, same preview-then-transaction pattern as the sibling `screenings.team_id` backfill. **Needs Vlad to run it** in the Supabase SQL editor.
- ~~`screenings.team_id` backfill (116 orphaned rows, `one-off-backfill-null-team-id-from-project.sql`)~~ — **confirmed run, 2026-07-29 system health audit.** Verified directly: 0 rows now have `team_id = NULL` in `screenings`. Recruiter/admin Pipeline visibility should now match exactly.
- **2026-07-29 — local-only commit `e23d748` ("fix: two near-term gaps flagged in the fraud/validation logic review") found stranded during the system health audit, never pushed to origin at all.** Fixes: (1) `compareFingerprints()`'s exact-skillsHash-match floor now requires a low corroboration bar first (same idea as the name-match fixes below), (2) system-wide rejection history now tags matches `name_and_resume` vs `name_only` using `resume_content_hash`, surfaced in `ResultCard`'s rejection banner. This landed on the `fix/interview-doc-label-activity-timeline-and-200-cap` branch AFTER that branch's PR #51 (Transfer feature) was already merged to `main` — same "stranded commit" pattern as several past handoffs. Also has 2 more local, uncommitted, unpushed fixes stacked on top from this same session (`findNameMatchInProject` and `findCrossProjectNameMatch` fingerprint corroboration — both real, verified fixes for a false-positive "Name match"/cross-project-mention bug Vlad hit live with two different "Charlie Wang" candidates). **Needs Vlad:** decide whether to commit the 2 staged fixes, then push everything and open a fresh PR — `tsc --noEmit` clean with all of it stacked, do-not-touch files reconfirmed zero diff against `origin/main`.
- ~~2026-07-29 — dead route `app/api/projects/[id]/better-fit-matches/route.ts` still on disk, returns 410, safe to delete~~ — **confirmed gone, 2026-08-19.** File no longer exists (someone with real delete access removed it between sessions). This note was stale.
- **2026-08-19 (Cowork architecture pass) — 3 more dead-code items found and handled, same `rm` permission block as the entry above.** `app/api/generate-question/route.ts` was a real, live, functional Claude-calling route (screening-question generator) with zero callers anywhere in the app — confirmed via full-repo grep, never flagged before. Stubbed to a 410 (same pattern as `compare-resumes`) rather than deleted, since this sandbox's `rm` fails with the same "Operation not permitted" seen before. Also confirmed still-undeleted and still genuinely dead: `app/api/compare-resumes/route.ts`, `lib/compareResumes.ts`, `app/compare/page.tsx`, `app/compare/[screeningId]/page.tsx` — all already inert stubs (per `compareResumes.ts`'s own comment, written for exactly this situation), just physically still on disk. **Needs Vlad or a real-machine session to `rm -rf app/compare app/api/compare-resumes app/api/generate-question lib/compareResumes.ts`** — `npx tsc --noEmit` confirmed clean both before and after the `generate-question` stub, nothing else references any of these 5 paths.
- **2026-07-29 — Round 44's redesigned Transfer (dedicated button + optional re-score) is code-complete and type-checked but NOT live-tested.** Needs Vlad to click through all three paths on a real candidate: (1) transfer to a project where the candidate already exists (should link, not duplicate), (2) transfer with the re-score toggle off (should carry the old score over, same as round 43), (3) transfer with the toggle on (should show a real new score from `/transfer/preview` before confirming, then persist that exact score via `/transfer`). Also worth specifically re-testing the exact repro that failed before (transfer without the migration run) to confirm it now completes cleanly instead of throwing.
- **2026-07-29 — New bug report: recruiter-status filter under `test@brillio.com` shows only 111 candidates vs. 225+ actually scanned in the pipeline.** Not yet investigated with a live query. Likely candidate: the same legacy `user_id: null` attribution gap from the since-fixed 2026-07-20 recruiter-attribution bug — any screening saved before that fix has no `user_id`, so a recruiter-scoped filter (as opposed to the unfiltered "All" count) would undercount by exactly that gap. Needs Claude Code or Vlad, with real DB access, to run a count of `screenings` where `user_id IS NULL` for this project/team to confirm or rule this out before any fix is attempted.
- **2026-07-29 — New bug report: the "recent screenings" (Newest sort) filter doesn't show archived candidates when the status filter is set to "All."** Root cause not yet located — likely `statusFilter === null` ("All") in `PipelineTab` doesn't actually mean "every status," just "no status filter applied on top of whatever else is already excluding archived," or a separate archived-exclusion path exists elsewhere in the filter chain. Needs a closer read of `filteredScreenings`'s filter predicate (as distinct from its sort comparator, which the round-43 "Newest" sort fix already touched) before changing anything.
- **2026-07-29 — `maxDuration` raised 60 → 300 in `app/api/screen-resumes/route.ts` to address a reported screening timeout — needs Vlad to confirm this doesn't fail the deploy.** Setting `maxDuration` above 60 only works on Vercel Pro/Enterprise, or Hobby with Fluid Compute explicitly enabled — on plain Hobby it fails the DEPLOYMENT outright rather than silently clamping. Also shipped alongside it: a real efficiency pass in `saveScreening()` (lib/screenings.ts) — parallelized 4 previously-sequential match lookups, folded `resume_content_hash`'s write into the main insert instead of a separate round trip, and stopped re-resolving the same acting user/project once per resume in a batch. All verified via `npx tsc --noEmit` (clean) and a careful manual re-read (logic confirmed unchanged, just reordered/parallelized) — `npm run build` itself couldn't run in this sandbox (same "Bus error" as the batch-page work). **Needs Vlad/Claude Code:** confirm the Vercel plan/Fluid-Compute situation before merging (drop back to `maxDuration = 60` if neither applies — the efficiency pass alone is still a real improvement), then live-test a 3-resume batch and check Vercel's function logs for actual invocation duration to see if this actually fixed the reported error.
- ~~`supabase-migration-batch-id.sql` MUST run before this branch deploys~~ — **run by Vlad, confirmed 2026-07-29** (verified via direct REST query before/after).
- ~~`npm run build` cannot be verified from this sandbox for the batch-page change~~ — **confirmed clean, 2026-07-29**, on a real machine with real Supabase access: all 44 routes generated, including `/projects/[id]/batches/[batchId]`. The prior sandbox's "Bus error" was environment-specific, not a code issue.
- ~~Live-testing the cross-project/cross-team fixes needs to happen outside this sandbox~~ — **done, 2026-07-29**, via a real Playwright-driven click-through against the actual running app (see session-log.md's matching entry for full detail). IDOR fix, cross-team 403, fingerprint team-scoping, and the cross-project-fit gate (including its manual override) all confirmed working live, not just via code review.
- ~~Real, confirmed bug: the batch page's "Back" promise breaks the moment a recruiter screens more resumes in the same visit~~ — **fixed, 2026-07-28.** Asked Vlad which direction fit the multi-team goal better rather than picking blind; he asked for the tradeoff reasoning first. Chose: stop clearing `currentBatchId` on "Screen more" (`handleReset()`), only ever overwrite it once a real new batchId comes back — it's client-side-only React state either way, so this has no multi-user downside, and it beats the alternative (falling back to the sessionStorage-restoring Screen tab) since that's exactly the browser-tab-local mechanism this whole feature was built to replace. Committed `051e91b`, `npx tsc --noEmit` clean. See session-log.md's round 40 entry.
- **2026-07-29 — 4 docs files show as unstaged local deletions, cause unknown, not investigated (out of scope this session).** `docs/Cirot-Database-Migration-Runbook.md`, `-Enterprise-Teams-Onboarding.md`, `-Lever-Integration-Brief.md`, `-Load-Testing-Assessment.md` — all still exist in git history (`HEAD`), just missing from the working tree. Left alone since unrelated to the batch-page task; Vlad can restore with `git checkout HEAD -- docs/` if they were deleted by accident, or confirm intentional if not needed anymore.
- **2026-07-28 — Pre-pilot cross-project audit: 3 design tradeoffs need your call, 1 real security gap already fixed.** Fixed without asking (matches an established, unambiguous pattern): `check-existing/route.ts` was missing the `canAccessProject` check every sibling route already has — any authenticated user on any team could've read another team's candidate list by guessing a `projectId`. Not fixed, needs your decision: (1) the cross-project fit check scores candidates against other projects with zero calibration examples, unlike a real screening into that project — should it use `listCalibrationExamples` like the main route does, even though that's slower/costs more? (2) fingerprint matching flags two candidates as a duplicate/fraud match whenever their skills list hashes identically, regardless of how different the rest of their resume is — a false "Known fraud pattern" is a real reputational risk in front of a client; should the skills-hash-alone floor be removed or weakened? (3) the Fit Suggestion feature is fully suppressed whenever a cross-project name match exists, but that's an unverified name-string match (no email/phone/content check) — should a plain name collision really block a potentially valuable "check other roles" suggestion? Full detail in session-log.md's 2026-07-28 entry.
- **2026-07-27 — Prod-data reassignment (test@brillio.com → tetiana.nytsyk@brillio.com) prepared, NOT run.** `supabase/migrations/one-off-reassign-test-brillio-to-tetiana.sql` reassigns `screenings.user_id` only — deliberately not `screening_actions` (audit trail) or `calibration_examples`. No live Supabase access from this sandbox; this is destructive against real pilot data, so it was written as a script with a preview step, a one-user-per-email safety check, and an explicit transaction (inspect before `COMMIT`) rather than run directly. **Needs Vlad (or Claude Code with real DB access) to review and run it**, and to confirm whether `projects`/`calibration_examples` ownership should also move — the literal ask was "candidates," so those were left alone on purpose.
- **2026-07-27 — Rescreen (new `/api/history/[id]/rescreen`) does not touch status, even if the new score crosses the project's auto-archive threshold.** Deliberate: a rescore refreshing a stale JD-vs-candidate match shouldn't silently archive someone a recruiter already has in a later pipeline stage. **Confirm with Vlad** this is the behavior he wants once he's tried it — the alternative (auto-move to archived on a below-threshold rescore, same as a brand-new below-threshold save) is a one-line change in the new route if he'd rather it did.
- **2026-07-27 — New `/candidates/[id]` full-result page has no visible "you are here" indicator distinguishing it from a normal Pipeline card** beyond the subtitle ("Screened in '[project]'") and a Back link. If Vlad wants it to look more obviously like a standalone/shareable page (vs. an accidental duplicate of the Pipeline card), that's a quick follow-up, not a structural change.

- **2026-07-27 — New cross-project name-match mention is deliberately ephemeral (screening-moment only) — confirm with Vlad whether it should also persist.** `findCrossProjectNameMatch()` (`lib/screenings.ts`) only ever shows on the fresh Screen tab result, right after scoring; it's never written to the DB, so reloading Pipeline/All Candidates never shows it, unlike `historyAlertType`. Built this way to match the literal ask ("during the screening"). If Vlad wants this to also show up later (like a standing badge), that needs a real migration (new column) and is a bigger change than this session did — check before building it.
- **2026-07-27 — Confirm Supabase's project-level `db-max-rows` setting isn't independently capping `listScreenings()` now that the code-level `.limit(200)` is removed.** This couldn't be checked from the sandbox — needs Vlad to look at the Supabase dashboard's API/PostgREST settings directly. If there's a separate server-side cap sitting underneath, counts could still silently stall at whatever that number is, just higher than 200. Also worth deciding, not urgently: `listScreenings()` still fetches every matching row in one request with no real pagination (limit/offset) — fine at current volume, a real architecture decision (not a quick fix) once Brillio's full pilot volume grows further.
- ~~`supabase-migration-cross-ref-doc-type.sql` needs to be run before the cross-reference doc mislabeling fix actually takes effect.~~ — **confirmed run, 2026-07-31 (verified live: `screenings.cross_ref_is_linkedin` column exists).** Still worth a live-test pass: a second-resume cross-reference upload should show "Cross-Reference," a real LinkedIn PDF export should still show "LinkedIn."
- **2026-07-20 — Calibration weighting thresholds (4 / 8 examples) are a first judgment call, not measured data.** `calibrationWeightGuidance()` in `lib/scoreCandidate.ts` — do-not-touch exception, Vlad's explicit ask, full reasoning in decisions-log.md's matching entry — picks 4 and 8 as the points where JD-vs-calibration trust shifts. Same category as the earlier -8/-25 credibility deduction magnitude: reasonable-feeling, not derived from anything measured. **Confirm with Vlad** once a role has a real, growing calibration set (ideally past 8 examples) whether the curve actually produces the behavior he wants, or needs different thresholds/a smoother scale.
- **2026-07-20 — Calibration example cap (6 per scoring call) is also a first judgment call, same category as the 4/8 thresholds above.** `MAX_CALIBRATION_EXAMPLES_PER_SCORE` in the new `lib/calibrationSelection.ts` — built ahead of real need, for enterprise-scale calibration sets (see decisions-log.md's matching entry). **Confirm with Vlad** once a real project's example count actually exceeds 6: does the recency+outcome-balanced subset feel representative, or does the cap need to be higher, or does it need the similarity-based retrieval upgrade (embeddings) flagged as the deliberate next step in that same entry?
- ~~Auto-archive default reason doesn't match any real picker option~~ — **fixed 2026-07-17.** "Role alignment" restored as a real 5th `ARCHIVE_REASONS` entry, `DEFAULT_AUTO_ARCHIVE_REASON` now points at it. On `fix/archive-reason-and-linkedin-badge-consistency`, pushed, not yet merged.
- ~~LinkedIn source-indicator badge is inconsistent across all three places it renders~~ — **fixed 2026-07-17, same branch.** All three (Pipeline tab, `app/candidates/page.tsx`, `components/ResultCard.tsx`) now render the same SVG "in" mark.
- **2026-07-17 handoff: only 5 of ~18 uncommitted follow-ons were in scope; Vlad chose surgical extraction over doing everything or bundling.** Asked directly (AskUserQuestion) rather than guessing, since several files (`app/projects/[id]/page.tsx`, `components/SiteHeader.tsx`, `lib/screenings.ts`) had the 5 requested changes mixed inline with ~13 other unrelated follow-ons with no clean line boundary. Chose: extract only the 5 named branches by hand, leave everything else stashed. All 5 pushed and individually build-verified in true isolation — see state.md's 2026-07-17 section for the full branch list, extraction notes per file, and compare URLs. **Needs Vlad:** review/merge Branch 1 first (highest severity, real cross-team data leak), then Branch 2 (depends on Branch 1's new helper). Branches 3-5 are independent, can merge in any order.
- **The other ~13 follow-ons from that same session are NOT lost — sitting in one git stash on top of `main`, message "2026-07-17-handoff: full working tree, pending 5-branch surgical split."** Includes a whole in-app feedback form (with its own migration, not yet run), fraud/duplicate "ring" clustering UI, a stop button for in-progress screening batches, a smart candidate filter bar upgrade, and several smaller fixes — full list in state.md's 2026-07-17 entry. Will need its own scoping/branching pass, likely several more branches given how entangled some of it is with what just shipped.
- **`tracker.screening_id`'s `ON DELETE CASCADE` rule is unverifiable from any sandbox — needs Vlad to check the Supabase dashboard directly** (Table Editor → tracker → screening_id column → FK delete rule). The `tracker`/`screenings`/`calibration_examples`/`access_requests`/`feedback` tables predate the migration-file convention (created directly in the dashboard), so there's no CREATE TABLE to check in code. If there's no cascade, deleting a screening either leaves a harmless orphaned tracker row or throws a swallowed FK-violation error — worth knowing which before it matters.
- ~~Three-epic feature batch (2026-07-15) — built, verified only via code-read, not yet build-checked or live-tested~~ — **merged, 2026-07-16.** Both `feat/team-project-management` (PR #19) and `feat/pipeline-scoring-design-system-and-latency` (PR #20) confirmed merged into `main`.
- ~~2026-07-16 handoff produced 4 branches, not the 3 described~~ — **all 4 merged same day.** PR #21-#24, confirmed via `git log origin/main`. Still needs live-test: Branch 1 (tracker date) and Branch 3 (LinkedIn — needs a real LinkedIn PDF upload).
- ~~Real, still-open bug found while investigating the 2026-07-16 handoff: `fade-in-up` keyframe should end on `transform: none`, not `translateY(0)`~~ — **fixed 2026-07-17, then found re-regressed by the same stale-base-branch pattern and fixed again the same day.** See state.md's 2026-07-17 recovery entry.
- ~~"Phase 2.3" was named as the next thing to build in today's handoff, with zero scope given~~ — **Vlad confirmed Cowork will pick up Phase 2.3, not Claude Code.** No action needed here.
- **Live bug on `main` right now (found 2026-07-15): saving a Location or archive reason on a candidate silently doesn't survive a reload.** PR #17 (pipeline status restructuring) merged early and stranded two already-pushed commits (`920a658`, `cf28da3`) that wire `location`/`archive_reason` into the shared read paths — confirmed directly against `origin/main`'s `lib/screenings.ts`, which still lacks both fields on read. Not data loss (the write side works fine, it's in the DB) — just never read back, so the UI shows it reverted after refresh. Fix recovered onto `fix/location-archive-reason-read-wiring` and pushed as its own PR (unrelated to the credibility branch below) — needs Vlad's review/merge. Worth prioritizing since it's live and user-visible right now, not a code-complete-but-unmerged item like the rest of this list.
- **Credibility Checker accuracy overhaul + score impact (2026-07-15) — the exact scoring formula's numbers are my judgment call, not something Vlad specified.** He confirmed the *policy* via AskUserQuestion (only material discrepancies count, capped deduction) but not the specific magnitude. I picked -8 points per material discrepancy, capped at -25 total (`computeCredibilityScoreDelta()` in `lib/assessCredibility.ts`) — reasonable-feeling defaults, not derived from any stated requirement. **Confirm with Vlad** whether -8/-25 feels right once he's seen it live-tested against a real material discrepancy (e.g. does a single real red flag docking 8 points feel proportionate, or should one material discrepancy hurt more/less?). Committed to `feat/credibility-accuracy-and-scoring` and real-build-verified (`npm run build` clean, not the sandbox mount) — still needs Vlad's own click-through pass in the app plus this magnitude confirmation before merge.
- ~~Education date-tolerance rule needs re-verification against Vlad's two test cases~~ — **verified, found it didn't hold, fixed for real, 2026-07-15.** Ran the actual `assessCredibility()` function repeatedly (not a read-through) — the PhD/Expected-2029 case matched cleanly every time, but the MS-CS-2024 case was genuinely ~50/50 across 6 runs despite the prompt's own worked example covering it. Root cause was tool-schema field order (the model's `note` field showed correct arithmetic but came after the `status` field it should have justified), not the wording itself — fixed with a new `reasoning` field ordered before `status`. Re-verified 12/12 clean on both cases plus 3/3 correct on a deliberate negative control. See decisions-log.md for the full trail.
- **Teti feedback batch (2026-07-15) — 3 bugs fixed, needs live-test + commit/push/PR.** Bug 1 (filename-collision skip-scoring), Bug 2 (missing candidate name via Claude PDF-vision fallback), Bug 3 (company-alias false positives in credibility check) — see state.md's "Fixed, 2026-07-15" section for full detail and the exact live-test checklist. None of the three has been live-tested yet (Bug 2 only verified via an isolated `tsc --noEmit` compile check + direct code re-read); this session's sandbox mount was not used for `npm run build`/`tsc` against the real tree this pass. Needs Vlad's own machine for real build verification, then the live-test checklist, then Claude Code handoff for branch/commit/PR (do-not-touch note: `parseResume.ts` should be zero diff this round — Bug 2's fix lives in `screenings.ts` + the new `extractCandidateNameFallback.ts`, not in the do-not-touch parser).
- ~~`components/AlreadyScreenedCard.tsx`'s "possible_update" credibility comparison has the same backwards resume/cross-ref direction and data-corruption side effect as the ResultCard bug~~ — **resolved 2026-07-15, by removal rather than by fixing the direction.** Investigating this turned up that the whole `possible_update` classification was dead weight: Bug 1's fix (2026-07-15) already meant `app/projects/[id]/page.tsx`'s upload filter only ever routes `status === "duplicate"` items to `AlreadyScreenedCard` — so its `possible_update` branch (and the backwards/corrupting Compare button inside it) had been unreachable code since that fix shipped, not a live bug. Separately, Vlad questioned whether filename-matching was even a good signal at all, given it flagged unrelated candidates who both happened to have generic download filenames like "Resume (16).pdf" (normalizes to the bare word "resume" — zero identity signal). Decided to retire filename-matching entirely rather than patch it: `possible_update` removed from `CheckExistingResult`, `normalizeFileName()` deleted, `AlreadyScreenedCard` simplified to duplicate-only, and the `ResultCard.tsx` Compare action moved onto the more reliable post-score `nameMatch` signal (real identity comparison, not a filename string) instead. See decisions-log.md and state.md's 2026-07-15 entries for the full reasoning trail. Not yet live-tested.
- ~~Pipeline status restructuring (Interview removed, Screening becomes the TrackerStage container) — spec captured, not built~~ — **built same day, 2026-07-15, per Vlad's "let's keep going with that then."** See state.md's "Shipped (code-complete, NOT yet live-tested)" section for the full build (CandidateStatus enum change, merged StatusStageControl, drawer changes, auto-archive, two migrations). Confirmed via `npx tsc --noEmit` clean, but genuinely NOT live-tested — needs Vlad to run both migrations (in order) and work through the 8-item checklist in state.md before this is considered done. Vlad also corrected a misunderstanding about this build the same day: candidates already sitting in a Tracker stage keep their exact stage after the migration (the migration never touches the `tracker` table) — this was already true by construction, just clarified in state.md's checklist wording after he flagged it.
- **Archive reason (2026-07-15) — two design decisions made without Vlad's confirmation because `AskUserQuestion` errored out twice.** (1) The reason picker was placed inline on the `StatusStageControl` status pill rather than in a separate drawer/detail view. (2) Auto-archived (below-threshold) candidates start with no reason selected rather than getting an automatic "Below threshold" 5th option. Both were the "Recommended" options in the question that failed to send — see decisions-log.md for the full reasoning. **Confirm with Vlad next session** whether these match what he actually wanted; if not, (1) is a StatusStageControl.tsx change and (2) is a one-line change in `lib/screenings.ts`'s auto-archive branch.
- ~~Save-all + rejection-history feature needs commit/PR + live-test~~ — **fully closed, 2026-07-10.** Commit `967d159` on `feat/save-all-and-rejection-history`, merged to main. Build clean, do-not-touch diff clean. Vlad confirmed the full live-test checklist (fit-suggestion banner above Career Trajectory with no "Save anyway," the rose "Previously rejected" banner, reason-field persistence across a reload, the Excel export's new column) — all working.

- ~~Phase numbering is inconsistent across documents~~ — **partly resolved 2026-07-09.** The main source of ambiguity, outreach drafting being called "Phase 3" in some places and "Phase 5" in others, went away when outreach drafting itself was dropped from the roadmap (see decisions-log). Still worth noting: the dev log separately uses "Phase 2/3" for the architecture redesign (problem statement + locked design phases, a different scheme entirely from roadmap phases) — low priority since it hasn't caused real confusion.
- **Impact metrics still not measured.** The BLUEPRINT and Impact Report reference a "60% throughput improvement" as validated, but the dev log's own "what's next" lists "measure screening time per role and TA false positive rate" as still outstanding as of the last entry. Worth checking whether the Impact Report's number predates real measurement or if measurement happened outside the dev log.
- **`supabase-migration-interview.sql` and `supabase-migration-projects.sql` show as an uncommitted local deletion in the working tree on `main`** (as of 2026-07-08). Vlad confirmed both are intentional/already-run — low priority, but the deletion has never actually been staged/committed, so `git status` will keep flagging it until someone runs `git rm` + commit, or restores the files.
- ~~Migration SQL files could be grouped into `supabase/migrations/`~~ — **done 2026-07-09, committed.** All 11 `supabase-migration-*.sql` files moved to `supabase/migrations/`, no renaming. The four root-level `.docx` narrative docs and `Cirot_Prior_Art_Document.pdf` (previously in `memory/`) all moved to `docs/`. The two stale `supabase-migration-interview.sql`/`supabase-migration-projects.sql` files were restored via `git show main:...` into `supabase/migrations/` rather than left deleted. All of this landed in commit `b22b2ab` on `phase-1-3-teams-architecture` (committed by Claude Code, confirmed via `git status` — this session's own working-tree `mv` never got committed directly, Claude Code redid/confirmed it cleanly).
- **Teams migration (`supabase-migration-teams.sql`, Phase 1.3) puts every existing user into one "General" team before Vlad has a chance to split them.** Since team-based visibility is shared (not per-user like today), running the migration as-is means Teti immediately sees all of Vlad's existing projects/candidates and vice versa, the moment it's run — not a bug, just a consequence of the backfill needing *some* default. Before running it, decide: stay in one shared team (fine if that's actually the intent), or create a second team and move one recruiter into it right after migrating, before real use resumes.
- **Phase 1.4's migration is now run** (confirmed 2026-07-09, after it caused an app-wide candidate-visibility outage — see decisions-log). Still needs live-testing for the team-isolation boundary specifically: same resume/swapped-identity into two projects in the same team should show "Previously seen"/"Known fraud pattern"; into a project in a *different* team should show nothing. The second case matters more — a leak across the 1.3 team boundary would be a real privacy issue, not just a missing feature.
- **Confirm the `team_id` backfill queries were run.** Two straggler-cleanup queries were given to Vlad during the 2026-07-09 incident: one for `screenings.team_id` (given before the outage was diagnosed) and one for `resume_fingerprints.team_id` (only runnable after the 1.4 migration). Unclear if the first one actually got run before attention moved to the bigger outage — worth Vlad re-running both now that the migration is in, to make sure no screening/fingerprint rows are still sitting on `team_id = NULL` from the `getProject()` bug window.
- ~~No PR was ever actually created for `phase-1-3-teams-architecture`~~ — **wrong, corrected 2026-07-09.** Vlad was in fact creating and merging a separate PR after each Claude Code push (via the compare-URL fallback each session gave, since `gh` was never on PATH in that environment) — PR #4 (1.3), #5 (1.4 + team chips), #6 (1.5), on top of #1–#3 for earlier features. He just didn't narrate each merge back into this conversation, so from this side it looked like nothing had happened, leading to a redundant "create one combined PR" instruction on already-merged work. Confirmed via GitHub's PR list: 0 open, 6 closed, all merged, main fully caught up through Phase 1. **Lesson:** when a PR's fate is unclear across a gap in the conversation, ask the user to check GitHub's PR list directly (open AND closed) before drafting more instructions — don't assume silence means nothing happened.
- ~~`Cirot_Prior_Art_Document.pdf` corrupted on disk~~ — **fixed 2026-07-09.** The disk copy was truncated (no `startxref`/`%%EOF`, broken trailer) but an intact version existed in git at commit `3d78ea5` (dated July 7, 2026) — restored via `git show 3d78ea5:memory/Cirot_Prior_Art_Document.pdf`, verified clean with `qpdf --check`.
- ~~Missing "Novel Implementation C" entry~~ — **added 2026-07-09.** No generator script existed in the repo, so the document was rebuilt from scratch with `reportlab` (Platypus), reproducing Sections 1–2 and Implementations A/B verbatim from the restored text, adding IMPLEMENTATION C (Identity-Blind Content Fingerprinting for Cross-Submission Fraud Detection — conception July 2026, reduction to practice July 8, 2026, extended July 9, 2026 for cross-project matching), two new Evidence Timeline rows, and updated Section 6 to cover a provisional patent application for C alongside A. Document date field, declaration paragraph, and signature date all updated to reflect the July 9, 2026 revision while preserving the original July 7, 2026 date for context. Verified with `qpdf --check` and by rendering all 7 pages to images and visually reviewing them. Assessment for 1.3–1.5 stands: only 1.1/C needed a new entry — 1.3 (Teams) is standard multi-tenancy, 1.4 directly reuses 1.1's fingerprinting technique, 1.5 is prompt composition over existing signals.
- ~~FunnelView is merged to main but still needs live-testing~~ — **confirmed 2026-07-09.** Validated against 78 real candidate screenings; funnel numbers confirmed accurate. Non-admin 403 and mobile layout weren't separately re-confirmed in this pass — low priority given the page gate hasn't changed since it was built.
- ~~Phase 2.1 (Cross-Project Fit Suggestion) needs a fresh live-test round before commit/push/PR~~ — **resolved 2026-07-10.** The auto-fire gate was rebuilt four times (must-have score → overall score → keyword overlap → cheap Claude classification call) before one worked — full trail in decisions-log.md/state.md. A real `useEffect` bug (self-cancelling dependency loop) was also found and fixed along the way. Vlad confirmed the final version working on the exact previously-failing candidate. Committed as `66bf129`, pushed to `feat/cross-project-fit-suggestion`, **PR #10 merged to main 2026-07-10** (merge commit `a7f8a26`, confirmed via `git log origin/main` and `git merge-base --is-ancestor 66bf129 origin/main`). **Still open:** a pass on the manual link + Transfer button specifically (not yet separately confirmed, lower priority than the gate itself).
- **Correction, 2026-07-10:** an earlier check this session read the local `main` branch ref (stale, never pulled) and concluded 66bf129 was *not* merged — wrong. The GitHub compare page showing empty was the actual signal it *was* already merged (no diff between an already-merged branch and main). Lesson: when checking merge status in this sandbox, compare against `origin/main` (after `git fetch`), not local `main` — the local mount's git state has been unreliable all session (see the two entries below) and can silently be behind.
- **FunnelView dropdown / admin team-deletion recovery attempt (2026-07-10) landed a PR, but not the actual code.** Vlad ran the handoff commands on his own machine and pushed `feat/funnelview-dropdown-and-team-delete`; PR #12 merged to main (`206c9b1`) — but checking the PR's Files Changed on GitHub showed only 3 files under `memory/` (+33/-7), i.e. this session's own doc edits that happened to be sitting dirty in the working tree. None of the 6 target files (`app/funnelview/page.tsx`, `lib/funnelview/data.ts`, `lib/funnelview/types.ts`, `lib/teams.ts`, `app/api/admin/teams/[id]/route.ts`, `app/admin/users/page.tsx`) are in the diff. Likely cause: `git stash pop` didn't apply cleanly (or applied to the wrong branch) and `git add -A && git commit` silently committed only what was actually present. **Status unknown, low priority — not blocking anything live.** Vlad was asked to run `git stash list` to check whether the original stash entry still exists (safe, nothing lost) or is now empty (6 files of work were dropped and would need rebuilding); as of 2026-07-10 he deprioritized checking this ("let's keep moving") since neither feature is urgent. Revisit `git stash list` next session before assuming the work is gone.
- **This session's sandbox couldn't reliably run `git` or `tsc` against the mounted `C:\Portfolio\Cirot` repo** — `git rev-parse HEAD` failed with the commit object missing from `.git/objects` even though `refs/heads/main` points to a real-looking SHA, and `tsc` read several files (including untouched ones like `app/layout.tsx`) as truncated mid-line, throwing cascading fake syntax errors. The Read/Edit file tools showed the same files as complete and correct, so this looks like a mount-layer artifact specific to the shell, not real repo corruption — but it means Phase 1.3's changes were never committed, branched, or PR'd from this session. Vlad needs to review the diff and handle branch/commit/PR from his own machine.
- **Confirmed 2026-07-10, extends to `.git/config` itself.** `git status`/`git branch -f` failed with "bad config line 1 in file .git/config" plus an unremovable `.git/config.lock` (permission denied), while the Read tool showed `.git/config` as complete and well-formed. Read-only git queries (`fetch`, `log`, `branch -a`, `merge-base --is-ancestor` against `origin/*` refs) worked fine and gave trustworthy results — it's specifically write/mutating operations and the local `main`/working-tree state that can't be trusted this session. This is why the stash recovery (item above) was handed off to Vlad's machine instead of attempted here.
- **Also observed 2026-07-10: an Edit tool call that reported success against `open-questions.md` did not actually persist** — a follow-up `Read` in the same session showed the pre-edit text still on disk. Re-applied the edit a second time and verified it landed before moving on. Worth a quick sanity-check (re-read after edit) any time a memory-file edit in this project matters and the mount has been acting up — don't assume a success message means the write landed.
- **`app/api/screenings/regenerate-trajectories/route.ts` has no caller anywhere in the app** (confirmed via repo-wide search, 2026-07-10). It re-runs Claude's trajectory generation for every screening in scope, unconditionally — no check for whether a trajectory already exists — so if it's ever invoked (manually, or from a debug tool not in the repo), it burns a full Claude call per candidate even for ones that don't need it. Left in place rather than removed unilaterally since it's unclear whether Vlad still uses it manually (curl, admin script) — worth a quick check: if it's dead, delete it; if it's a real manual tool, either wire it to a real UI control or add a "skip if trajectory already exists" guard.
- ~~Pre-Screen Duplicate Check needs build-check + live-test~~ — **committed and merged, 2026-07-10.** Migration run, build clean, three live-test rounds done (exact-duplicate skip, post-score name cross-check, persisted "Name match" badge all confirmed working). Claude Code committed `55bfb8e` (14 files) on a fresh `feat/pre-screen-duplicate-check` branch off current `main` (had to recover from being on a stale branch first — stashed, resynced, popped clean) and merged via PR. Do-not-touch files and full diff both reconfirmed clean. **Still open, low priority:** the delete-refresh fix, `possible_update` credibility-comparison path, and genuinely-new-resume path weren't separately re-confirmed after this final merge — worth a quick pass next time the app is used, not blocking.
- **Repeat incident, 2026-07-10: added `name_match_id` to `SCREENING_COLUMNS` (the shared select used by `listScreenings`/`getScreeningsByIds`) before the migration was confirmed run, causing candidates to disappear entirely (Pipeline + All Candidates both empty) — this is the exact same failure mode as the 2026-07-09 outage already logged under [[feedback_migration_sequencing]] in the global memory vault.** Vlad reported "can't see candidates at all"; root cause diagnosed immediately from the pattern match, gave him the migration SQL to run as the fix (`ALTER TABLE screenings ADD COLUMN IF NOT EXISTS name_match_id ...`). **This is a repeated mistake, not a new class of bug** — the standing rule was already documented and should have been checked before adding the column to a live shared select. Going forward: never add a new column to `SCREENING_COLUMNS` (or any other shared select list) in the same step as writing its migration — land the migration, confirm it ran, only then wire the column into a shared query path.
- **Do-not-touch exception: `lib/parseResume.ts` was directly modified 2026-07-10** (new `.doc` branch via `word-extractor`, see state.md). `parseResume.ts` is on 00-index.md's explicit do-not-touch list ("validated core, build around them never through them"). This wasn't caught in the moment — Vlad's own explicit ask ("add real .doc parsing support," chosen directly over two other options), same category of deliberate exception as the `screen-resumes/route.ts` score-gate removal, but unlike that change it was never flagged as an exception at the time. Flagging now so it doesn't read as an accidental do-not-touch violation in the next diff review. Worth Vlad confirming after his own `tsc`/build pass that the pdf/docx/txt branches are still byte-for-byte unchanged (only the new `doc` branch + `WordExtractor` import were added).
- ~~2026-07-17 (even later) — backfill script written for the 66 affected rows~~ — **run, 2026-07-17, 66/66 clean, zero failures.** `scripts/backfill-resume-hashes.ts`, committed on `chore/backfill-resume-hashes`, pushed. Re-verified via REST API: zero screenings now have a null `resume_content_hash`. See session-log.md for the full run output.
- **2026-07-17 (later session) — 8 changes, build-verified and shipped on `fix/resume-hash-write-and-cancel-button-and-ring-highlight`; still needs Vlad's live-test pass in the running app.** Full detail in state.md's matching 2026-07-17 sections and session-log.md's "later still" entries. Summary: (1) Cancel button during resume screening (client-side abort only — server-side Claude call still finishes). (2) `AlreadyScreenedCard` status chip now shows status + archive reason merged into one pill (e.g. "Archived · Role alignment"). (3) Verified (no bug) — cross-reference check save-reliability on `ResultCard`. (4) Verified (no bug) — Pipeline `ScoreBadge` ring updates after a credibility check. (5) Fraud-signal Ring clustering: clicking a Ring chip now highlights + dims instead of removing non-members from the list (was a real regression). (6) Ring-clustered candidates now sort adjacent to each other instead of scattered by score, on both All Candidates and Pipeline. (7) **Real bug fixed:** `resume_content_hash` write in `saveScreening()` used to silently fail whenever `generateFingerprint()`'s Claude call failed, permanently breaking the free duplicate-detection pre-check for that screening — now runs in its own try/catch, independent of fingerprinting. **Still needs:** a full click-through of items 1, 2, 5, 6 in the running app.
- **Twin bug found, not fixed: `lib/calibrationExamples.ts` has the identical unsanitized-filename storage-key bug** that was just fixed in `lib/screenings.ts` (`resumePath = \`calibration/${randomUUID()}/${fileName}\`` — same "Invalid key" failure mode for any filename with `~`, accents, `%`, etc.). `calibrationExamples.ts` is also on the do-not-touch list, so this was deliberately left unfixed rather than silently edited — needs Vlad's explicit go-ahead (or Claude Code) to apply the same `sanitizeStorageFileName`-style fix there. Low urgency until someone actually hits it via the calibration flow specifically (thumbs-down correction upload).
